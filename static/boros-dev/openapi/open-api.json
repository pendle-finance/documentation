{
  "openapi": "3.0.0",
  "paths": {
    "/v1/amm/states": {
      "get": {
        "operationId": "AMMV2Controller_getAmmStates",
        "x-computing-unit": "1+",
        "summary": "Get AMM states by market IDs",
        "description": "Returns AMM states for the requested markets, in the order of the `marketIds` query param. Markets without an attached AMM, missing IDs, and non-whitelisted markets are silently omitted (no 404).\n\n**Limits.** Minimum 1, maximum **100** IDs per request.\n\n**Cost.** **1 CU** for a single ID, **2 CU** for 2+ IDs.\n\nSee [Order Book mechanics](https://docs.pendle.finance/boros-dev/Mechanics/OrderBook) for how AMM state composes with resting limit orders.",
        "parameters": [
          {
            "name": "marketIds",
            "required": true,
            "in": "query",
            "description": "Comma-separated market IDs (e.g. `1,2,3`). Numeric market identifier (1-based). Min 1, max 100 per request.",
            "schema": {
              "example": "1,2,3",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "AMM states",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GetAMMStatesResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid `marketIds` (missing, empty, or contains non-positive integers)"
          }
        },
        "tags": [
          "AMM"
        ]
      }
    },
    "/v1/assets": {
      "get": {
        "operationId": "AssetsController_listAssets",
        "x-computing-unit": "1",
        "summary": "List all supported assets",
        "description": "Returns the list of **whitelisted** assets/tokens recognised by the Boros backend with their on-chain metadata and current USD price \u2014 non-whitelisted assets are excluded. Not paginated \u2014 the response includes every whitelisted asset in one call.\n\n**Filtering.** When `isCollateral` is omitted, both collateral and non-collateral assets are returned. Pass `isCollateral=true` to restrict to assets that can back margin (used as collateral for positions); pass `isCollateral=false` for the inverse.",
        "parameters": [
          {
            "name": "isCollateral",
            "required": false,
            "in": "query",
            "description": "Filter by collateral status. If omitted, returns all assets",
            "schema": {
              "type": "boolean"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "List of supported assets with metadata and pricing",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ListAssetsResponse"
                }
              }
            }
          }
        },
        "tags": [
          "Assets"
        ]
      }
    },
    "/v1/markets": {
      "get": {
        "operationId": "MarketsController_listMarkets",
        "x-computing-unit": "2",
        "summary": "List markets",
        "description": "Returns a paginated list of markets with their configuration and real-time pricing data, ordered by ascending `marketId`. Non-whitelisted markets (`metadata.isWhitelisted = false`) are never returned. Use `isUiWhitelisted` to narrow the set, `isMatured` to filter by maturity status, or `resumeToken`/`limit` for cursor-based pagination.\n\n**Pagination.** `limit` defaults to **50** and is capped at **200**. `resumeToken` encodes the last `marketId` returned; pages are stable across calls because markets are ordered by an immutable numeric ID. The response `resumeToken` is `null` when no more pages remain.\n\nUse `GET /markets/by-ids` to fetch specific markets by ID. See [Glossary](https://docs.pendle.finance/boros-dev/Backend/glossary) for terminology.",
        "parameters": [
          {
            "name": "isMatured",
            "required": false,
            "in": "query",
            "description": "Filter by maturity status. If omitted, returns all markets (including matured).",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "isUiWhitelisted",
            "required": false,
            "in": "query",
            "description": "Filter by UI whitelist status. A market is UI-whitelisted when it is not flagged as a dev/test market. If omitted, returns both UI-whitelisted and non-UI-whitelisted markets. Non-UI-whitelisted markets are not displayed in the UI but are accessible to market makers.",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "limit",
            "required": false,
            "in": "query",
            "description": "Page size.",
            "schema": {
              "default": 50,
              "type": "number"
            }
          },
          {
            "name": "resumeToken",
            "required": false,
            "in": "query",
            "description": "Resume token from a previous response to fetch the next page.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "List of markets with configuration and real-time pricing",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ListMarketsResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid query parameters (e.g., invalid resumeToken)"
          }
        },
        "tags": [
          "Markets"
        ]
      }
    },
    "/v1/markets/by-ids": {
      "get": {
        "operationId": "MarketsController_getMarketsByIds",
        "x-computing-unit": "1+",
        "summary": "Get markets by IDs",
        "description": "Returns the requested markets in the order of the `marketIds` query param. Non-whitelisted markets (`metadata.isWhitelisted = false`) are never returned, even by direct ID lookup; matured markets remain reachable. Missing or non-whitelisted IDs are silently omitted (no 404). The response `resumeToken` is always `null` (this endpoint does not paginate).\n\n**Limits.** Minimum 1, maximum **100** IDs per request. IDs must be positive integers; duplicates yield duplicate response entries.\n\n**Cost.** **1 CU** for a single ID, **2 CU** for 2+ IDs.",
        "parameters": [
          {
            "name": "marketIds",
            "required": true,
            "in": "query",
            "description": "Comma-separated market IDs (e.g. `1,2,3`). Returns the requested markets in the order provided.",
            "schema": {
              "example": "1,2,3",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Requested markets, in the order supplied. Missing IDs are omitted from the response.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ListMarketsResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid `marketIds` (missing, empty, or contains non-positive integers)"
          }
        },
        "tags": [
          "Markets"
        ]
      }
    },
    "/v1/markets/trades": {
      "get": {
        "operationId": "MarketsController_listMarketTrades",
        "x-computing-unit": "1+",
        "summary": "List market trades",
        "description": "Returns a paginated list of executed trades for a market, ordered **newest-first** by `eventIndex`. Pages are stable across calls \u2014 the cursor advances over a strictly monotone field, so newly-arrived trades appear only on the first page.\n\n**Pagination.** `limit` defaults to **10**, capped at **2000**. Pass `resumeToken` from the previous response to fetch the next page; `resumeToken` is `null` when no more results.\n\n**Cost.** Dynamic: **1 CU per 200 trades** requested (e.g. `limit=10` \u2192 1 CU, `limit=400` \u2192 2 CU, `limit=2000` \u2192 10 CU).",
        "parameters": [
          {
            "name": "marketId",
            "required": true,
            "in": "query",
            "description": "Numeric market identifier (1-based).",
            "schema": {
              "minimum": 1,
              "type": "number"
            }
          },
          {
            "name": "limit",
            "required": false,
            "in": "query",
            "description": "Maximum number of results to return. The parameter is capped at 2000.",
            "schema": {
              "default": 10,
              "type": "number"
            }
          },
          {
            "name": "resumeToken",
            "required": false,
            "in": "query",
            "description": "Resume token for cursor-based pagination. Pass the resumeToken from the previous response to fetch the next page.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "List of executed trades for the specified market",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MarketTradesV2Response"
                }
              }
            }
          },
          "400": {
            "description": "Invalid marketId, pagination parameters, or resumeToken"
          }
        },
        "tags": [
          "Markets"
        ]
      }
    },
    "/v1/markets/order-book": {
      "get": {
        "operationId": "MarketsController_getOrderBook",
        "x-computing-unit": "2",
        "summary": "Get market order book",
        "description": "Returns aggregated order book data for a market, grouped by `tickSize` (tick aggregation step). The response contains `long` (bid) and `short` (ask) sides as parallel arrays: `ia[i]` is the **tick index** at level `i` \u2014 a signed integer, NOT an APR. Multiply by the requested `tickSize` to get the implied APR as a unitless decimal (e.g. `ia[i] = 316` at `tickSize = 0.0001` \u2192 `0.0316` = 3.16% APR; negative values are valid on negative-funding markets). `sz[i]` is the total notional size at that level as a **bigint string** in 18-decimal YU (yield units). Long levels are sorted descending by APR (best bid first); short levels ascending by APR (best ask first). Each side is capped at the **best 50 entries**.\n\n**AMM merging.** When `includeAmm=true`, on-chain AMM positions are merged into the book before the 50-entry cap is applied. When `includeAmm=false` (default), only resting limit orders are returned, also capped at 50 entries per side.\n\n**Tick size.** `tickSize` controls how raw ticks are bucketed before aggregation. See [Order Book mechanics](https://docs.pendle.finance/boros-dev/Mechanics/OrderBook) for the tick-to-rate exponential mapping and matching rules.",
        "parameters": [
          {
            "name": "marketId",
            "required": true,
            "in": "query",
            "description": "Numeric market identifier (1-based).",
            "schema": {
              "minimum": 1,
              "type": "number"
            }
          },
          {
            "name": "tickSize",
            "required": true,
            "in": "query",
            "description": "Tick aggregation step. Adjacent raw ticks are grouped into a single price level before sizes are summed; larger values produce coarser, more readable books.",
            "schema": {
              "enum": [
                0.0001,
                0.001,
                0.01,
                0.1
              ],
              "type": "number"
            }
          },
          {
            "name": "includeAmm",
            "required": false,
            "in": "query",
            "description": "When `true`, on-chain AMM liquidity is merged into both sides of the book and each side is capped at **50** entries. When `false` (default), only resting limit orders are returned.",
            "schema": {
              "default": false,
              "example": false,
              "type": "boolean"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Order book with long and short sides (up to 50 entries each), optionally including AMM liquidity",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OrderBooksResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid marketId or tickSize"
          }
        },
        "tags": [
          "Markets"
        ]
      }
    },
    "/v1/markets/ohlcv": {
      "get": {
        "operationId": "MarketsController_getOhlcv",
        "x-computing-unit": "2",
        "summary": "Get OHLCV chart data",
        "description": "Returns OHLCV (Open, High, Low, Close, Volume) candlestick data for a specific market, ordered ascending by `ts`. Each candle covers a half-open `[ts, ts + timeFrame)` window.\n\n**Timestamp rounding.** `startTimestamp` and `endTimestamp` are floored to the nearest `timeFrame` boundary before the query runs. `endTimestamp` defaults to *now* and is clamped to *now*; `startTimestamp` defaults to `0` (the engine then clamps the window \u2014 see below).\n\n**Gap-filling.** If a bucket has no trades, the candle is emitted with `o = h = l = c` equal to the previous candle's close (or `0` if no prior candle exists) and `v = 0`. Buckets are never skipped.\n\n**Response cap.** The `[startTimestamp, endTimestamp]` window is capped to at most **200** candles at the requested `timeFrame`; if the window is larger, `startTimestamp` is automatically advanced so the response holds the **most recent 200 candles** ending at `endTimestamp`. For longer ranges, page backwards with smaller windows or use `GET /indicators/export` (CSV, up to 10,000 rows).",
        "parameters": [
          {
            "name": "marketId",
            "required": true,
            "in": "query",
            "description": "Numeric market identifier (1-based).",
            "schema": {
              "minimum": 1,
              "type": "number"
            }
          },
          {
            "name": "timeFrame",
            "required": true,
            "in": "query",
            "description": "Candle width. { FIVE_MINUTES : 5m, ONE_HOUR : 1h, ONE_DAY : 1d, ONE_WEEK : 1w }",
            "schema": {
              "enum": [
                "5m",
                "1h",
                "1d",
                "1w"
              ],
              "type": "string"
            }
          },
          {
            "name": "startTimestamp",
            "required": false,
            "in": "query",
            "description": "Start timestamp (Unix seconds). Floored to `timeFrame`. Defaults to `0`. The window is capped to the most recent **200** candles ending at `endTimestamp` \u2014 if `[startTimestamp, endTimestamp]` is wider, `startTimestamp` is advanced automatically.",
            "schema": {
              "default": 0,
              "type": "number"
            }
          },
          {
            "name": "endTimestamp",
            "required": false,
            "in": "query",
            "description": "End timestamp (Unix seconds). Floored to `timeFrame` and clamped to *now*. Defaults to *now*.",
            "schema": {
              "default": 1786688082,
              "type": "number"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Array of OHLCV candlestick data points for the specified market and time range (max 200 candles)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OhlcvChartResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid marketId, timeFrame, or timestamp range"
          }
        },
        "tags": [
          "Markets"
        ]
      }
    },
    "/v1/indicators": {
      "get": {
        "operationId": "IndicatorsController_getIndicators",
        "x-computing-unit": "1+",
        "summary": "Get market indicators with dynamic selection",
        "description": "Unified endpoint for fetching market indicators on a uniform time grid:\n- `u` \u2014 underlying APR (unitless decimal, `0.085` = 8.5%).\n- `fp` \u2014 future premium (mark APR minus underlying APR).\n- `fgi` \u2014 Fear & Greed Index, returned as `{ v: 0..100, vc: classification }`.\n- `ap` \u2014 asset price in **USD**.\n- `pi` \u2014 perpetual premium index for the market's funding-rate symbol, i.e. the venue-published `(mark - index) / index` premium the underlying funding rate is derived from. Raw per-interval decimal, **not** annualised. Stored as hourly bars, so finer `timeFrame`s repeat the hourly value.\n- `udma:<periods>` \u2014 X-day moving average(s) of the underlying funding rate; periods are integers `1..365` separated by `;`, max 10 periods (e.g. `udma:7;30`).\n\n**Time grid.** `startTimestamp` and `endTimestamp` are **Unix seconds**, floored to the chosen `timeFrame` bucket. `endTimestamp` defaults to *now*; `startTimestamp` defaults to `0`. `endTimestamp >= startTimestamp` is enforced after rounding. Each result row corresponds to one bucket on the grid, ordered ascending by `ts`.\n\n**Response cap.** The `[startTimestamp, endTimestamp]` window is capped to **500** buckets at the chosen `timeFrame`; if the window is larger, `startTimestamp` is automatically advanced so the response holds the **most recent 500 buckets** ending at `endTimestamp`. For longer ranges, use `GET /indicators/export` (CSV, up to 10,000 rows).\n\n**Forward-fill.** Sparse indicators (e.g. hourly `fgi` queried at `5m`) are forward-filled from the most recent known value. `metadata.firstDataTimestamp[indicator]` reports the earliest bucket with real data so callers can render \"no data before X\". `metadata.uLastSettledTimestamp` marks the last bucket where `u` is settled historical data; later buckets use real-time funding rates and may revise.\n\n**Cost.** Dynamic: **1 CU per indicator in `select`.** A `udma:7;30` entry counts as a single indicator regardless of how many moving-average periods are packed into it.",
        "parameters": [
          {
            "name": "marketId",
            "required": true,
            "in": "query",
            "description": "Numeric market identifier (1-based).",
            "schema": {
              "minimum": 1,
              "type": "number"
            }
          },
          {
            "name": "timeFrame",
            "required": true,
            "in": "query",
            "description": "Bucket width on the response time grid. { FIVE_MINUTES : 5m, ONE_HOUR : 1h, ONE_DAY : 1d, ONE_WEEK : 1w }",
            "schema": {
              "enum": [
                "5m",
                "1h",
                "1d",
                "1w"
              ],
              "type": "string"
            }
          },
          {
            "name": "startTimestamp",
            "required": false,
            "in": "query",
            "description": "Start timestamp (Unix seconds), floored to `timeFrame`. Defaults to `0`. The window is capped to the most recent **500** buckets ending at `endTimestamp` \u2014 if the requested window is wider, `startTimestamp` is advanced automatically.",
            "schema": {
              "default": 0,
              "type": "number"
            }
          },
          {
            "name": "endTimestamp",
            "required": false,
            "in": "query",
            "description": "End timestamp (Unix seconds), floored to `timeFrame` and clamped to *now*. Defaults to *now*. Must be `>= startTimestamp` after rounding.",
            "schema": {
              "default": 1786688082,
              "type": "number"
            }
          },
          {
            "name": "select",
            "required": true,
            "in": "query",
            "description": "Comma-separated list of indicators to return. Supported: `u` (underlying APR), `fp` (future premium), `fgi` (Fear & Greed Index), `ap` (asset price USD), `pi` (perpetual premium index, raw per-interval decimal), `udma:<periods>` (X-day moving averages of underlying funding rate, integer periods 1-365 separated by `;`, max 10 periods, e.g. `udma:7;30`). At least 1 entry required. Each entry costs **1 CU**.",
            "schema": {
              "example": "u,fp,fgi,ap,udma:7;30",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Returns requested indicators for the specified timeframe (max 500 datapoints per call)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/IndicatorsResponse"
                }
              }
            }
          }
        },
        "tags": [
          "Indicators"
        ]
      }
    },
    "/v1/indicators/export": {
      "get": {
        "operationId": "IndicatorsController_getIndicatorsExport",
        "x-computing-unit": "20",
        "summary": "Export market indicators as CSV (up to 10,000 data points)",
        "description": "Streams a CSV file with one row per `timeFrame` bucket on the `[startTimestamp, endTimestamp]` grid (Unix seconds, floored to `timeFrame`). OHLCV columns (`o`, `h`, `l`, `c`, `v` \u2014 APR decimals for OHLC, USD for `v`) are **always** included; pass `select` to add any of `u`, `fp`, `fgi`, `ap`, `pi`, `udma:<periods>` (same format as `GET /indicators`).\n\n**Response cap.** Up to **10,000** rows per call. If the requested window exceeds 10,000 buckets, `startTimestamp` is advanced so the file holds the most recent 10,000 buckets ending at `endTimestamp`.\n\nFilename pattern: `indicators-<marketId>-<timeFrame>-<unixMs>.csv`, served as `text/csv` attachment.",
        "parameters": [
          {
            "name": "marketId",
            "required": true,
            "in": "query",
            "description": "Numeric market identifier (1-based).",
            "schema": {
              "minimum": 1,
              "type": "number"
            }
          },
          {
            "name": "timeFrame",
            "required": true,
            "in": "query",
            "description": "CSV row width. { FIVE_MINUTES : 5m, ONE_HOUR : 1h, ONE_DAY : 1d, ONE_WEEK : 1w }",
            "schema": {
              "example": "5m",
              "enum": [
                "5m",
                "1h",
                "1d",
                "1w"
              ],
              "type": "string"
            }
          },
          {
            "name": "startTimestamp",
            "required": false,
            "in": "query",
            "description": "Start timestamp (Unix seconds), floored to `timeFrame`. Defaults to `0`. Capped to the most recent **10,000** buckets ending at `endTimestamp` \u2014 if the window is wider, `startTimestamp` is advanced automatically.",
            "schema": {
              "default": 0,
              "example": 0,
              "type": "number"
            }
          },
          {
            "name": "endTimestamp",
            "required": false,
            "in": "query",
            "description": "End timestamp (Unix seconds), floored to `timeFrame` and clamped to *now*. Defaults to *now*. Must be `>= startTimestamp` after rounding.",
            "schema": {
              "default": 1786688082,
              "type": "number"
            }
          },
          {
            "name": "select",
            "required": false,
            "in": "query",
            "description": "Comma-separated list of **additional** indicators to include alongside OHLCV (which is always present). Supported: `u`, `fp`, `fgi`, `ap`, `pi`, `udma:<periods>` (integer periods 1-365 separated by `;`, max 10 periods, e.g. `udma:3;7;30`). Empty/omitted = OHLCV only.",
            "schema": {
              "example": "u,fgi,fp,ap,udma:3;7;30",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Returns CSV file with OHLCV + requested indicators for the specified timeframe (max 10,000 points)",
            "content": {
              "text/csv": {
                "schema": {
                  "type": "string",
                  "format": "binary"
                }
              }
            }
          }
        },
        "tags": [
          "Indicators"
        ]
      }
    },
    "/v1/market-acc/encode": {
      "get": {
        "operationId": "MarketAccV2Controller_encode",
        "x-computing-unit": "1",
        "summary": "Encode a `marketAcc`",
        "description": "Packs `root + accountId + tokenId + marketId` into the 54-character hex identifier used across the API. **Concept.** A `marketAcc` uniquely identifies a margin account inside Pendle Boros. It is a 26-byte identifier laid out as `root` (20 bytes, the user wallet) \u00b7 `accountId` (1 byte, 0-255 sub-account index) \u00b7 `tokenId` (2 bytes, the collateral token) \u00b7 `marketId` (3 bytes). When `marketId` is `0xFFFFFF` the identifier points at the user's cross-margin account; any other value pins it to a single isolated market. Every account/balance/trading endpoint expects this same handle, so reuse the output here when calling them.",
        "parameters": [
          {
            "name": "root",
            "required": true,
            "in": "query",
            "description": "The user wallet address (42-char hex). This is the `root` of the margin account.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "accountId",
            "required": false,
            "in": "query",
            "description": "Sub-account index (0-255). A single wallet (`root`) may hold up to 256 accounts. Defaults to 0 (the main account).",
            "schema": {
              "minimum": 0,
              "maximum": 255,
              "default": 0,
              "type": "number"
            }
          },
          {
            "name": "tokenId",
            "required": true,
            "in": "query",
            "description": "Collateral `tokenId` assigned to this market account (e.g. 1 for USD0).",
            "schema": {
              "minimum": 1,
              "type": "number"
            }
          },
          {
            "name": "marketId",
            "required": false,
            "in": "query",
            "description": "Market identifier. Omit (or pass the decimal equivalent of `0xFFFFFF` = 16777215) to target the cross account. Any other value pins the identifier to that isolated market.",
            "schema": {
              "minimum": 1,
              "type": "number"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Packed `marketAcc`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EncodeMarketAccResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid root / accountId / tokenId / marketId."
          }
        },
        "tags": [
          "Market Account (Utilities)"
        ]
      }
    },
    "/v1/market-acc/decode": {
      "get": {
        "operationId": "MarketAccV2Controller_decode",
        "x-computing-unit": "1",
        "summary": "Decode a `marketAcc`",
        "description": "Unpacks a 54-character `marketAcc` into `{ root, accountId, tokenId, marketId, isCross }`. Handy when you receive a `marketAcc` from another endpoint and want to render the account in a UI or re-derive any of its components. See the `/encode` endpoint for the full layout.",
        "parameters": [
          {
            "name": "marketAcc",
            "required": true,
            "in": "query",
            "description": "Packed 54-character hex identifier (`0x` + 26 bytes). Layout: `root` (20 bytes) \u00b7 `accountId` (1 byte) \u00b7 `tokenId` (2 bytes) \u00b7 `marketId` (3 bytes).",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Unpacked fields.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DecodeMarketAccResponse"
                }
              }
            }
          },
          "400": {
            "description": "Malformed `marketAcc` (wrong length or invalid hex)."
          }
        },
        "tags": [
          "Market Account (Utilities)"
        ]
      }
    },
    "/v1/orders/by-tx-hash": {
      "get": {
        "operationId": "OrdersV2Controller_getOrdersByPlacedTxHash",
        "x-computing-unit": "1",
        "summary": "Get orders by placed transaction hash",
        "description": "Returns every limit order placed by a specific transaction hash. Useful for reconciling a `place-order` / `place-orders` call against the resulting on-chain order ids.",
        "parameters": [
          {
            "name": "placedTxHash",
            "required": true,
            "in": "query",
            "description": "The transaction hash of the placed order",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Returns orders for the given placedTxHash",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OrdersByTxHashResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid transaction hash format"
          }
        },
        "tags": [
          "Miscellaneous"
        ]
      }
    },
    "/v1/calldata-builder/user/deposit": {
      "post": {
        "operationId": "CalldataBuilderUserController_buildDeposit",
        "x-computing-unit": "1",
        "summary": "Build deposit calldata",
        "description": "Generates the **unsigned** transaction the user signs to deposit collateral into a margin account. Identity is supplied via a single `marketAcc` (packs `root + accountId + tokenId + marketId`); `0xFFFFFF` in the `marketId` segment targets the **cross** account, any other value targets that **isolated** market.\n\n**Signing & submission.** This is **user-signed**, not agent-signed. Nothing is broadcast by Pendle \u2014 after signing, the user (or their wallet) submits the returned transaction directly on-chain and pays gas themselves. The user wallet is the `from` echoed in the response.\n\n**Pre-conditions.** The user wallet must hold a sufficient ERC20 allowance to the router for `amount`; the calldata itself only encodes the deposit, not the approval. If allowance is missing, `gas` may be omitted from the response (see the `gas` field).\n\nSee [Glossary](https://docs.pendle.finance/boros-dev/Backend/glossary) for `marketAcc` packing details.",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DepositBodyDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Deposit calldata",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CalldataBuilderUserResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid parameters (e.g., malformed marketAcc, unsupported token, or zero amount)."
          }
        },
        "tags": [
          "Calldata-Builder (User-Signed)"
        ]
      }
    },
    "/v1/calldata-builder/user/request-withdrawal": {
      "post": {
        "operationId": "CalldataBuilderUserController_buildRequestWithdrawal",
        "x-computing-unit": "1",
        "summary": "Build request-withdrawal calldata",
        "description": "Generates the **unsigned** transaction the user signs to request a withdrawal. **User-signed and user-broadcast** \u2014 Pendle does not submit this for you.\n\n**Two-step on-chain flow.** A withdrawal is `request \u2192 cooldown \u2192 finalize`. **Finalization is automatic**: a backend bot calls the finalize step on-chain after the cooldown elapses, so there is no separate finalize endpoint here. The user only signs the request.\n\n**Cross-only.** Withdrawals always exit from the **cross** account for `(root, tokenId)`. To withdraw funds that are currently sitting in an isolated market, first move them with `POST /calldata-builder/agent/cash-transfer` (`ISOLATED_TO_CROSS`).\n\nTo abort before the cooldown expires, use `/cancel-withdrawal`.",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RequestWithdrawalBodyDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Request-withdrawal calldata",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CalldataBuilderUserResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid parameters (e.g., invalid address or zero amount)."
          }
        },
        "tags": [
          "Calldata-Builder (User-Signed)"
        ]
      }
    },
    "/v1/calldata-builder/user/cancel-withdrawal": {
      "post": {
        "operationId": "CalldataBuilderUserController_buildCancelWithdrawal",
        "x-computing-unit": "1",
        "summary": "Build cancel-withdrawal calldata",
        "description": "Generates the **unsigned** transaction the user signs to cancel a pending withdrawal request **before its cooldown elapses**. **User-signed and user-broadcast.** Once the cooldown finishes the backend bot will auto-finalize the withdrawal and this endpoint can no longer be used for that request.",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CancelWithdrawalBodyDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Cancel-withdrawal calldata",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CalldataBuilderUserResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid parameters or no pending withdrawal to cancel."
          }
        },
        "tags": [
          "Calldata-Builder (User-Signed)"
        ]
      }
    },
    "/v1/calldata-builder/user/approve-agent": {
      "post": {
        "operationId": "CalldataBuilderUserController_buildApproveAgent",
        "x-computing-unit": "1",
        "summary": "Build approve-agent calldata",
        "description": "Generates the **unsigned** transaction the user signs to authorize an **agent address** to sign trading / order operations on behalf of the user. Approval auto-expires at `expiry` (unix seconds, must be in the future). This is the prerequisite for **every** endpoint under `Calldata-Builder (Agent-Executable)` \u2014 without an active approval, the agent bot cannot submit place-order, cancel, enter/exit-markets, etc. for that `(root, accountId)`.\n\n**Two submission paths** (both produce the same on-chain state \u2014 pick whichever fits the client):\n1. **User submits on-chain directly.** The user (or their wallet) broadcasts the returned transaction themselves \u2014 Pendle never sees the signature. The user pays gas on the target chain. Standard path.\n2. **User signs off-chain + posts to Pendle BE.** The user EIP-712-signs the payload with their wallet and posts the signed blob via `POST /v1/agents/approve-agent` (the auth/agents endpoint, not this builder); Pendle then submits the on-chain tx for them via the backend bot. Useful when the user does not hold native gas on the target chain.\n\nSee the [Agent Trading guide](https://docs.pendle.finance/boros-dev/Backend/agent) for the full lifecycle and revocation rules.",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ApproveAgentBodyDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Approve-agent calldata",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CalldataBuilderUserResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid user/agent address, account ID, or `expiry` in the past."
          }
        },
        "tags": [
          "Calldata-Builder (User-Signed)"
        ]
      }
    },
    "/v1/calldata-builder/user/revoke-agent": {
      "post": {
        "operationId": "CalldataBuilderUserController_buildRevokeAgent",
        "x-computing-unit": "1",
        "summary": "Build revoke-agent calldata",
        "description": "Generates the **unsigned** transaction the user signs to revoke a previously approved agent before its `expiry`. **User-signed and user-broadcast** (or sent through the off-chain approve-agent path the same way as `/approve-agent`). Once mined, all future agent-signed calls under this `(root, accountId, agentAddress)` triple revert on-chain.\n\nSee the [Agent Trading guide](https://docs.pendle.finance/boros-dev/Backend/agent) for revocation semantics.",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RevokeAgentBodyDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Revoke-agent calldata",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CalldataBuilderUserResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid user/agent address or account ID."
          }
        },
        "tags": [
          "Calldata-Builder (User-Signed)"
        ]
      }
    },
    "/v1/calldata-builder/user/vault-pay-treasury": {
      "post": {
        "operationId": "CalldataBuilderUserController_buildVaultPayTreasury",
        "x-computing-unit": "1",
        "summary": "Build vault-pay-treasury calldata (tops up the user's off-chain gas budget)",
        "description": "Generates the **unsigned** transaction the user (or their vault contract) signs to pay treasury fees **directly from the wallet**, bypassing the agent flow used by `POST /calldata-builder/agent/pay-treasury`. **User-signed and user-broadcast** \u2014 useful for vaults / smart-contract wallets that do not delegate to an agent.\n\n**Side effect on the backend (off-chain, post-tx):** identical to `agent/pay-treasury` \u2014 once the tx is mined, the backend credits the equivalent USD value to the user's **off-chain gas budget**, which funds future agent-submitted transactions. Treasury accrual itself is a real on-chain fee payment ([Fees mechanics](https://docs.pendle.finance/boros-dev/Mechanics/Fees)). `amount` is in the paying token's **native decimals** \u2014 look up `decimals` from `/open-api-v2/v1/assets`.",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/VaultPayTreasuryBodyDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Vault-pay-treasury calldata",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CalldataBuilderUserResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid parameters (e.g., invalid address or zero amount)."
          }
        },
        "tags": [
          "Calldata-Builder (User-Signed)"
        ]
      }
    },
    "/v1/calldata-builder/agent/place-order": {
      "post": {
        "operationId": "CalldataBuilderAgentController_buildPlaceOrder",
        "x-computing-unit": "1",
        "summary": "Build place-order calldata (simple, UI-style)",
        "description": "Generate **agent-executable** calldata for a single order using the UI-style parameter set (`rate` + `slippage`). Targets the common case: a trader places one order on one market.\n\n**Submission.** The returned `calls[0].calldata` is signed by the **agent** (not the user), and the agent (typically a Pendle backend bot) submits it on-chain via `POST /v1/agent/send-transactions`. Gas is **not** paid in native ETH/etc. \u2014 it is debited from the user's **off-chain gas budget** (top up via `pay-treasury`). The user must have a non-expired agent approval first (see `/calldata-builder/user/approve-agent`).\n\n**Pre-conditions.** For a market the account has not held a position on yet, `enter-markets` must succeed before the order can match. Min-order-value is enforced server-side per market.\n\nFor advanced / market-making flows that need raw `limitTick`, absolute `desiredRate`, or many orders across markets in one request, use POST `/place-orders`.\n\nReturns a `resolved` block echoing the exact tick / rate actually encoded so the UI can show the user before they sign. See [Order Book mechanics](https://docs.pendle.finance/boros-dev/Mechanics/OrderBook) for tick/rate semantics, [Margin](https://docs.pendle.finance/boros-dev/Mechanics/Margin) for IM / leverage, and [Fees](https://docs.pendle.finance/boros-dev/Mechanics/Fees) for taker-fee mechanics.",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PlaceOrderSimpleBodyDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "`calls` contains one entry, with a `resolved` block describing what was encoded.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PlaceOrdersResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid parameters \u2014 e.g. `rate` missing for a limit order, `slippage` out of range, market expired, `marketId` does not match `marketAcc`, or order value below minimum."
          }
        },
        "tags": [
          "Calldata-Builder (Agent-Executable)"
        ]
      }
    },
    "/v1/calldata-builder/agent/place-orders": {
      "post": {
        "operationId": "CalldataBuilderAgentController_buildPlaceOrders",
        "x-computing-unit": "1+",
        "summary": "Build place-orders calldata (advanced, liquidity-provisioning batch)",
        "description": "**Advanced use case.** Build **agent-executable** calldata for a heterogeneous batch of order requests in one HTTP call. Primary target is **market-making / liquidity-provisioning** flows that need to place many resting orders across tick levels and/or markets at once.\n\n**Submission.** Each entry of the response `calls[]` is one **agent-signed** on-chain call \u2014 the agent (typically a Pendle backend bot) submits them in order via `POST /v1/agent/send-transactions`. Gas is debited from the user's **off-chain gas budget**, not paid in native token. An active agent approval is required (see `/calldata-builder/user/approve-agent`).\n\nEach entry in `orderRequests[]` is exactly one of:\n- `singleOrder` \u2014 compiles to one on-chain `placeSingleOrder` call. Carries its own `marketAcc`, `ammId`, limit price and execution guard. Power-user controls: raw `limitTick` XOR human-friendly `rate`, absolute `desiredRate` XOR relative `slippage` (**exactly one** required), optional `preCancelOrderId` (atomic strict cancel-and-replace).\n- `bulkOrders` \u2014 compiles to one on-chain `bulkOrders` multicall covering N per-market sub-orders. Each `bulks[i]` carries its own `marketId`, `cancelData` (orders to atomically cancel before placing), `orders` (shared `side`/`tif`, per-order `size`/`limitTick`), and `desiredRate` XOR `slippage`.\n\nRequest order is preserved in the response \u2014 `calls[i]` corresponds to `orderRequests[i]`. Cost: **one on-chain call per `orderRequests[]` entry**.\n\n**CU cost.** Dynamic: **1 CU per 10 entries** (e.g. `orderRequests.length=10` \u2192 1 CU, `length=100` \u2192 10 CU).\n\n**Constraints.** All entries execute under the same `(root, accountId)` \u2014 one agent signature covers the whole batch. Entries may freely target different markets. `enter-markets` must already cover every targeted market for the account.\n\nFor the common single-order UI case, prefer POST `/place-order` \u2014 simpler DTO, no power-user knobs. See [Order Book](https://docs.pendle.finance/boros-dev/Mechanics/OrderBook) for tick semantics and [Best Practices](https://docs.pendle.finance/boros-dev/Backend/best-practices) for batching tips.",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PlaceOrdersBodyDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "`calls` has one entry per `orderRequests[]` entry (same length).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PlaceOrdersResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid parameters on any entry \u2014 e.g. neither or both of `singleOrder` / `bulkOrders` set, neither or both of `desiredRate` / `slippage` set (exactly one is required), `limitTick` and `rate` both set, `marketId` does not match `marketAcc`, market expired, mismatched `sizes` / `limitTicks` lengths, or order value below minimum."
          }
        },
        "tags": [
          "Calldata-Builder (Agent-Executable)"
        ]
      }
    },
    "/v1/calldata-builder/agent/cancel-orders": {
      "post": {
        "operationId": "CalldataBuilderAgentController_buildCancelOrders",
        "x-computing-unit": "1",
        "summary": "Build cancel-orders calldata (multi-market)",
        "description": "Generate **agent-executable** calldata to cancel resting orders across one or more markets. **One on-chain `bulkCancels` call per `markets[]` entry** \u2014 the response has N `calls` for N markets, each individually agent-signed and submitted via `POST /v1/agent/send-transactions`.\n\nPer-entry: set `cancelAll: true` to cancel every resting order on that `(marketAcc, marketId)`, or `cancelAll: false` + `orderIds: [\"123\", \"456\", ...]` to target specific ids. Cancels are emitted in **non-strict** mode here \u2014 ids already filled or already cancelled are skipped silently and the other entries still go through. For **strict** atomic cancel-then-place (revert if the cancel cannot be honored), use `preCancelOrderId` on `/place-order` / `/place-orders` or the per-bulk `cancelData.isStrict` inside `bulkOrders`.",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CancelOrdersBodyDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "`calls` has one entry per market in `markets[]` (same length).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AgentCalldataResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid parameters \u2014 e.g. `marketId` does not match `marketAcc`, market expired, `cancelAll: false` with empty `orderIds`."
          }
        },
        "tags": [
          "Calldata-Builder (Agent-Executable)"
        ]
      }
    },
    "/v1/calldata-builder/agent/cash-transfer": {
      "post": {
        "operationId": "CalldataBuilderAgentController_buildCashTransfer",
        "x-computing-unit": "1",
        "summary": "Build cash-transfer calldata (cross \u21c4 isolated)",
        "description": "Generate **agent-executable** calldata to move cash between the user's cross account and the isolated account on a specific market. Direction is explicit (`CROSS_TO_ISOLATED` or `ISOLATED_TO_CROSS`); `amount` is always positive \u2014 the backend applies the sign off-chain from `direction` before encoding `signedAmount` into the call.\n\nThe corresponding cross leg is implicit \u2014 there is exactly one cross account per `(root, accountId, tokenId)`, derived from the market's `tokenId`. Clients only specify the isolated leg's `marketId`.\n\n**Common reason to call this:** to withdraw funds locked in an isolated market \u2014 first move them back to cross via `ISOLATED_TO_CROSS`, then call `/calldata-builder/user/request-withdrawal`. **One on-chain call** per request. See [Margin](https://docs.pendle.finance/boros-dev/Mechanics/Margin) for cross vs isolated semantics.",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CashTransferBodyDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "`calls` contains a single entry.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AgentCalldataResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid parameters \u2014 e.g. market expired, unknown direction, zero amount."
          }
        },
        "tags": [
          "Calldata-Builder (Agent-Executable)"
        ]
      }
    },
    "/v1/calldata-builder/agent/enter-markets": {
      "post": {
        "operationId": "CalldataBuilderAgentController_buildEnterMarkets",
        "x-computing-unit": "1",
        "summary": "Build enter-markets calldata",
        "description": "Generate **agent-executable** calldata to opt the account into the given markets. **Required prerequisite** before that `(root, accountId)` can place orders / hold positions on a market it has not previously entered. **One on-chain call** per request.\n\n- **Cross** (`isCross: true`, default): `marketIds` may list multiple markets in one call \u2014 they all get entered atomically against the cross account for `(root, accountId, tokenId)`.\n- **Isolated** (`isCross: false`): `marketIds` must have length **1** \u2014 an isolated account is pinned to a single market.\n\nEntering charges a one-time per-market `marketEntranceFee` from the account; pre-fund accordingly. See [Margin](https://docs.pendle.finance/boros-dev/Mechanics/Margin).",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/EnterExitMarketsBodyDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "`calls` contains a single entry.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AgentCalldataResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid `marketIds` (unknown market, expired market, empty array, or length > 1 with `isCross: false`)."
          }
        },
        "tags": [
          "Calldata-Builder (Agent-Executable)"
        ]
      }
    },
    "/v1/calldata-builder/agent/exit-markets": {
      "post": {
        "operationId": "CalldataBuilderAgentController_buildExitMarkets",
        "x-computing-unit": "1",
        "summary": "Build exit-markets calldata",
        "description": "Generate **agent-executable** calldata to opt the account out of the given markets. The account must hold no open positions / open orders on a market for the exit to succeed on-chain. **One on-chain call** per request.\n\n- **Cross** (`isCross: true`, default): `marketIds` may list multiple markets in one call.\n- **Isolated** (`isCross: false`): `marketIds` must have length **1**.",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/EnterExitMarketsBodyDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "`calls` contains a single entry.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AgentCalldataResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid `marketIds` (unknown market, expired market, empty array, or length > 1 with `isCross: false`)."
          }
        },
        "tags": [
          "Calldata-Builder (Agent-Executable)"
        ]
      }
    },
    "/v1/calldata-builder/agent/pay-treasury": {
      "post": {
        "operationId": "CalldataBuilderAgentController_buildPayTreasury",
        "x-computing-unit": "1",
        "summary": "Build pay-treasury calldata (tops up the user's off-chain gas budget)",
        "description": "Generate **agent-executable** calldata to pay treasury fees on a specific market. `amount` is a bigint string in the paying token's **native decimals** (no USD conversion at this endpoint \u2014 look up `decimals` from `/open-api-v2/v1/assets`). **One on-chain call** per request.\n\n**Side effect on the backend (off-chain, post-tx):** once the on-chain tx is mined, the backend credits the equivalent **USD** value to the user's **off-chain gas budget**. That budget funds subsequent agent-signed transactions (place-order, enter-markets, cancel-orders, \u2026) that the Pendle backend bot wallet submits on the user's behalf \u2014 users do **not** pay native ETH/etc. for agent-submitted txs. Paying treasury is the canonical way to top up gas for ongoing agent-driven activity.\n\nIf the user instead wants to pay treasury directly from their wallet (e.g. vault contracts that don't delegate to an agent), use `POST /calldata-builder/user/vault-pay-treasury`. See [Fees mechanics](https://docs.pendle.finance/boros-dev/Mechanics/Fees) and the [Agent Trading guide](https://docs.pendle.finance/boros-dev/Backend/agent).",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PayTreasuryBodyDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "`calls` contains a single entry.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AgentCalldataResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid parameters \u2014 e.g. unknown market, non-positive `amount`."
          }
        },
        "tags": [
          "Calldata-Builder (Agent-Executable)"
        ]
      }
    },
    "/v1/calldata-builder/agent/add-liquidity-to-amm": {
      "post": {
        "operationId": "CalldataBuilderAgentController_buildAddLiquidityToAmm",
        "x-computing-unit": "1",
        "summary": "Build add-liquidity-to-amm calldata (single-cash deposit)",
        "description": "Generate **agent-executable** calldata for depositing cash into a market's AMM pool as **single-sided liquidity** (cash only \u2014 the AMM auto-balances by taking the matching swap leg internally). Cash is sourced from the user's **cross** account on the market's `tokenId`.\n\n**Returned `executeParams[]` is a 2-call sequence**: an `ammCashTransfer` that pulls cash from the cross account into the AMM-account leg (its `cashIn` already includes any one-time `marketEntranceFee` if the AMM account has not yet entered the market), followed by the `addLiquiditySingleCashToAmm` call. The agent must submit the entries in order via `POST /v1/agent/send-transactions`.\n\n`root` is **required**: the emitted bytes are not user-agnostic. Both `enterMarket` and the `marketEntranceFee` above depend on whether that `root`\u2019s AMM sub-account has already entered this market, and this endpoint is called before any signature exists, so it cannot infer the address.\n\n`minLpOut` is the slippage guard on LP tokens received \u2014 set conservatively. The desired-swap-rate guard for the AMM's internal balancing is computed by the backend from the current mid-rate.",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AddLiquidityToAmmAgentBodyDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Add-liquidity-to-amm calldata",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BulkAgentExecuteParamsResponseV2"
                }
              }
            }
          },
          "400": {
            "description": "Invalid parameters or insufficient balance for liquidity provision."
          }
        },
        "tags": [
          "Calldata-Builder (Agent-Executable)"
        ]
      }
    },
    "/v1/calldata-builder/agent/remove-liquidity-from-amm": {
      "post": {
        "operationId": "CalldataBuilderAgentController_buildRemoveLiquidityFromAmm",
        "x-computing-unit": "1",
        "summary": "Build remove-liquidity-from-amm calldata (single-cash withdrawal)",
        "description": "Generate **agent-executable** calldata for burning LP tokens from a market's AMM and receiving **cash** back into the user's **cross** account on the market's `tokenId`.\n\n**Returned `executeParams[]` is a 2-call sequence**: `removeLiquiditySingleCashFromAmm` (burn LP, internally settles a swap leg against the AMM's mid-rate) followed by `ammCashTransfer` that sweeps the resulting cash from the AMM account back into the user's cross account. The agent must submit them in order via `POST /v1/agent/send-transactions`.\n\n`minCashOut` is the slippage guard on the cash received \u2014 set conservatively.",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RemoveLiquidityFromAmmAgentBodyDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Remove-liquidity-from-amm calldata",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BulkAgentExecuteParamsResponseV2"
                }
              }
            }
          },
          "400": {
            "description": "Invalid parameters or insufficient LP token balance."
          }
        },
        "tags": [
          "Calldata-Builder (Agent-Executable)"
        ]
      }
    },
    "/v1/funding-rate/all-funding-rate-symbols": {
      "get": {
        "operationId": "FundingRateV2Controller_getAllFundingRateSymbols",
        "x-computing-unit": "1",
        "summary": "Get all funding rate symbols",
        "description": "Returns the catalog of off-chain perp funding rate streams that Boros markets settle against \u2014 each entry pairs a venue (`exchange`, e.g. `binance`, `hyperliquid`) with the venue-specific instrument string (`fundingRateSymbol`, e.g. `BTCUSDT`) and the canonical `assetSymbol`. **A \"symbol\" here is a (venue, contract) pair, not a Boros market** \u2014 multiple Boros markets can quote the same symbol with different maturities. Use this to discover which feeds are available; map a feed to its tradable Boros markets via `GET /v1/markets`.",
        "parameters": [],
        "responses": {
          "200": {
            "description": "List of all available funding rate symbols with their exchange and asset info",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FundingRateSymbolListResponse"
                }
              }
            }
          }
        },
        "tags": [
          "Miscellaneous"
        ]
      }
    },
    "/v1/funding-rate/settlement-summary": {
      "post": {
        "operationId": "FundingRateV2Controller_getSettlementSummaryByMarket",
        "x-computing-unit": "1+",
        "summary": "Get settlement summary",
        "description": "Returns one row per **on-chain settlement event** that fell inside the `[fromTimestamp, toTimestamp]` window (Unix seconds, UTC), sorted by `periodTimestamp` descending. `periodTimestamp` is the start of the funding period; period length matches the market's configured `settlementInterval` (e.g. 1h / 2h / 8h). Non-whitelisted markets are silently excluded.\n\n**CU cost.** Dynamic: **1 CU per 10 returned rows** (e.g. up to 10 rows \u2192 1 CU, 100 rows \u2192 10 CU).\n\nSee [Settlement mechanics](https://docs.pendle.finance/boros-dev/Mechanics/Settlement) for how funding payments accrue and settle.",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/GetSettlementSummaryByMarketQueryDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Settlement summaries for the requested markets",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SettlementMarketSummaryListResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid market IDs in request body"
          }
        },
        "tags": [
          "Miscellaneous"
        ]
      }
    },
    "/v1/events/liquidation-events": {
      "get": {
        "operationId": "EventsV2Controller_getLiquidationEvents",
        "x-computing-unit": "1+",
        "summary": "Get liquidation events, sorted by timestamp descending",
        "description": "Returns the most recent on-chain `Liquidate` events emitted by the market contract, **sorted by `blockTimestamp` descending** (newest first). For each event the response also includes the violator and liquidator position snapshots (rate + notional size) before and after the liquidation trade.\n\nThe endpoint is **not cursor-paginated**: it always returns the top-N events that match the filters (`limit`, default 10, max 2000). To page through history, narrow the window with `fromTimestamp`/`toTimestamp`. Cost scales with `limit`: **CU = max(1, ceil(limit / 200))**.\n\nSee [Margin mechanics](https://docs.pendle.finance/boros-dev/Mechanics/Margin) for liquidation rules and health ratio.",
        "parameters": [
          {
            "name": "marketId",
            "required": false,
            "in": "query",
            "description": "Restrict results to a single market id. If omitted, liquidations from all markets are returned.",
            "schema": {
              "minimum": 1,
              "type": "number"
            }
          },
          {
            "name": "fromTimestamp",
            "required": false,
            "in": "query",
            "description": "Inclusive lower bound on the event block timestamp (Unix seconds, UTC). Must be at or after Boros mainnet genesis (`1753334471`).",
            "schema": {
              "type": "number"
            }
          },
          {
            "name": "toTimestamp",
            "required": false,
            "in": "query",
            "description": "Inclusive upper bound on the event block timestamp (Unix seconds, UTC).",
            "schema": {
              "type": "number"
            }
          },
          {
            "name": "limit",
            "required": false,
            "in": "query",
            "description": "Maximum number of results to return. The parameter is capped at 2000.",
            "schema": {
              "default": 10,
              "type": "number"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Paginated list of liquidation events sorted by most recent first",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LiquidationEventListResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid query parameters"
          }
        },
        "tags": [
          "Miscellaneous"
        ]
      }
    },
    "/v1/on-chain-events": {
      "get": {
        "operationId": "OnChainEventsV2Controller_getEvents",
        "x-computing-unit": "3",
        "summary": "Get on-chain events with filters",
        "description": "Returns raw decoded events emitted by Boros contracts. Each item includes the base envelope (`eventName`, `txHash`, `blockNumber`, `logIndex`, `eventIndex`, `blockTimestamp`, `isFinalized`) plus a `data` map carrying event-specific decoded fields.\n\nSorted by `eventIndex` descending (newest first); `eventIndex` is a stable monotonic ordering key. Pagination is cursor-based via `resumeToken`. The `[fromBlockNumber, toBlockNumber]` filter is inclusive on both ends. `eventName` is matched exactly and case-sensitively against the emitted contract event name (e.g. `BulkOrdersExecuted`, `Liquidate`, `PaymentFromSettlement`). An unrecognised name is not rejected \u2014 it returns 200 with zero results rather than a 400.",
        "parameters": [
          {
            "name": "limit",
            "required": false,
            "in": "query",
            "description": "Maximum number of results to return. The parameter is capped at 5000.",
            "schema": {
              "default": 10,
              "type": "number"
            }
          },
          {
            "name": "resumeToken",
            "required": false,
            "in": "query",
            "description": "Resume token for cursor-based pagination. Pass the resumeToken from the previous response to fetch the next page.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "eventName",
            "required": false,
            "in": "query",
            "description": "Filter by on-chain event name (free-form match \u2014 e.g. `BulkOrdersExecuted`, `Liquidate`, `Settlement`).",
            "schema": {
              "example": "BulkOrdersExecuted",
              "type": "string"
            }
          },
          {
            "name": "sourceAddress",
            "required": false,
            "in": "query",
            "description": "Filter by source contract address. Case-insensitive \u2014 value is lower-cased before lookup.",
            "schema": {
              "example": "0x8080808080dab95efed788a9214e400ba552def6",
              "type": "string"
            }
          },
          {
            "name": "fromBlockNumber",
            "required": false,
            "in": "query",
            "description": "Inclusive lower bound on block number.",
            "schema": {
              "minimum": 0,
              "type": "number"
            }
          },
          {
            "name": "toBlockNumber",
            "required": false,
            "in": "query",
            "description": "Inclusive upper bound on block number.",
            "schema": {
              "minimum": 0,
              "type": "number"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Returns on-chain events matching the query parameters",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OnChainEventsResponse"
                }
              }
            }
          }
        },
        "tags": [
          "Miscellaneous"
        ]
      }
    },
    "/v1/simulations/deposit": {
      "post": {
        "operationId": "SimulationsController_simulateDeposit",
        "x-computing-unit": "1",
        "summary": "Deposit collateral to isolated/cross margin account",
        "description": "Simulates depositing collateral from a wallet into an isolated or cross margin account. **Preview-only counterpart** to `POST /calldata-builder/user/deposit` \u2014 same `marketAcc` + `amount` body, no signing. `0xFFFFFF` in the `marketAcc` marketId segment targets the cross account; any other value targets that isolated market.\n\nReturns the projected `collateralBalance` / `maintenanceMargin` / `marginRatio` pre and post deposit, plus `minReceived` (the deposit scaled into the 18-decimal cash accounting unit). The simulator does **not** check the wallet's on-chain balance \u2014 it assumes the deposit succeeds. See [Margin mechanics](https://docs.pendle.finance/boros-dev/Mechanics/Margin) for how `marginRatio` is computed.",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DepositSimulationBodyDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Returns deposit collateral simulation result",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DepositSimulationResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid parameters (e.g., malformed marketAcc, unsupported token, or zero amount)"
          }
        },
        "tags": [
          "Simulations"
        ]
      }
    },
    "/v1/simulations/request-withdrawal": {
      "post": {
        "operationId": "SimulationsController_simulateRequestWithdrawal",
        "x-computing-unit": "1",
        "summary": "Request withdrawal of collateral",
        "description": "Previews the margin state change that a withdrawal request would produce on the user's **cross** account. **Preview-only counterpart** to `POST /calldata-builder/user/request-withdrawal`; isolated balances must be moved to cross via `cash-transfer` first.\n\n**Unit caveat:** unlike the calldata-builder body, the simulation `amount` is in the **18-decimal cash accounting unit** (the unit `collateralBalance` is reported in), not the token's native decimals. The simulator returns `preUserState` / `postUserState` only \u2014 it does not enforce the on-chain liveness or available-margin guards (see [Margin mechanics](https://docs.pendle.finance/boros-dev/Mechanics/Margin)).",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RequestWithdrawalSimulationBodyDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Returns request-withdrawal simulation result",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WithdrawSimulationResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid parameters (e.g., invalid address, unsupported token, or zero amount)"
          }
        },
        "tags": [
          "Simulations"
        ]
      }
    },
    "/v1/simulations/cash-transfer": {
      "post": {
        "operationId": "SimulationsController_simulateCashTransfer",
        "x-computing-unit": "1",
        "summary": "Cash transfer",
        "description": "Previews a cross \u21c4 isolated cash transfer and returns the pre/post `collateralBalance` / `maintenanceMargin` / `marginRatio` of **both** legs. **Preview-only counterpart** to `POST /calldata-builder/agent/cash-transfer` \u2014 uses the same `marketId` + `direction` + `amount` trio. The user context (`root` + optional `accountId`) is supplied explicitly here because simulations have no agent signature to read it from. `amount` is in 10^18 cash units \u2014 same as the calldata endpoint.",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CashTransferSimulationBodyDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Returns cash transfer simulation result for both legs",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CashTransferSimulationResponseV2"
                }
              }
            }
          },
          "400": {
            "description": "Invalid parameters (e.g. unknown `marketId` / `direction`, zero amount)"
          }
        },
        "tags": [
          "Simulations"
        ]
      }
    },
    "/v1/simulations/place-order": {
      "post": {
        "operationId": "SimulationsController_simulatePlaceOrder",
        "x-computing-unit": "1",
        "summary": "Place order (simple, UI-style)",
        "description": "**Preview-only counterpart** to `POST /calldata-builder/agent/place-order` \u2014 submit the same body here to preview, and to the calldata endpoint to build a signed transaction. The two surfaces intentionally share one DTO (`rate` + `slippage` + `ammId`, no raw `limitTick` / `desiredRate`).\n\n`rate` is required for limit orders (`GTC` / `ALO` / `SOFT_ALO`); optional for `FOK` / `IOC` market orders. `slippage` is optional but strongly recommended for market orders \u2014 a default is applied when omitted.\n\nThe simulator handles market entrance automatically \u2014 it does **not** require the account to have already entered the market. See [Margin mechanics](https://docs.pendle.finance/boros-dev/Mechanics/Margin) and [Order book mechanics](https://docs.pendle.finance/boros-dev/Mechanics/OrderBook).",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PlaceOrderSimulationBodyDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Returns place order simulation result",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PlaceOrderSimulationResponseV3"
                }
              }
            }
          },
          "400": {
            "description": "Invalid order parameters (e.g. `rate` missing for a limit order, `slippage` out of range, market expired, or order value below minimum)"
          }
        },
        "tags": [
          "Simulations"
        ]
      }
    },
    "/v1/simulations/place-order-anonymous": {
      "post": {
        "operationId": "SimulationsController_simulatePlaceOrderAnonymous",
        "x-computing-unit": "1",
        "summary": "Place order (anonymous, no wallet connected)",
        "description": "Preview a place-order without a connected wallet.",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PlaceOrderAnonymousSimulationBodyDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Returns anonymous place order simulation result",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AnonymousPlaceOrderSimulationResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid order parameters."
          }
        },
        "tags": [
          "Simulations"
        ]
      }
    },
    "/v1/simulations/add-liquidity-to-amm": {
      "post": {
        "operationId": "SimulationsController_simulateAddLiquidityToAmm",
        "x-computing-unit": "1",
        "summary": "Add liquidity to AMM \u2014 fee preview (single-cash)",
        "description": "**Fee-only preview** counterpart to `POST /calldata-builder/agent/add-liquidity-to-amm` \u2014 uses the same `root` / `accountId` / `marketId` / `netCashIn` body. The simulator runs the full add-liquidity flow on a forked state to compute fees but only the fee breakdown is exposed publicly: `marketEntranceFee` (charged once when the account first enters this market) + `vaultDepositFee`. Fuller state projections (projected margin, matched sizes, LP out, post-deposit margin ratio) are intentionally not part of the public simulation surface.\n\nFunds are assumed to be available in the cross account; the simulator does not pre-check the caller's cross balance.",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AddLiquidityToAmmV2SimulationBodyDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Fee breakdown for add-liquidity-to-amm",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AddLiquidityToAmmFeeSimulationResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid parameters or insufficient balance for liquidity provision."
          }
        },
        "tags": [
          "Simulations"
        ]
      }
    },
    "/v1/simulations/remove-liquidity-from-amm": {
      "post": {
        "operationId": "SimulationsController_simulateRemoveLiquidityFromAmm",
        "x-computing-unit": "1",
        "summary": "Remove liquidity from AMM \u2014 fee preview (single-cash)",
        "description": "**Fee-only preview** counterpart to `POST /calldata-builder/agent/remove-liquidity-from-amm` \u2014 uses the same `root` / `accountId` / `marketId` / `lpToRemove` body. Only `feeBreakdown.vaultWithdrawalFee` is exposed; fuller state projections (cash out, post-burn margin ratio) are intentionally not part of the public simulation surface. The simulator assumes the AMM sub-account holds at least `lpToRemove` LP \u2014 it does not pre-check the balance.",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RemoveLiquidityFromAmmV2SimulationBodyDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Fee breakdown for remove-liquidity-from-amm",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RemoveLiquidityFromAmmFeeSimulationResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid parameters or insufficient LP token balance."
          }
        },
        "tags": [
          "Simulations"
        ]
      }
    },
    "/v1/accounts/amm-states": {
      "get": {
        "operationId": "AccountsV2Controller_getUserAmmStates",
        "summary": "Get user AMM states",
        "description": "Returns the list of AMMs (LP vaults) the **sub-account** has active deposits in, with per-vault user data only. Each entry tells you how much LP this `(root, accountId)` holds in a given `(ammId, marketId, tokenId)` vault, the average LP price the user paid, and the wallet's currently available balance of the vault's collateral token.\n\n**Identity.** Takes the 44-char `account` handle (`root` + `accountId`), **not** a `marketAcc` \u2014 LP positions are not pinned to a single market. Use `/v1/market-acc/encode` for the trading-side handle and a hex-concat helper for this one.\n\n**Note.** Does not return global vault state (TVL, supply, fees). For that, see the dedicated AMM endpoints.",
        "parameters": [
          {
            "name": "account",
            "required": true,
            "in": "query",
            "description": "Packed 44-character account hex (`0x` + 21 bytes): `root` (20B) \u00b7 `accountId` (1B). Identifies a sub-account without a market binding.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "User AMM states",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UserVaultStatesResponse"
                }
              }
            }
          }
        },
        "tags": [
          "Accounts"
        ]
      }
    },
    "/v1/accounts/gas-balance": {
      "get": {
        "operationId": "AccountsV2Controller_getAccountGasBalance",
        "x-computing-unit": "1",
        "summary": "Get account gas balance",
        "description": "Returns the **wallet-level** gas balance (USD) for the specified `root`. Gas balance pays the per-action fee charged when a Pendle bot relays the user's agent-signed transactions on-chain (order placement/cancellation, cash transfer, AMM add/remove liquidity, enter/exit markets, conditional-order execution, pay-treasury, collateral swap). It is **not** the EVM gas of the underlying chain and is not segregated per `accountId` or per `marketAcc` \u2014 every sub-account under the same `root` draws from this single bucket.\n\nTop up via the `pay-treasury` calldata endpoints (`/calldata-builder/agent/pay-treasury` or `/calldata-builder/user/vault-pay-treasury`) \u2014 the backend credits the equivalent USD value to this budget once the on-chain tx is mined. Funded with a collateral ERC-20, **not** the chain-native gas token.",
        "parameters": [
          {
            "name": "root",
            "required": true,
            "in": "query",
            "description": "User wallet address \u2014 the `root` of the margin account.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AccountGasBalanceResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid `root` address format"
          }
        },
        "tags": [
          "Accounts"
        ]
      }
    },
    "/v1/accounts/gas-consumption-history": {
      "get": {
        "operationId": "AccountsV2Controller_getGasConsumptionHistory",
        "x-computing-unit": "1+",
        "summary": "Get gas consumption history",
        "description": "Returns the per-action gas-fee debits charged to the wallet-level gas balance, in USD. One row per chargeable relayed event (e.g. order placement/cancellation, cash transfer, AMM add/remove liquidity, enter/exit markets, conditional-order execution, pay-treasury, collateral swap). **Wallet-level**: filtered by `root` only \u2014 covers every sub-account under that wallet.\n\n**Pagination.** Cursor-based: pass the previous response's `resumeToken` for the next page. Sorted by event index descending (newest first). `resumeToken` is `null` when the page is the last one.\n\n**Cost.** `1 + ceil(limit / 200) - 1` CU.",
        "parameters": [
          {
            "name": "root",
            "required": true,
            "in": "query",
            "description": "User wallet address \u2014 the `root` of the margin account.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "required": false,
            "in": "query",
            "description": "Maximum number of results to return. The parameter is capped at 2000.",
            "schema": {
              "default": 10,
              "type": "number"
            }
          },
          {
            "name": "resumeToken",
            "required": false,
            "in": "query",
            "description": "Resume token for cursor-based pagination. Pass the resumeToken from the previous response to fetch the next page.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Gas consumption history",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GasConsumptionHistoryV2RootResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid `root` address or pagination parameters"
          }
        },
        "tags": [
          "Accounts"
        ]
      }
    },
    "/v1/accounts/transfer-logs": {
      "get": {
        "operationId": "AccountsV2Controller_getTransferLogs",
        "x-computing-unit": "1+",
        "summary": "Get transfer logs",
        "description": "Returns the **collateral-balance** movement log for a sub-account: external deposits, external withdrawals, and **internal** transfers between fund locations (cross account, isolated markets, vaults, pending-withdrawal queue). One row per movement. Filter by `root + accountId` (required) and optionally by `tokenId`.\n\n**Pagination.** Cursor-based, sorted by event index descending. Pass the previous `resumeToken` to fetch the next page; `null` when exhausted.\n\nPair `fromFundLocation` / `toFundLocation` to classify the movement (e.g. `WALLET \u2192 CROSS` = deposit, `ISOLATED \u2192 WALLET` = withdrawal, `CROSS \u2192 ISOLATED` = internal cash transfer).",
        "parameters": [
          {
            "name": "resumeToken",
            "required": false,
            "in": "query",
            "description": "Resume token for cursor-based pagination. Pass the resumeToken from the previous response to fetch the next page.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "required": false,
            "in": "query",
            "description": "Maximum number of results to return. The parameter is capped at 2000.",
            "schema": {
              "default": 10,
              "type": "number"
            }
          },
          {
            "name": "root",
            "required": true,
            "in": "query",
            "description": "User wallet address \u2014 the `root` of the margin account.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "accountId",
            "required": false,
            "in": "query",
            "description": "Sub-account index under `root` (0\u2013255). `0` is the main sub-account.",
            "schema": {
              "minimum": 0,
              "maximum": 255,
              "default": 0,
              "type": "number"
            }
          },
          {
            "name": "tokenId",
            "required": false,
            "in": "query",
            "description": "Optional collateral-token filter. The numeric `tokenId` of the asset (see `/v1/assets`). Omit to return movements for every token in the sub-account.",
            "schema": {
              "minimum": 1,
              "type": "number"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Returns transfer logs",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TransferLogsV2Response"
                }
              }
            }
          },
          "400": {
            "description": "Invalid query parameters"
          }
        },
        "tags": [
          "Accounts"
        ]
      }
    },
    "/v1/accounts/orders": {
      "get": {
        "operationId": "AccountsV2Controller_getOrders",
        "x-computing-unit": "1+",
        "summary": "Get orders (cursor-based pagination, sorted by last-updated)",
        "description": "Returns the sub-account's orders (both resting limit orders and historical market orders that swept the book) sorted by **last-updated** event index descending. Filter by `root + accountId` (required), `marketId`, `isActive`, and one or more `orderType`s.\n\n**Pagination caveat \u2014 read first.** The cursor advances on `eventIndex`, which is mutated whenever an order is filled, partially-filled, or cancelled. If an order is updated **after** it has already been returned in a previous page, paginating to the end of the dataset may silently miss or duplicate it.\n\n**Use `/v1/accounts/orders-by-placed-time` for any full-history scan / indexing job** \u2014 it sorts by the immutable placed-time index. This endpoint is appropriate for UIs showing recent activity.\n\nSee [`/v2/accounts/limit-orders`](https://docs.pendle.finance/boros-dev/Backend/api#which-limit-orders-endpoint-to-use) for the same trade-off written up in the dev docs.",
        "parameters": [
          {
            "name": "root",
            "required": true,
            "in": "query",
            "description": "User wallet address \u2014 the `root` of the margin account.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "accountId",
            "required": false,
            "in": "query",
            "description": "Sub-account index under `root` (0\u2013255). `0` is the main sub-account.",
            "schema": {
              "minimum": 0,
              "maximum": 255,
              "default": 0,
              "type": "number"
            }
          },
          {
            "name": "marketId",
            "required": false,
            "in": "query",
            "description": "Optional market filter. Omit to return orders across every market the sub-account has touched.",
            "schema": {
              "minimum": 1,
              "type": "number"
            }
          },
          {
            "name": "resumeToken",
            "required": false,
            "in": "query",
            "description": "Resume token for cursor-based pagination. Pass the resumeToken from the previous response to fetch the next page.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "required": false,
            "in": "query",
            "description": "Maximum number of results to return. The parameter is capped at 2000.",
            "schema": {
              "default": 10,
              "type": "number"
            }
          },
          {
            "name": "isActive",
            "required": false,
            "in": "query",
            "description": "`true` \u2192 only currently-resting orders (status = filling). `false` \u2192 only finalised orders (filled, cancelled, expired). Omit to return both.",
            "schema": {
              "example": true,
              "type": "boolean"
            }
          },
          {
            "name": "orderType",
            "required": false,
            "in": "query",
            "description": "NormalOrderType { LIMIT : 0, MARKET : 1 }. Comma-separated for multiple values (e.g., \"0,1\"). Filters by **order kind** \u2014 `MARKET` orders sweep the book and never rest, `LIMIT` orders rest until matched, cancelled, or expired. Time-In-Force (GTC / IOC / FOK / POST_ONLY) is a separate concept set at order placement. Conditional (TP / SL) orders are not served by this API.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Returns orders, sorted by last-updated block number descending",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LimitOrdersV2Response"
                }
              }
            }
          },
          "400": {
            "description": "Invalid query parameters or unsupported orderBy field"
          }
        },
        "tags": [
          "Accounts"
        ]
      }
    },
    "/v1/accounts/orders-by-placed-time": {
      "get": {
        "operationId": "AccountsV2Controller_getOrdersByPlacedTime",
        "x-computing-unit": "1+",
        "summary": "Get orders by placed time (cursor-based pagination, placed-index descending)",
        "description": "Returns **every** orderbook order (limit + market, every status) ever placed by the sub-account, sorted by `placedEventIndex` descending. Because `placedEventIndex` is set when the order is placed and never mutated thereafter, **iterating to the end of the cursor is guaranteed to yield each order exactly once** \u2014 regardless of fills, cancels, or expirations happening mid-pagination.\n\n**Use this for syncing/indexing jobs.** For UIs that just need to render recent activity, `/v1/accounts/orders` is cheaper because it can be filtered by `marketId` / `isActive` / `orderType`.\n\n**No filters.** Only `root` and `accountId` are honored \u2014 by design.",
        "parameters": [
          {
            "name": "root",
            "required": true,
            "in": "query",
            "description": "User wallet address \u2014 the `root` of the margin account.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "accountId",
            "required": false,
            "in": "query",
            "description": "Sub-account index under `root` (0\u2013255). `0` is the main sub-account.",
            "schema": {
              "minimum": 0,
              "maximum": 255,
              "default": 0,
              "type": "number"
            }
          },
          {
            "name": "resumeToken",
            "required": false,
            "in": "query",
            "description": "Resume token for cursor-based pagination. Pass the resumeToken from the previous response to fetch the next page.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "required": false,
            "in": "query",
            "description": "Maximum number of results to return. The parameter is capped at 2000.",
            "schema": {
              "default": 10,
              "type": "number"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Returns all orders, sorted by placed event index descending",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LimitOrdersV2Response"
                }
              }
            }
          },
          "400": {
            "description": "Invalid query parameters"
          }
        },
        "tags": [
          "Accounts"
        ]
      }
    },
    "/v1/accounts/position-update-events": {
      "get": {
        "operationId": "AccountsV2Controller_getPositionUpdateEvents",
        "x-computing-unit": "1+",
        "summary": "Get position-update events (cursor-based pagination)",
        "description": "Returns every on-chain event that moved this position's size or cost basis on a specific `(marketAcc, marketId)` pair: market-order fills, limit-order fills (maker side), and liquidations. Each row carries pre/post position state (`prevPositionS/F`, `postPositionS/F`) and the realised PnL booked at that event.\n\n**Identity.** Both `marketAcc` **and** `marketId` are required. For an isolated `marketAcc` they will match, but for a **cross** `marketAcc` (the one whose embedded marketId is `0xFFFFFF`) the `marketId` query param is what disambiguates the per-market position. Use `/v1/market-acc/encode` to construct `marketAcc`.\n\n**Pagination.** Cursor-based, sorted by event index descending. `fromBlockNumber` / `toBlockNumber` narrow the time range; `isLimitOrderTrade=true` filters to maker fills only.",
        "parameters": [
          {
            "name": "marketAcc",
            "required": true,
            "in": "query",
            "description": "Fully-packed 54-character `marketAcc` handle (cross or isolated). Build via `/v1/market-acc/encode`.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "marketId",
            "required": true,
            "in": "query",
            "description": "Numeric market id of the position. **Required even for isolated `marketAcc`s** (must equal the embedded marketId). For cross `marketAcc`s, this disambiguates which per-market position to pull events for.",
            "schema": {
              "minimum": 1,
              "type": "number"
            }
          },
          {
            "name": "limit",
            "required": false,
            "in": "query",
            "description": "Maximum number of results to return. The parameter is capped at 2000.",
            "schema": {
              "default": 10,
              "type": "number"
            }
          },
          {
            "name": "resumeToken",
            "required": false,
            "in": "query",
            "description": "Resume token for cursor-based pagination. Pass the resumeToken from the previous response to fetch the next page.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "fromBlockNumber",
            "required": false,
            "in": "query",
            "description": "Inclusive lower bound on block number.",
            "schema": {
              "minimum": 0,
              "type": "number"
            }
          },
          {
            "name": "toBlockNumber",
            "required": false,
            "in": "query",
            "description": "Inclusive upper bound on block number.",
            "schema": {
              "minimum": 0,
              "type": "number"
            }
          },
          {
            "name": "isLimitOrderTrade",
            "required": false,
            "in": "query",
            "description": "When `true`, returns only events where this `marketAcc` filled as the **maker** (its resting limit order was hit). When `false`, returns only **taker** fills. Omit to get both.",
            "schema": {
              "type": "boolean"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Returns position-update events",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TransactionsV2Response"
                }
              }
            }
          },
          "400": {
            "description": "Invalid query parameters"
          }
        },
        "tags": [
          "Accounts"
        ]
      }
    },
    "/v1/accounts/settlement-events": {
      "get": {
        "operationId": "AccountsV2Controller_getSettlementEvents",
        "x-computing-unit": "1+",
        "summary": "Get settlement events (cursor-based pagination)",
        "description": "Returns periodic funding-rate **settlement events** for the sub-account, optionally narrowed to a single `marketId`. A settlement is the on-chain event where a position's accrued floating-vs-fixed differential is realised into cash; it is independent of fills.\n\n**Identity.** Filter by `root + accountId` (required) plus optional `marketId`. **No `marketAcc` is needed** \u2014 settlements are queried by sub-account because cross/isolated positions on the same `(root, accountId, marketId)` settle on the same schedule.\n\n**Pagination.** Cursor-based, sorted by event index descending.\n\nSee [Settlement mechanics](https://docs.pendle.finance/boros-dev/Mechanics/Settlement) for the math.",
        "parameters": [
          {
            "name": "root",
            "required": true,
            "in": "query",
            "description": "User wallet address \u2014 the `root` of the margin account.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "accountId",
            "required": false,
            "in": "query",
            "description": "Sub-account index under `root` (0\u2013255). `0` is the main sub-account.",
            "schema": {
              "minimum": 0,
              "maximum": 255,
              "default": 0,
              "type": "number"
            }
          },
          {
            "name": "marketId",
            "required": false,
            "in": "query",
            "description": "Optional market filter. Omit to return settlements across every market the sub-account has positions in.",
            "schema": {
              "minimum": 1,
              "type": "number"
            }
          },
          {
            "name": "resumeToken",
            "required": false,
            "in": "query",
            "description": "Resume token for cursor-based pagination. Pass the resumeToken from the previous response to fetch the next page.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "required": false,
            "in": "query",
            "description": "Maximum number of results to return. The parameter is capped at 2000.",
            "schema": {
              "default": 10,
              "type": "number"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Returns settlement events",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SettlementsV2Response"
                }
              }
            }
          },
          "400": {
            "description": "Invalid query parameters"
          }
        },
        "tags": [
          "Accounts"
        ]
      }
    },
    "/v1/accounts/market-acc-infos": {
      "post": {
        "operationId": "AccountsV2Controller_getMarketAccInfos",
        "x-computing-unit": "1+",
        "summary": "Get market acc infos by marketAccs",
        "description": "Batch lookup of full margin-account state for up to **100** `marketAcc` handles in one call.\n\n**Identity.** Pass each handle as the **fully-packed 26-byte `marketAcc`** (use `/v1/market-acc/encode` to build it). For a cross account, embed `0xFFFFFF` as `marketId`; for isolated, embed the actual `marketId`. The two views return distinct slices of the same underlying balances \u2014 request both if you need both.\n\n**Cost.** Dynamic: **1 CU per 10 `marketAcc` handles** (e.g. `length=10` \u2192 1 CU, `length=100` \u2192 10 CU).\n\nSee [Margin mechanics](https://docs.pendle.finance/boros-dev/Mechanics/Margin#health-ratio) for IM / MM / health-ratio formulas.",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/GetMarketAccInfosV2Dto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Get market acc infos",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MarketAccInfosResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid `marketAcc` addresses"
          }
        },
        "tags": [
          "Accounts"
        ]
      }
    },
    "/v1/accounts/market-acc-infos-by-root": {
      "get": {
        "operationId": "AccountsV2Controller_getMarketAccInfosByRoot",
        "x-computing-unit": "5",
        "summary": "Get market acc infos by root",
        "description": "Returns full margin-account state for every funded `marketAcc` under `(root, accountId = 0)` \u2014 the main sub-account's cross handle plus each isolated handle holding non-zero collateral. **Only `accountId = 0` is enumerated; non-zero sub-accounts are not returned.** Useful to discover dust accounts on the main sub-account without knowing the `(tokenId, marketId)` triples up front.\n\nEach entry has the same shape as `/v1/accounts/market-acc-infos`: `totalCash`, `netBalance`, `positions[]`, IM/MM, etc. A `marketAcc` with non-zero collateral but no open positions will show `positions: []` and `netBalance == totalCash`.\n\n**Cost.** Flat **5** CU regardless of how many funded `marketAcc`s are returned.",
        "parameters": [
          {
            "name": "root",
            "required": true,
            "in": "query",
            "description": "User wallet address \u2014 the `root` of the margin account.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Market acc infos for every funded sub-account",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MarketAccInfosResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid `root` address format"
          }
        },
        "tags": [
          "Accounts"
        ]
      }
    },
    "/v1/accounts/active-positions": {
      "get": {
        "operationId": "AccountsV2Controller_getActivePositions",
        "x-computing-unit": "1",
        "summary": "Get active positions (with PnL summary)",
        "description": "Returns every position with **non-zero signed size** belonging to the sub-account, across both its cross `marketAcc` and any isolated `marketAcc`s, each enriched with a per-position PnL summary.\n\n**Identity.** Filter by `root + accountId` (the sub-account); positions across **all** entered markets and isolated accounts are returned in one call. Use this to populate a portfolio view in a single request.\n\n**\"Active\" vs \"entered\".** A market is *active* iff the user holds a non-zero position there. A market is *entered* (returned by `/v1/accounts/entered-markets`) iff the user has called `enterMarket` on a cross account; an entered market with zero size will not appear here. See [Margin mechanics](https://docs.pendle.finance/boros-dev/Mechanics/Margin) for why entering is required for cross.",
        "parameters": [
          {
            "name": "root",
            "required": true,
            "in": "query",
            "description": "User wallet address \u2014 the `root` of the margin account.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "accountId",
            "required": false,
            "in": "query",
            "description": "Sub-account index under `root` (0\u2013255). `0` is the main sub-account.",
            "schema": {
              "minimum": 0,
              "maximum": 255,
              "default": 0,
              "type": "number"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Active positions with per-position PnL summary",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ActivePositionsWithPnlResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid query parameters"
          }
        },
        "tags": [
          "Accounts"
        ]
      }
    },
    "/v1/accounts/entered-markets": {
      "get": {
        "operationId": "AccountsV2Controller_getEnteredMarkets",
        "x-computing-unit": "1",
        "summary": "Get entered markets for a cross market account",
        "description": "Returns every market the **cross** `marketAcc` has called `enterMarket` on and has not yet exited. **Cross-only**: entering a market is the on-chain bookkeeping step that lets a cross account use that market's PnL toward its shared margin pool \u2014 it is a prerequisite for trading on a market in cross mode. Isolated `marketAcc`s do not need to enter markets, so calling this on an isolated handle returns an empty list.\n\n**Identity.** Pass the fully-packed cross `marketAcc` (with `marketId = 0xFFFFFF`). Use `/v1/market-acc/encode` to build it.\n\n**Entered \u2260 active.** A market may be entered but currently flat (no position). Use `/v1/accounts/active-positions` for the dual view filtered to non-zero size.",
        "parameters": [
          {
            "name": "marketAcc",
            "required": true,
            "in": "query",
            "description": "Fully-packed 54-character cross `marketAcc` (i.e. embedded `marketId = 0xFFFFFF`). Build via `/v1/market-acc/encode` with `marketId` omitted. Passing an isolated `marketAcc` is allowed but returns an empty list.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Returns markets that the user has entered and not exited. Only suitable for cross `marketAcc`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EnteredMarketsResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid `marketAcc` address"
          }
        },
        "tags": [
          "Accounts"
        ]
      }
    },
    "/v1/accounts/margin-config": {
      "get": {
        "operationId": "AccountsV2Controller_getMarginConfig",
        "x-computing-unit": "1",
        "summary": "Get personal margin config for an account",
        "description": "Returns the account's on-chain personal initial-margin overrides \u2014 one entry per market where a personal `kIM` is set for this `marketAcc`, empty when none. The personal `kIM` is a system-imposed risk floor (not a user setting); overlay it on the global `market.config.kIM` to compute per-user margin. Returns an empty list until personal margin bumps go live.",
        "parameters": [
          {
            "name": "marketAcc",
            "required": true,
            "in": "query",
            "description": "Fully-packed 54-character `marketAcc` (cross or isolated) whose personal margin overrides to return. Build via `/v1/market-acc/encode`.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MarginConfigsResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid `marketAcc`"
          }
        },
        "tags": [
          "Accounts"
        ]
      }
    },
    "/v1/accounts/light-event-feed": {
      "get": {
        "operationId": "AccountsV2Controller_getLightEventFeed",
        "x-computing-unit": "1+",
        "summary": "Get fast-sync live-feed event diffs (premium-only, last 1 hour)",
        "description": "Returns the most recent per-event diffs live feed. Only for premium users.",
        "parameters": [
          {
            "name": "root",
            "required": true,
            "in": "query",
            "description": "User wallet address \u2014 the `root` of the margin account.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "required": false,
            "in": "query",
            "description": "Maximum number of results to return. The parameter is capped at 1000.",
            "schema": {
              "default": 10,
              "type": "number"
            }
          },
          {
            "name": "resumeToken",
            "required": false,
            "in": "query",
            "description": "Resume token for cursor-based pagination. Pass the resumeToken from the previous response to fetch the next page.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "onlyPositionChange",
            "required": false,
            "in": "query",
            "description": "Filter to events that carry a `position` change",
            "schema": {
              "default": false,
              "type": "boolean"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Live-feed event diffs",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LightEventFeedResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid query parameters or `resumeToken`"
          }
        },
        "tags": [
          "Accounts"
        ]
      }
    },
    "/v1/incentives/maker-incentives/campaigns/{marketId}": {
      "get": {
        "operationId": "IncentivesController_getMakerIncentiveCampaign",
        "x-computing-unit": "1+",
        "summary": "Get maker incentive campaign for a market",
        "description": "Returns the **PENDLE-denominated maker incentive program** for a specific market.\n\n`maker` is optional \u2014 if not provided, per-maker fields stay at 0 and CU cost is 1. Supplying a `maker` triggers the per-maker lookup and the call is charged at **2 CU**.",
        "parameters": [
          {
            "name": "marketId",
            "required": true,
            "in": "path",
            "description": "Numeric market identifier (1-based).",
            "schema": {
              "minimum": 1,
              "type": "number"
            }
          },
          {
            "name": "maker",
            "required": false,
            "in": "query",
            "description": "Maker wallet address (optional). When omitted, `currentEligibleShare` and `accumulatedReward` are returned as `0`.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Maker incentive campaign",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MakerIncentiveCampaignResponse"
                }
              }
            }
          }
        },
        "tags": [
          "Miscellaneous"
        ]
      }
    },
    "/v1/incentives/amm-incentives": {
      "get": {
        "operationId": "IncentivesController_getAmmIncentives",
        "summary": "Get user AMM incentives",
        "description": "Returns **AMM liquidity-provider rewards** for a single user across all Boros AMMs (PENDLE incentive emissions plus accumulated swap fees), broken down by market.\n\n`user` is optional \u2014 if not provided, aggregates are 0 and `perMarket` is empty.",
        "parameters": [
          {
            "name": "user",
            "required": false,
            "in": "query",
            "description": "User wallet address (optional). When omitted, `accruedAmountInUsd` / `unclaimedAmountInUsd` are `0` and `perMarket` is empty.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "User AMM incentives",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AmmIncentivesResponse"
                }
              }
            }
          }
        },
        "tags": [
          "Miscellaneous"
        ]
      }
    },
    "/v1/maker-volume/rolling": {
      "get": {
        "operationId": "MakerVolumeController_getRollingVolume",
        "x-computing-unit": "3",
        "summary": "Get rolling maker volume per market",
        "deprecated": true,
        "description": "**Deprecated \u2014 use `/v1/trade-volume/rolling`**, which takes the same query parameters and window and reports each market's total traded volume. There, `userVolume` becomes `userMakerVolume`.\n\nMaker volume per market over a rolling window ending now: fills where the account was the maker, its resting limit order having been hit.\n\n**Units.** Raw 18-decimal integer as a string, in that market's notional unit \u2014 **not summable across markets**, which settle in different collateral.\n\n**`windowHours`** min 1, max 168, default 24. **`user`** (optional) is matched at wallet (`root`) level and adds `userVolume` to each market.\n\n**Cost.** Flat 3 CU.",
        "parameters": [
          {
            "name": "user",
            "required": false,
            "in": "query",
            "description": "Wallet address (`root`) to additionally report volume for. Matched at wallet level, covering every sub-account under it. When supplied, each market gains a `userVolume` field; when omitted, only the all-user `volume` is returned.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "windowHours",
            "required": false,
            "in": "query",
            "description": "Length of the rolling window in hours, counted backwards from the time of the request. Min **1**, max **168** (7 days).",
            "schema": {
              "minimum": 1,
              "maximum": 168,
              "default": 24,
              "example": 24,
              "type": "number"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Rolling maker volume",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RollingVolumeResponse"
                }
              }
            }
          }
        },
        "tags": [
          "Miscellaneous"
        ]
      }
    },
    "/v1/trade-volume/rolling": {
      "get": {
        "operationId": "TradeVolumeController_getRollingTradeVolume",
        "x-computing-unit": "3",
        "summary": "Get rolling traded volume per market",
        "description": "Traded volume per market over a rolling window ending now, across all users. Supersedes `/v1/maker-volume/rolling`.\n\n**Units.** Raw 18-decimal integer as a string, in that market's notional unit \u2014 **not summable across markets**, which settle in different collateral.\n\n**`windowHours`** min 1, max 168, default 24.\n\n**`user`** (optional, wallet `root` level) adds that user's own volume to each market: `userMakerVolume` from their limit orders being filled, `userTakerVolume` from their market orders.\n\n**Cost.** Flat 3 CU.",
        "parameters": [
          {
            "name": "user",
            "required": false,
            "in": "query",
            "description": "Wallet address (`root`) to also report volume for, covering every sub-account under it. Adds `userMakerVolume` / `userTakerVolume` to each market.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "windowHours",
            "required": false,
            "in": "query",
            "description": "Rolling window length in hours, counted back from now. Min 1, max 168 (7 days).",
            "schema": {
              "minimum": 1,
              "maximum": 168,
              "default": 24,
              "example": 24,
              "type": "number"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Rolling traded volume",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RollingTradeVolumeResponse"
                }
              }
            }
          }
        },
        "tags": [
          "Miscellaneous"
        ]
      }
    },
    "/v1/agents/expiry-time": {
      "get": {
        "operationId": "AgentsV2Controller_getAgentExpiryTime",
        "x-computing-unit": "1",
        "summary": "Get agent expiry time",
        "description": "Reads the on-chain expiry of a specific (`root`, `accountId`, `agentAddress`) tuple. Agents are externally-controlled addresses that the user has approved to submit non-sensitive trading actions (place/cancel orders, etc.) on behalf of a margin account.\n\n- **`expiryTime`** is the Unix-seconds (UTC) deadline after which the agent's signed actions will be rejected.\n- **`expiryTime == 0`** means the agent has never been approved (or has been explicitly revoked).\n- **`expiryTime <= now`** means the approval has lapsed.\n\nTo refresh an expiring agent, the user must submit a new on-chain approval; the next call will then reflect the new expiry.\n\nSee [Agent Trading guide](https://docs.pendle.finance/boros-dev/Backend/agent) for the full agent lifecycle.",
        "parameters": [
          {
            "name": "root",
            "required": true,
            "in": "query",
            "description": "User wallet address \u2014 the `root` of the margin account.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "accountId",
            "required": false,
            "in": "query",
            "description": "Sub-account index under `root` (0\u2013255). `0` is the main sub-account.",
            "schema": {
              "minimum": 0,
              "maximum": 255,
              "default": 0,
              "type": "number"
            }
          },
          {
            "name": "agentAddress",
            "required": true,
            "in": "query",
            "description": "Address of the agent whose expiry is being looked up.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Unix seconds (UTC) after which the agent's authorization is no longer valid. `0` means the agent is not approved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AgentExpiryTimeResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid root address, account ID, or agent address"
          }
        },
        "tags": [
          "Agents"
        ]
      }
    },
    "/v1/strategies": {
      "get": {
        "operationId": "StrategiesV2Controller_findStrategies",
        "x-computing-unit": "3",
        "summary": "Find funding-rate arbitrage strategies with positive `aprTimesMaxLeverage` and more than 10 days to maturity",
        "description": "Returns **cross-venue spread** strategies \u2014 pairs of (long market, short market) grouped by the same `(assetSymbol, tokenId, maturity)` (i.e. identical maturity but different funding-rate venues) \u2014 that satisfy:\n\n- both markets have **> 10 days to maturity**, and\n- the estimated APR \u00d7 max leverage (net of fees and combined margin) is strictly positive.\n\nResults are sorted by `aprTimesMaxLeverage` descending.",
        "parameters": [],
        "responses": {
          "200": {
            "description": "Returns available strategies",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FindStrategiesResponse"
                }
              }
            }
          }
        },
        "tags": [
          "Miscellaneous"
        ]
      }
    },
    "/v1/gas-price/current": {
      "get": {
        "operationId": "GasPriceV2Controller_getGasPriceInfo",
        "x-computing-unit": "1",
        "summary": "Get gas price info and order gas estimation",
        "description": "Returns the latest known Arbitrum One gas price (Wei + USD) and an estimated USD cost for placing a **single limit order**. Use `timestamp` to gauge staleness; numeric fields may be `null` if upstream data is unavailable.",
        "parameters": [],
        "responses": {
          "200": {
            "description": "Gas price information and order gas estimation",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GasPriceInfoResponse"
                }
              }
            }
          }
        },
        "tags": [
          "Miscellaneous"
        ]
      }
    },
    "/v1/leaderboard": {
      "get": {
        "operationId": "LeaderboardV2Controller_getLeaderboard",
        "x-computing-unit": "2",
        "summary": "Get leaderboard data",
        "description": "Returns the trading leaderboard scoped to a single (`period`, `tokenId`) pair, **ranked by ROI** (1-based, ordinal, no ties).\n\nData is updated daily; `snapshotTimestamp` reports the as-of time of the served snapshot. Pagination is offset-based via `limit` (default 100, max 1000) and `offset` (default 0).",
        "parameters": [
          {
            "name": "period",
            "required": true,
            "in": "query",
            "description": "Leaderboard window. `7d` = trailing 7 calendar days, `30d` = trailing 30 calendar days, `all_time` = since genesis.",
            "schema": {
              "example": "7d",
              "enum": [
                "all_time",
                "30d",
                "7d"
              ],
              "type": "string"
            }
          },
          {
            "name": "tokenId",
            "required": true,
            "in": "query",
            "description": "Collateral token id the leaderboard is scoped to (one leaderboard per token). Amounts in the response are denominated in this token as 18-decimal fixed-point integers; the scale is 1e18 for every token, independent of its ERC-20 decimals.",
            "schema": {
              "minimum": 1,
              "type": "number"
            }
          },
          {
            "name": "limit",
            "required": false,
            "in": "query",
            "description": "Page size \u2014 number of ranked entries to return (1-1000).",
            "schema": {
              "default": 100,
              "example": 100,
              "type": "number"
            }
          },
          {
            "name": "offset",
            "required": false,
            "in": "query",
            "description": "Number of entries to skip from the top of the ranking. Combine with `limit` for offset pagination.",
            "schema": {
              "default": 0,
              "example": 0,
              "type": "number"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LeaderboardResponse"
                }
              }
            }
          }
        },
        "tags": [
          "Miscellaneous"
        ]
      }
    },
    "/v1/leaderboard/search": {
      "get": {
        "operationId": "LeaderboardV2Controller_searchUser",
        "x-computing-unit": "1",
        "summary": "Check specific user in the leaderboard",
        "description": "Returns a single (`userAddress`, `accountId`) row for the (`period`, `tokenId`) leaderboard.\n\n- If the user **is** ranked, all fields are populated including `rank` and `roi`.\n- If the user is **not** ranked (e.g. did not meet the minimum-capital filter), `rank` and `roi` are omitted; `pnl` and `tradingVolume` are still populated for the requested period.\n- If the user has no recorded activity, all amount fields return `\"0\"`.",
        "parameters": [
          {
            "name": "userAddress",
            "required": true,
            "in": "query",
            "description": "User wallet address \u2014 the `root` of the margin account.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "accountId",
            "required": true,
            "in": "query",
            "description": "Sub-account index under `root` (0\u2013255). `0` is the main sub-account.",
            "schema": {
              "minimum": 0,
              "maximum": 255,
              "type": "number"
            }
          },
          {
            "name": "period",
            "required": true,
            "in": "query",
            "description": "Leaderboard window. `7d`, `30d`, or `all_time`.",
            "schema": {
              "example": "7d",
              "enum": [
                "all_time",
                "30d",
                "7d"
              ],
              "type": "string"
            }
          },
          {
            "name": "tokenId",
            "required": true,
            "in": "query",
            "description": "Numeric `tokenId` of the collateral asset.",
            "schema": {
              "minimum": 1,
              "type": "number"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UserSearchResponse"
                }
              }
            }
          }
        },
        "tags": [
          "Miscellaneous"
        ]
      }
    },
    "/v1/total-value-locked": {
      "get": {
        "operationId": "TvlV2Controller_getTvl",
        "x-computing-unit": "1",
        "summary": "Get protocol TVL",
        "description": "Returns the Total Value Locked of the Boros protocol in USD, with a per-collateral-token `breakdown`.",
        "parameters": [],
        "responses": {
          "200": {
            "description": "Protocol TVL with per-collateral-token breakdown",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TvlResponse"
                }
              }
            }
          }
        },
        "tags": [
          "Miscellaneous"
        ]
      }
    },
    "/v1/send-txs/bulk-calls": {
      "post": {
        "operationId": "SendTxsController_bulkCalls",
        "x-computing-unit": "5",
        "summary": "Submit agent-signed calls",
        "description": "Submit one or more calldatas, each accompanied by the agent's EIP-712 signature, to be executed on-chain via the Router. Mirrors send-txs-bot's regular-agent /v3/agent/bulk-direct-call: returns a per-call `TxResponse[]` in the SAME order as the input, with on-chain status decoded from the receipt. Gas balance is charged up front via the shared GasTrackingService \u2014 submission throws BadRequestException if the signer is below the gas-balance floor.",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/BulkAgentExecuteDto"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/TxResponse"
                  }
                }
              }
            }
          },
          "400": {
            "description": "Invalid request, bulk size > limit, gas balance below floor, or unsorted/used nonces"
          },
          "500": {
            "description": "send-txs-bot-v2 unreachable or returned failure"
          }
        },
        "tags": [
          "Send-Txs"
        ]
      }
    },
    "/v1/send-txs/approve": {
      "post": {
        "operationId": "SendTxsController_approve",
        "x-computing-unit": "5",
        "summary": "Approve an agent",
        "description": "Approve an agent wallet to act on behalf of a user account. Calldata must encode the Router.approveAgent function. The endpoint validates the function selector to prevent gas-balance bypass attacks. Free action \u2014 the user is not charged gas balance; the bot wallet pays the on-chain gas. Mirrors send-txs-bot's /v1/agent/approve.",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ApproveAgentQueryDto"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ApproveAgentResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid approveAgentCalldata or simulation reverted"
          }
        },
        "tags": [
          "Send-Txs"
        ]
      }
    },
    "/v1/send-txs/trace": {
      "post": {
        "operationId": "SendTxsController_trace",
        "x-computing-unit": "5",
        "summary": "Trace a (agent, nonce) submission",
        "description": "Returns the tracked status of the submission the given (agent, nonce) was sent in: HAVENT_SEEN, PROCESSING, PROCESSED (with `result` shaped like a single /bulk-calls `TxResponse` for that nonce \u2014 currently carrying just the on-chain txHash; use /tx-status with that hash for full per-call status), or SEND_FAILED. PROCESSING means the bot is mid-send \u2014 clients should poll this endpoint themselves if they need to wait for it to resolve.",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TraceQueryDto"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TraceResponse"
                }
              }
            }
          }
        },
        "tags": [
          "Send-Txs"
        ]
      }
    },
    "/v1/send-txs/tx-status": {
      "post": {
        "operationId": "SendTxsController_txStatus",
        "x-computing-unit": "5",
        "summary": "Get transaction status",
        "description": "Returns the on-chain status of a Router tryAggregate transaction submitted via /bulk-calls or /approve, with per-call success/revert decoded from the receipt (or from `debug_traceTransaction` if the outer tx reverted).",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TxStatusQueryDto"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DedicatedTxStatusResponseV2"
                }
              }
            }
          }
        },
        "tags": [
          "Send-Txs"
        ]
      }
    },
    "/v1/send-txs/tx-status-with-events": {
      "post": {
        "operationId": "SendTxsController_txStatusWithEvents",
        "x-computing-unit": "5",
        "summary": "Get transaction status with limit order events",
        "description": "Same as /tx-status but additionally parses limit order events (placed / cancelled) and market order executions from the transaction logs, grouped by the tryAggregate call that emitted them.",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TxStatusQueryDto"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DedicatedTxStatusWithEventsResponse"
                }
              }
            }
          }
        },
        "tags": [
          "Send-Txs"
        ]
      }
    },
    "/v1/send-txs/dedicated/bulk-calls": {
      "post": {
        "operationId": "SendTxsController_dedicatedBulkCalls",
        "x-computing-unit": "5",
        "summary": "Submit dedicated agent-signed calls",
        "description": "Submit one or more agent-signed calldatas via a dedicated bot. Differences from `/bulk-calls`:\n\n- Sent by a dedicated bot, so the batch lands in a transaction of its own (not merged with other signers' calls).\n- Accepts an optional `simulate` flag \u2014 when `true`, calls are simulated first and reverters are reported in `failedSimulations`. With `requireSuccess: true`, nothing is broadcast if any call would revert; with `requireSuccess: false`, reverters are filtered out and survivors are broadcast.\n\n**Whitelist required.** Only approved accounts can call this endpoint. Contact us if you need access.",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DedicatedBulkAgentExecuteDto"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DedicatedTxResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid request, bulk size > limit, unsorted/used nonces, or executeCashSwap selector used"
          },
          "500": {
            "description": "send-txs-bot-v2 unreachable or returned failure"
          }
        },
        "tags": [
          "Send-Txs"
        ]
      }
    },
    "/v1/api-keys": {
      "post": {
        "operationId": "ApiKeysController_create",
        "summary": "Create an API signing key",
        "description": "Generates an Ed25519 keypair, stores only the public key and returns the private key **once**. A root may hold at most 6 live keys. Authenticated by an EIP-712 ApiKeyAction signature with action=\"create\".",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateSigningKeyDto"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SigningKeySecretDto"
                }
              }
            }
          },
          "422": {
            "description": "Key limit reached, or the name is already in use"
          }
        },
        "tags": [
          "API Keys"
        ]
      },
      "get": {
        "operationId": "ApiKeysController_list",
        "summary": "List your live API signing keys",
        "description": "Never returns secrets. Authenticated by the dashboard SIWE session cookie, or by an EIP-712 ApiKeyAction signature with action=\"list\" passed as query parameters.",
        "parameters": [
          {
            "name": "root",
            "required": false,
            "in": "query",
            "description": "Root wallet. Required only without a session cookie.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "timestamp",
            "required": false,
            "in": "query",
            "description": "Unix milliseconds; within 5 minutes of server time",
            "schema": {
              "type": "number"
            }
          },
          {
            "name": "nonce",
            "required": false,
            "in": "query",
            "description": "Single-use 32-byte hex nonce",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "signature",
            "required": false,
            "in": "query",
            "description": "EIP-712 ApiKeyAction signature with action=\"list\"",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/SigningKeyDto"
                  }
                }
              }
            }
          }
        },
        "tags": [
          "API Keys"
        ]
      }
    },
    "/v1/api-keys/update": {
      "post": {
        "operationId": "ApiKeysController_update",
        "summary": "Update one API signing key",
        "description": "Patch semantics \u2014 omitted fields are left unchanged. Never touches key material, so anything already signing with this key keeps working.",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateSigningKeyDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SigningKeyDto"
                }
              }
            }
          },
          "400": {
            "description": "No updatable field was provided"
          },
          "422": {
            "description": "The name is already in use by another live key"
          }
        },
        "tags": [
          "API Keys"
        ]
      }
    },
    "/v1/api-keys/revoke": {
      "post": {
        "operationId": "ApiKeysController_revoke",
        "summary": "Revoke one API signing key",
        "description": "Frees both its name and a key slot.",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SigningKeyIdDto"
              }
            }
          }
        },
        "responses": {
          "204": {
            "description": "Revoked"
          },
          "404": {
            "description": "No live key with that id belongs to this root"
          }
        },
        "tags": [
          "API Keys"
        ]
      }
    },
    "/v1/api-keys/eip712-domain": {
      "get": {
        "operationId": "ApiKeysMetaController_domain",
        "summary": "EIP-712 domain for ApiKeyAction signatures",
        "description": "Public. Sign management envelopes against exactly this domain.",
        "parameters": [],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Eip712DomainDto"
                }
              }
            }
          }
        },
        "tags": [
          "API Keys"
        ]
      }
    },
    "/v1/stop-orders": {
      "get": {
        "operationId": "StopOrdersController_getStopOrders",
        "x-computing-unit": "1+",
        "summary": "List your stop orders",
        "description": "Cursor-paginated conditional (stop-loss / take-profit) orders owned by the API key. Sorted newest first; pass the previous response's `resumeToken` for the next page, which is `null` once exhausted.\n\n**Cost.** `1 + ceil(limit / 200) - 1` CU.",
        "parameters": [
          {
            "name": "marketId",
            "required": false,
            "in": "query",
            "description": "Optional market filter. Omit to return conditional orders across every market.",
            "schema": {
              "minimum": 1,
              "type": "number"
            }
          },
          {
            "name": "tokenId",
            "required": false,
            "in": "query",
            "description": "Optional collateral-token filter (see `/v1/assets`). Ignored when `marketId` is supplied.",
            "schema": {
              "minimum": 1,
              "type": "number"
            }
          },
          {
            "name": "isActive",
            "required": false,
            "in": "query",
            "description": "`true` \u2192 only live orders (pending / executing / retrying). `false` \u2192 only finalised ones (filled, cancelled, expired, failed). Omit to return both.",
            "schema": {
              "example": true,
              "type": "boolean"
            }
          },
          {
            "name": "resumeToken",
            "required": false,
            "in": "query",
            "description": "Resume token for cursor-based pagination. Pass the resumeToken from the previous response to fetch the next page.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "required": false,
            "in": "query",
            "description": "Maximum number of results to return. The parameter is capped at 200.",
            "schema": {
              "default": 10,
              "type": "number"
            }
          },
          {
            "name": "x-pendle-auth",
            "in": "header",
            "description": "Compact JWT signed with an API signing key (alg EdDSA, kid = keyId). Claims: iss=pendle, aud=pendle-open-api, sub=keyId, jti, nbf, exp (<= nbf+120s), uri=\"<METHOD> <path>\".",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StopOrdersResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid query parameters"
          },
          "401": {
            "description": "Missing or invalid authentication"
          }
        },
        "tags": [
          "Stop Orders"
        ]
      }
    },
    "/v1/stop-orders/prepare": {
      "get": {
        "operationId": "StopOrdersController_prepareTpslStopOrder",
        "x-computing-unit": "1",
        "summary": "Prepare a take-profit / stop-loss order",
        "description": "Builds the order struct and trigger condition to sign. Sign `req` (with `offchainCondition` replaced by its keccak256 hash) and submit it to `POST /stop-orders/place`.\n\nThe returned `req.account` is derived from the API key \u2014 it is not something you choose.\n\nSee the [Stop Orders guide](https://docs.pendle.finance/boros-dev/Backend/stop-orders) for the full workflow.",
        "parameters": [
          {
            "name": "marketId",
            "required": true,
            "in": "query",
            "description": "Market of the position the trigger protects.",
            "schema": {
              "minimum": 1,
              "type": "number"
            }
          },
          {
            "name": "isCross",
            "required": true,
            "in": "query",
            "description": "Which account holds the position: `true` \u2192 the **cross** account for this `tokenId`, `false` \u2192 the **isolated** account on `marketId`. Must match where the position actually sits, otherwise the prepared order targets an empty account.",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "side",
            "required": true,
            "in": "query",
            "description": "Side { LONG : 0, SHORT : 1 }",
            "schema": {
              "enum": [
                0,
                1
              ],
              "type": "number"
            }
          },
          {
            "name": "type",
            "required": true,
            "in": "query",
            "description": "StopAprOrderType { TAKE_PROFIT_MARKET : 2, STOP_LOSS_MARKET : 3 }",
            "schema": {
              "enum": [
                2,
                3
              ],
              "type": "number"
            }
          },
          {
            "name": "closePosition",
            "required": true,
            "in": "query",
            "description": "If `true` the trigger closes the whole position and `size` is ignored; if `false` it closes `size`.",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "size",
            "required": true,
            "in": "query",
            "description": "BigInt string of the notional size to close, scaled by 10^18. Ignored when `closePosition` is `true`.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "stopApr",
            "required": true,
            "in": "query",
            "description": "APR that arms the trigger (decimal, e.g. `0.085` = 8.5%). For a take-profit on a long this is the upper bound; for a stop-loss on a long, the lower bound.",
            "schema": {
              "type": "number"
            }
          },
          {
            "name": "x-pendle-auth",
            "in": "header",
            "description": "Compact JWT signed with an API signing key (alg EdDSA, kid = keyId). Claims: iss=pendle, aud=pendle-open-api, sub=keyId, jti, nbf, exp (<= nbf+120s), uri=\"<METHOD> <path>\".",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PrepareTpslStopOrderResponse"
                }
              }
            }
          },
          "400": {
            "description": "Unknown market, isolated-only market requested as cross, or zero size"
          },
          "401": {
            "description": "Missing or invalid authentication"
          }
        },
        "tags": [
          "Stop Orders"
        ]
      }
    },
    "/v1/stop-orders/detail": {
      "get": {
        "operationId": "StopOrdersController_getStopOrder",
        "x-computing-unit": "1",
        "summary": "Get one stop order",
        "description": "Looks the order up scoped to the API key owner, so an id belonging to another root reads as not found.\n\nThe id travels as a query parameter, not a path segment, so one signed `x-pendle-auth` token covers every lookup \u2014 the token binds the path only.",
        "parameters": [
          {
            "name": "orderId",
            "required": true,
            "in": "query",
            "description": "Conditional-order hash \u2014 the `orderHash` returned by `POST /stop-orders/place`.",
            "schema": {
              "example": "0xabcdef1234567890abcdef1234567890abcdef1234567890abcdef1234567890",
              "type": "string"
            }
          },
          {
            "name": "x-pendle-auth",
            "in": "header",
            "description": "Compact JWT signed with an API signing key (alg EdDSA, kid = keyId). Claims: iss=pendle, aud=pendle-open-api, sub=keyId, jti, nbf, exp (<= nbf+120s), uri=\"<METHOD> <path>\".",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StopOrderResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid authentication"
          },
          "404": {
            "description": "No stop order with that id belongs to this root"
          }
        },
        "tags": [
          "Stop Orders"
        ]
      }
    },
    "/v1/stop-orders/place": {
      "post": {
        "operationId": "StopOrdersController_placeStopOrder",
        "x-computing-unit": "5",
        "summary": "Place a stop order",
        "description": "Submits an agent-signed conditional order prepared by `GET /stop-orders/prepare`. The order executes under the account derived from the API key; `agent` must already be approved on that account.",
        "parameters": [
          {
            "name": "x-pendle-auth",
            "in": "header",
            "description": "Compact JWT signed with an API signing key (alg EdDSA, kid = keyId). Claims: iss=pendle, aud=pendle-open-api, sub=keyId, jti, nbf, exp (<= nbf+120s), uri=\"<METHOD> <path>\".",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PlaceStopOrderBodyDto"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PlaceStopOrderResponse"
                }
              }
            }
          },
          "400": {
            "description": "Market expired, invalid signature, or expired agent"
          },
          "401": {
            "description": "Missing or invalid authentication"
          },
          "403": {
            "description": "Agent is disabled"
          }
        },
        "tags": [
          "Stop Orders"
        ]
      }
    },
    "/v1/stop-orders/cancel": {
      "post": {
        "operationId": "StopOrdersController_cancelStopOrders",
        "x-computing-unit": "5",
        "summary": "Cancel stop orders",
        "description": "Cancels up to 50 conditional orders in one signed request. All-or-nothing: the request fails if any id is unknown, already finalised, or owned by another account.",
        "parameters": [
          {
            "name": "x-pendle-auth",
            "in": "header",
            "description": "Compact JWT signed with an API signing key (alg EdDSA, kid = keyId). Claims: iss=pendle, aud=pendle-open-api, sub=keyId, jti, nbf, exp (<= nbf+120s), uri=\"<METHOD> <path>\".",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CancelStopOrdersBodyDto"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CancelStopOrdersResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid signature, expired agent, or unknown / finalised order id"
          },
          "401": {
            "description": "Missing or invalid authentication"
          },
          "403": {
            "description": "Agent is disabled"
          }
        },
        "tags": [
          "Stop Orders"
        ]
      }
    }
  },
  "info": {
    "title": "Boros Open API Docs",
    "description": "Boros Open API documentation (redesigned).\n\n**Compute Units (CU).** Every endpoint advertises a CU cost via the `x-computing-unit` OpenAPI extension, also returned as the `x-computing-unit` response header. A value like `1+` means the endpoint has a variable cost \u2014 the minimum is charged for an empty / default request, and the total scales with the work actually performed (usually page size, number of markets, or number of indicators).",
    "version": "1.0",
    "contact": {
      "name": "Pendle Finance",
      "url": "https://pendle.finance",
      "email": "hello@pendle.finance"
    }
  },
  "tags": [
    {
      "name": "Market Account (Utilities)",
      "description": ""
    },
    {
      "name": "Assets",
      "description": ""
    },
    {
      "name": "Markets",
      "description": ""
    },
    {
      "name": "Calldata-Builder (User-Signed)",
      "description": ""
    },
    {
      "name": "Calldata-Builder (Agent-Executable)",
      "description": ""
    },
    {
      "name": "Simulations",
      "description": ""
    },
    {
      "name": "Stop Orders",
      "description": ""
    },
    {
      "name": "Accounts",
      "description": ""
    },
    {
      "name": "Agents",
      "description": ""
    },
    {
      "name": "Indicators",
      "description": ""
    },
    {
      "name": "API Keys",
      "description": ""
    },
    {
      "name": "Miscellaneous",
      "description": ""
    }
  ],
  "servers": [
    {
      "url": "https://api-boros.pendle.finance/apis",
      "description": "production (via proxy)"
    }
  ],
  "components": {
    "schemas": {
      "AMMStateResponse": {
        "type": "object",
        "properties": {
          "totalFloatAmount": {
            "type": "string"
          },
          "normFixedAmount": {
            "type": "string"
          },
          "totalLp": {
            "type": "string"
          },
          "latestFTime": {
            "type": "string"
          },
          "maturity": {
            "type": "string"
          },
          "seedTime": {
            "type": "string"
          },
          "minAbsRate": {
            "type": "string"
          },
          "maxAbsRate": {
            "type": "string"
          },
          "cutOffTimestamp": {
            "type": "string"
          },
          "isCutOffReached": {
            "type": "boolean"
          },
          "disabled": {
            "type": "boolean"
          },
          "disabledAt": {
            "type": "number"
          },
          "disabledReason": {
            "type": "string"
          }
        },
        "required": [
          "totalFloatAmount",
          "normFixedAmount",
          "totalLp",
          "latestFTime",
          "maturity",
          "seedTime",
          "minAbsRate",
          "maxAbsRate",
          "cutOffTimestamp",
          "isCutOffReached",
          "disabled"
        ]
      },
      "GetAMMStateResponse": {
        "type": "object",
        "properties": {
          "marketId": {
            "type": "number",
            "description": "Numeric market identifier (1-based)."
          },
          "tokenId": {
            "type": "number",
            "description": "Numeric `tokenId` of the collateral asset.",
            "example": 1
          },
          "ammId": {
            "type": "number",
            "description": "AMM identifier for this `(marketId, tokenId)` vault."
          },
          "state": {
            "description": "Raw on-chain AMM contract state, used to combine with the order book.",
            "allOf": [
              {
                "$ref": "#/components/schemas/AMMStateResponse"
              }
            ]
          },
          "isPositive": {
            "type": "boolean",
            "description": "AMM directional bias: `true` = long-biased, `false` = short-biased."
          },
          "feeRate": {
            "type": "string",
            "description": "AMM swap fee rate as a bigint string in 18-decimal fixed-point.",
            "example": "1000000000000000"
          },
          "impliedRate": {
            "type": "number",
            "description": "Current AMM implied APR (unitless decimal, `0.085` = 8.5%) derived from `state` and `isPositive`.",
            "example": 0.083
          },
          "totalLp": {
            "type": "string",
            "description": "Total LP supply as a bigint string in 18-decimal LP-token units."
          },
          "totalValue": {
            "type": "string",
            "description": "TVL of the AMM as a bigint string in 18-decimal collateral units."
          },
          "totalSupplyCap": {
            "type": "string",
            "description": "Hard cap on LP supply (bigint string, 18 decimals). Further LP minting is rejected once `totalLp` reaches this value."
          },
          "lpApy": {
            "type": "number",
            "description": "Annualised LP yield as a unitless decimal (`0.12` = 12% APY).",
            "example": 0.12
          },
          "lpPrice": {
            "type": "number",
            "description": "Value of 1 LP token denominated in collateral units. Sourced from the latest periodic AMM snapshot \u2014 `0` until the first snapshot lands.",
            "example": 1.0023
          }
        },
        "required": [
          "marketId",
          "tokenId",
          "ammId",
          "state",
          "isPositive",
          "feeRate",
          "impliedRate",
          "totalLp",
          "totalValue",
          "totalSupplyCap",
          "lpApy",
          "lpPrice"
        ]
      },
      "GetAMMStatesResponse": {
        "type": "object",
        "properties": {
          "results": {
            "description": "AMM states in the order of the requested marketIds",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/GetAMMStateResponse"
            }
          }
        },
        "required": [
          "results"
        ]
      },
      "AssetMetadataResponse": {
        "type": "object",
        "properties": {
          "proSymbol": {
            "type": "string",
            "description": "Asset name used in the pro trading interface",
            "example": "stETH"
          }
        },
        "required": [
          "proSymbol"
        ]
      },
      "AssetItemResponse": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "Internal asset identifier (string). Stable across redeploys; not the same as `tokenId`.",
            "example": "ETH"
          },
          "address": {
            "type": "string",
            "description": "On-chain ERC-20 contract address (lowercased hex).",
            "example": "0x82af49447d8a07e3bd95bd0d56f35241523fbab1"
          },
          "tokenId": {
            "type": "number",
            "description": "Numeric token identifier used together with `marketId` to pack a `marketAcc` (see [Glossary](https://docs.pendle.finance/boros-dev/Backend/glossary)).",
            "example": 2
          },
          "name": {
            "type": "string",
            "description": "Human-readable asset name.",
            "example": "Wrapped Ether"
          },
          "symbol": {
            "type": "string",
            "description": "On-chain token symbol.",
            "example": "WETH"
          },
          "decimals": {
            "type": "number",
            "description": "On-chain ERC-20 decimals. Use to scale raw amounts (e.g. `amount` on calldata endpoints) to/from human units.",
            "example": 18
          },
          "usdPrice": {
            "type": "string",
            "description": "Latest USD spot price as a decimal string. Updated on the backend every few seconds.",
            "example": "3500.25"
          },
          "isCollateral": {
            "type": "boolean",
            "description": "Whether the asset can be used as collateral to back margin positions."
          },
          "metadata": {
            "description": "Display metadata used by the Boros UI.",
            "allOf": [
              {
                "$ref": "#/components/schemas/AssetMetadataResponse"
              }
            ]
          }
        },
        "required": [
          "id",
          "address",
          "tokenId",
          "name",
          "symbol",
          "decimals",
          "usdPrice",
          "isCollateral",
          "metadata"
        ]
      },
      "ListAssetsResponse": {
        "type": "object",
        "properties": {
          "results": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/AssetItemResponse"
            }
          }
        },
        "required": [
          "results"
        ]
      },
      "MarketListItemIMDataResponse": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Market name",
            "example": "stETH 26DEC2025"
          },
          "symbol": {
            "type": "string",
            "description": "Market symbol",
            "example": "stETH-26DEC2025"
          },
          "isIsolatedOnly": {
            "type": "boolean",
            "description": "Whether the market is isolated-only"
          },
          "maturity": {
            "type": "number",
            "description": "Market maturity as unix timestamp",
            "example": 1735689600
          },
          "tickStep": {
            "type": "number",
            "description": "Tick step used to convert ticks to rates"
          },
          "iTickThresh": {
            "type": "number",
            "description": "Isolated-tick threshold"
          },
          "marginFloor": {
            "type": "number",
            "description": "Margin floor of the market"
          }
        },
        "required": [
          "name",
          "symbol",
          "isIsolatedOnly",
          "maturity",
          "tickStep",
          "iTickThresh",
          "marginFloor"
        ]
      },
      "LiqSettingsResponse": {
        "type": "object",
        "properties": {
          "base": {
            "type": "string",
            "description": "Base liquidation incentive factor (bigint string, 18 decimals). See docs: https://docs.pendle.finance/boros-dev/Contracts/CustomTypes#liqsettings"
          },
          "slope": {
            "type": "string",
            "description": "Slope factor that scales liquidation incentive as health ratio decreases (bigint string, 18 decimals)"
          },
          "feeRate": {
            "type": "string",
            "description": "Liquidation fee rate charged to the liquidated account and collected by protocol treasury (bigint string, 18 decimals)"
          }
        },
        "required": [
          "base",
          "slope",
          "feeRate"
        ]
      },
      "MarketListItemConfigResponse": {
        "type": "object",
        "properties": {
          "maxOpenOrders": {
            "type": "number",
            "description": "Maximum number of open orders allowed per account on this market",
            "example": 80
          },
          "markRateOracle": {
            "type": "string",
            "example": "0x0000000000000000000000000000000000000000"
          },
          "fIndexOracle": {
            "type": "string",
            "example": "0xcd1f910d5b2a8b56805b50a06303feb7831ce08c"
          },
          "hardOICap": {
            "type": "string",
            "description": "Hard open interest cap (bigint string) \u2014 new position-increasing orders are rejected.",
            "example": "20000000000000000000000"
          },
          "takerFee": {
            "type": "string",
            "description": "Taker fee (bigint string, 18 decimals)",
            "example": "500000000000000"
          },
          "otcFee": {
            "type": "string",
            "description": "OTC trade fee rate (bigint string)",
            "example": "500000000000000"
          },
          "liqSettings": {
            "$ref": "#/components/schemas/LiqSettingsResponse"
          },
          "kIM": {
            "type": "string",
            "description": "Initial margin factor (bigint string, 18 decimals). Higher values require more margin.",
            "example": "476190476190476190"
          },
          "kMM": {
            "type": "string",
            "description": "Maintenance margin factor (bigint string, 18 decimals). Account is liquidated below this threshold.",
            "example": "333333333333333333"
          },
          "tThresh": {
            "type": "number",
            "example": 604800
          },
          "maxRateDeviationFactorBase1e4": {
            "type": "number",
            "description": "Maximum rate deviation factor in basis points (base 1e4)",
            "example": 1500
          },
          "closingOrderBoundBase1e4": {
            "type": "number",
            "description": "Closing order bound in basis points (base 1e4)",
            "example": 1000
          },
          "loUpperConstBase1e4": {
            "type": "number",
            "description": "Limit order upper constant bound in basis points (base 1e4)",
            "example": 40
          },
          "loUpperSlopeBase1e4": {
            "type": "number",
            "description": "Limit order upper slope bound in basis points (base 1e4)",
            "example": 10667
          },
          "loLowerConstBase1e4": {
            "type": "number",
            "description": "Limit order lower constant bound in basis points (base 1e4)",
            "example": -40
          },
          "loLowerSlopeBase1e4": {
            "type": "number",
            "description": "Limit order lower slope bound in basis points (base 1e4)",
            "example": 9333
          },
          "status": {
            "type": "number",
            "description": "Market status (numeric): `0` = PAUSED (no trading), `1` = CLOSE_ONLY (position-reducing only), `2` = GOOD (normal).",
            "enum": [
              0,
              1,
              2
            ],
            "example": 2
          },
          "useImpliedAsMarkRate": {
            "type": "boolean",
            "description": "Whether to use implied rate as the mark rate instead of oracle rate"
          },
          "softOICap": {
            "type": "number",
            "description": "Soft open interest cap \u2014 triggers close-only mode when exceeded.",
            "example": 9500
          },
          "cloLowerThresh": {
            "type": "number",
            "example": 9300
          },
          "cloUpperThresh": {
            "type": "number",
            "example": 9500
          }
        },
        "required": [
          "maxOpenOrders",
          "markRateOracle",
          "fIndexOracle",
          "hardOICap",
          "takerFee",
          "otcFee",
          "liqSettings",
          "kIM",
          "kMM",
          "tThresh",
          "maxRateDeviationFactorBase1e4",
          "closingOrderBoundBase1e4",
          "loUpperConstBase1e4",
          "loUpperSlopeBase1e4",
          "loLowerConstBase1e4",
          "loLowerSlopeBase1e4",
          "status",
          "useImpliedAsMarkRate"
        ]
      },
      "MarketListItemExtConfigResponse": {
        "type": "object",
        "properties": {
          "ammAddress": {
            "type": "string"
          },
          "ammId": {
            "type": "number",
            "example": 740
          },
          "isPositiveAMM": {
            "type": "boolean"
          },
          "settleFeeRate": {
            "type": "string",
            "example": "1000000000000000"
          },
          "paymentPeriod": {
            "type": "number",
            "example": 3600
          },
          "maxUpdateDelay": {
            "type": "number",
            "example": 900
          }
        },
        "required": [
          "settleFeeRate",
          "paymentPeriod",
          "maxUpdateDelay"
        ]
      },
      "MarketListItemMetadataResponse": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Curated market display name (e.g. \"ETHUSDT\").",
            "example": "ETHUSDT"
          },
          "underlyingSymbol": {
            "type": "string",
            "description": "Underlying asset symbol",
            "example": "stETH"
          },
          "fundingRateSymbol": {
            "type": "string",
            "description": "Funding rate symbol",
            "example": "STETHUSDT"
          },
          "maxLeverage": {
            "type": "number",
            "deprecated": true,
            "description": "DEPRECATED: cosmetic 1/kIM. Will be removed in a future release.",
            "example": 20
          },
          "maxPerpLeverage": {
            "type": "number",
            "description": "Maximum perp leverage of the market",
            "example": 10
          },
          "isUiWhitelisted": {
            "type": "boolean",
            "description": "Whether the market is UI-whitelisted (i.e. not flagged as a dev/test market)",
            "example": true
          }
        },
        "required": [
          "name",
          "maxLeverage",
          "isUiWhitelisted"
        ]
      },
      "MarketListItemDataResponse": {
        "type": "object",
        "properties": {
          "volume24h": {
            "type": "number",
            "description": "24-hour trading volume in YU (yield units, sum of |trade.size| over the rolling 24h window). Multiply by the collateral asset USD price for approximate USD notional.",
            "example": 15000000
          },
          "notionalOI": {
            "type": "number",
            "description": "Current one-sided notional open interest in YU (yield units; raw on-chain `getOI()` value halved to one side).",
            "example": 85000000
          },
          "markApr": {
            "type": "number",
            "description": "Mark APR (fair value rate). `0.085` = 8.5%",
            "example": 0.085
          },
          "lastTradedApr": {
            "type": "number",
            "description": "APR of the most recent executed trade",
            "example": 0.084
          },
          "midApr": {
            "type": "number",
            "description": "Mid-point of best bid and best ask",
            "example": 0.0845
          },
          "bestBid": {
            "type": "number",
            "description": "Best bid APR",
            "example": 0.081
          },
          "bestAsk": {
            "type": "number",
            "description": "Best ask APR",
            "example": 0.088
          },
          "ammImpliedApr": {
            "type": "number",
            "description": "AMM implied APR",
            "example": 0.083
          },
          "floatingApr": {
            "type": "number",
            "description": "Current floating/oracle APR",
            "example": 0.075
          },
          "longYieldApr": {
            "type": "number",
            "description": "Long yield APR (mark APR minus floating APR)",
            "example": 0.09
          },
          "nextSettlementTime": {
            "type": "number",
            "description": "Next settlement time (unix seconds)",
            "example": 1709164800
          },
          "timeToMaturity": {
            "type": "number",
            "description": "Seconds remaining until maturity",
            "example": 86400
          },
          "assetMarkPrice": {
            "type": "number",
            "description": "Current mark price of the underlying asset in USD",
            "example": 3500.25
          },
          "rateSensitivity": {
            "type": "number",
            "description": "PnL per 1 YU per 1% rate move (0 when matured)",
            "example": 0.0049
          },
          "settlementsToMaturity": {
            "type": "number",
            "description": "Number of remaining settlements until maturity (0 when matured)",
            "example": 30
          },
          "dailyVolatility": {
            "type": "number",
            "nullable": true,
            "description": "7-day moving average of daily implied-rate range; partial average while calibrating (<7 days); null only when state is new",
            "example": 0.0012
          },
          "dailyVolatilityState": {
            "type": "string",
            "enum": [
              "new",
              "calibrating",
              "ready"
            ],
            "description": "new: no history; calibrating: <7 days; ready: 7DMA available",
            "example": "ready"
          }
        },
        "required": [
          "volume24h",
          "notionalOI",
          "markApr",
          "lastTradedApr",
          "midApr",
          "floatingApr",
          "assetMarkPrice",
          "rateSensitivity",
          "settlementsToMaturity",
          "dailyVolatility",
          "dailyVolatilityState"
        ]
      },
      "MarketPlatformResponse": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Platform display name",
            "example": "Binance"
          },
          "platformId": {
            "type": "string",
            "description": "Platform identifier",
            "example": "Binance"
          }
        },
        "required": [
          "name",
          "platformId"
        ]
      },
      "MarketListItemResponse": {
        "type": "object",
        "properties": {
          "marketId": {
            "type": "number",
            "description": "Numeric market identifier (1-based).",
            "example": 1
          },
          "address": {
            "type": "string",
            "description": "Market contract address"
          },
          "tokenId": {
            "type": "number",
            "description": "Collateral token ID for this market",
            "example": 1
          },
          "imData": {
            "$ref": "#/components/schemas/MarketListItemIMDataResponse"
          },
          "config": {
            "$ref": "#/components/schemas/MarketListItemConfigResponse"
          },
          "extConfig": {
            "$ref": "#/components/schemas/MarketListItemExtConfigResponse"
          },
          "metadata": {
            "$ref": "#/components/schemas/MarketListItemMetadataResponse"
          },
          "data": {
            "$ref": "#/components/schemas/MarketListItemDataResponse"
          },
          "platform": {
            "description": "Underlying platform metadata",
            "allOf": [
              {
                "$ref": "#/components/schemas/MarketPlatformResponse"
              }
            ]
          }
        },
        "required": [
          "marketId",
          "address",
          "tokenId",
          "imData",
          "config",
          "extConfig",
          "metadata",
          "data",
          "platform"
        ]
      },
      "MarketsSyncStatusResponse": {
        "type": "object",
        "properties": {
          "blockNumber": {
            "type": "number",
            "description": "Latest block number at the time of the query"
          },
          "timestamp": {
            "type": "number",
            "description": "Timestamp of the latest block"
          }
        },
        "required": [
          "blockNumber",
          "timestamp"
        ]
      },
      "ListMarketsResponse": {
        "type": "object",
        "properties": {
          "results": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/MarketListItemResponse"
            }
          },
          "resumeToken": {
            "type": "string",
            "nullable": true,
            "description": "Resume token for the next page. `null` when no more results."
          },
          "syncStatus": {
            "description": "Current sync status",
            "allOf": [
              {
                "$ref": "#/components/schemas/MarketsSyncStatusResponse"
              }
            ]
          }
        },
        "required": [
          "results",
          "syncStatus"
        ]
      },
      "MarketTradeResponse": {
        "type": "object",
        "properties": {
          "size": {
            "type": "number",
            "description": "Signed trade notional size in **YU** (yield units). Positive for long (buy), negative for short (sell). Multiply by the collateral asset USD price for approximate USD notional.",
            "example": 50000
          },
          "rate": {
            "type": "number",
            "description": "Executed implied APR as a unitless decimal (`0.085` = 8.5%).",
            "example": 0.0845
          },
          "txHash": {
            "type": "string",
            "description": "Hash of the on-chain transaction that produced this trade.",
            "example": "0x1234abcd..."
          },
          "blockTimestamp": {
            "type": "number",
            "description": "Block timestamp of the trade (Unix seconds). Multiple trades from the same transaction share this value; ordering within a block follows on-chain event order.",
            "example": 1709164800
          }
        },
        "required": [
          "size",
          "rate",
          "txHash",
          "blockTimestamp"
        ]
      },
      "MarketTradesV2Response": {
        "type": "object",
        "properties": {
          "results": {
            "description": "Trades ordered newest-first by internal `eventIndex` (strictly monotone, not by `blockTimestamp`).",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/MarketTradeResponse"
            }
          },
          "resumeToken": {
            "type": "string",
            "nullable": true,
            "description": "Cursor for the next page. Pass back as `resumeToken` to continue. `null` when no more results."
          }
        },
        "required": [
          "results"
        ]
      },
      "SideTickResponse": {
        "type": "object",
        "properties": {
          "ia": {
            "description": "Implied APR at each aggregated tick level (unitless decimal, `0.085` = 8.5%). For the long side, sorted **descending** (best bid first). For the short side, sorted **ascending** (best ask first). `ia[i]` corresponds 1:1 with `sz[i]`.",
            "example": [
              0.085,
              0.086,
              0.087
            ],
            "type": "array",
            "items": {
              "type": "number"
            }
          },
          "sz": {
            "description": "Total resting notional at the matching `ia[i]` level as a **bigint string** in 18-decimal YU (yield units). Includes AMM contribution at this level when the request used `includeAmm=true`.",
            "example": [
              "1000000000000000000",
              "2000000000000000000",
              "500000000000000000"
            ],
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "required": [
          "ia",
          "sz"
        ]
      },
      "SyncStatusResponse": {
        "type": "object",
        "properties": {
          "blockNumber": {
            "type": "number",
            "description": "Latest fully processed block number"
          },
          "timestamp": {
            "type": "number",
            "description": "Timestamp of the latest fully processed block"
          }
        },
        "required": [
          "blockNumber",
          "timestamp"
        ]
      },
      "OrderBooksResponse": {
        "type": "object",
        "properties": {
          "long": {
            "description": "Bid side \u2014 orders to go long on rates. Best bid first.",
            "allOf": [
              {
                "$ref": "#/components/schemas/SideTickResponse"
              }
            ]
          },
          "short": {
            "description": "Ask side \u2014 orders to go short on rates. Best ask first.",
            "allOf": [
              {
                "$ref": "#/components/schemas/SideTickResponse"
              }
            ]
          },
          "syncStatus": {
            "description": "Current sync status of the order book",
            "allOf": [
              {
                "$ref": "#/components/schemas/SyncStatusResponse"
              }
            ]
          }
        },
        "required": [
          "long",
          "short",
          "syncStatus"
        ]
      },
      "OhlcvCandleResponse": {
        "type": "object",
        "properties": {
          "ts": {
            "type": "number",
            "description": "Bucket start (Unix seconds), aligned to `timeFrame`. Candle covers the half-open interval `[ts, ts + timeFrame)`.",
            "example": 1709164800
          },
          "o": {
            "type": "number",
            "description": "Open APR value for the candlestick period",
            "example": 0.085
          },
          "h": {
            "type": "number",
            "description": "Highest APR value during the candlestick period",
            "example": 0.092
          },
          "l": {
            "type": "number",
            "description": "Lowest APR value during the candlestick period",
            "example": 0.078
          },
          "c": {
            "type": "number",
            "description": "Close APR value for the candlestick period",
            "example": 0.088
          },
          "v": {
            "type": "number",
            "description": "Trading volume in YU (yield units, sum of |trade.size| in the bucket) during the candlestick period",
            "example": 150000.5
          }
        },
        "required": [
          "ts",
          "o",
          "h",
          "l",
          "c",
          "v"
        ]
      },
      "OhlcvChartResponse": {
        "type": "object",
        "properties": {
          "results": {
            "description": "Array of OHLCV candlestick data points for the requested market and time range",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/OhlcvCandleResponse"
            }
          }
        },
        "required": [
          "results"
        ]
      },
      "IndicatorsMetadata": {
        "type": "object",
        "properties": {
          "requested": {
            "description": "The `select` array as requested by the caller (echoed back).",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "available": {
            "description": "Subset of `requested` for which the backend actually returned at least one value within the response window. May be smaller than `requested` for newly-launched markets.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "firstDataTimestamp": {
            "type": "object",
            "description": "Earliest Unix-seconds timestamp at which each indicator has data (regardless of the response window). Useful for \"no data before X\" UI hints.",
            "example": {
              "u": 1700000000,
              "fp": 1700003600,
              "udma": 1700604800
            }
          },
          "uLastSettledTimestamp": {
            "type": "number",
            "description": "Last bucket (Unix seconds) where underlying rate `u` is sourced from settled on-chain historical data. Buckets after this point use real-time funding rates and may revise once settled.",
            "example": 1711152000
          }
        },
        "required": [
          "requested",
          "available"
        ]
      },
      "FGIData": {
        "type": "object",
        "properties": {
          "v": {
            "type": "number",
            "description": "Fear & Greed Index numeric value (`0`..`100`).",
            "example": 65
          },
          "vc": {
            "type": "string",
            "description": "Human-readable classification of `v` (e.g. `\"Fear\"`, `\"Neutral\"`, `\"Greed\"`).",
            "example": "Greed"
          }
        },
        "required": [
          "v",
          "vc"
        ]
      },
      "IndicatorDataPoint": {
        "type": "object",
        "properties": {
          "ts": {
            "type": "number",
            "description": "Bucket start (Unix seconds), aligned to the requested `timeFrame`.",
            "example": 1709164800
          },
          "u": {
            "type": "number",
            "description": "Underlying APR (unitless decimal, `0.085` = 8.5%).",
            "example": 0.075
          },
          "fp": {
            "type": "number",
            "description": "Future premium (mark APR minus underlying APR), unitless decimal.",
            "example": 0.01
          },
          "fgi": {
            "description": "Fear & Greed Index snapshot for this bucket.",
            "allOf": [
              {
                "$ref": "#/components/schemas/FGIData"
              }
            ]
          },
          "udma": {
            "type": "object",
            "description": "Underlying-rate moving averages keyed by period (in days). Only the periods requested via `udma:<periods>` appear; values are unitless decimals.",
            "example": {
              "7": 0.14,
              "30": 0.13
            }
          },
          "ap": {
            "type": "number",
            "description": "Underlying asset spot price (USD).",
            "example": 3500.25
          },
          "pi": {
            "type": "number",
            "description": "Perpetual premium index for the market's funding-rate symbol \u2014 the venue-published (mark - index)/index premium that the underlying funding rate is derived from. Unitless decimal (`0.0004` = 4 bps). Stored as hourly bars and forward-filled onto finer grids.",
            "example": 0.0004
          }
        },
        "required": [
          "ts"
        ]
      },
      "IndicatorsResponse": {
        "type": "object",
        "properties": {
          "metadata": {
            "description": "Response metadata",
            "allOf": [
              {
                "$ref": "#/components/schemas/IndicatorsMetadata"
              }
            ]
          },
          "results": {
            "description": "Indicator data points",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/IndicatorDataPoint"
            }
          }
        },
        "required": [
          "metadata",
          "results"
        ]
      },
      "EncodeMarketAccResponse": {
        "type": "object",
        "properties": {
          "marketAcc": {
            "type": "string",
            "description": "Fully-packed 54-character `marketAcc` (`0x` + 26 bytes). Layout: `root` (20B) \u00b7 `accountId` (1B) \u00b7 `tokenId` (2B) \u00b7 `marketId` (3B). The `marketId` segment is `0xFFFFFF` for a **cross** account, or the actual `marketId` for an **isolated** account.",
            "example": "0xabcdef0123456789abcdef0123456789abcdef01000001ffffff"
          }
        },
        "required": [
          "marketAcc"
        ]
      },
      "DecodeMarketAccResponse": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "User wallet address \u2014 the `root` of the margin account."
          },
          "accountId": {
            "type": "number",
            "description": "Sub-account index under `root` (0\u2013255). `0` is the main sub-account."
          },
          "tokenId": {
            "type": "number",
            "description": "Numeric `tokenId` of the collateral asset."
          },
          "marketId": {
            "type": "number",
            "description": "Market identifier. Decimal equivalent of `0xFFFFFF` (16777215) signals the cross account; any other value is an isolated-market id."
          },
          "isCross": {
            "type": "boolean",
            "description": "Convenience flag \u2014 `true` when `marketId === 0xFFFFFF` (cross account), `false` when isolated."
          }
        },
        "required": [
          "root",
          "accountId",
          "tokenId",
          "marketId",
          "isCross"
        ]
      },
      "LimitOrderMetadataResponse": {
        "type": "object",
        "properties": {
          "isRateImproved": {
            "type": "boolean",
            "description": "`true` if at least one fill executed at a strictly better rate than the order's limit (rate improvement).",
            "example": true
          },
          "isClosePosition": {
            "type": "boolean",
            "description": "Set only for conditional / stop orders \u2014 `true` if the order was placed with the intent to close an existing position (TP/SL). Undefined for plain limit orders.",
            "example": false
          }
        },
        "required": [
          "isRateImproved"
        ]
      },
      "LimitOrderResponseV2": {
        "type": "object",
        "properties": {
          "side": {
            "type": "number",
            "enum": [
              0,
              1
            ],
            "description": "Side { LONG : 0, SHORT : 1 }"
          },
          "placedSize": {
            "type": "string",
            "description": "Original notional size at placement (18-decimal bigint string)."
          },
          "unfilledSize": {
            "type": "string",
            "description": "Remaining unfilled notional size (18-decimal bigint string). Always 0 for filled, cancelled, or expired orders."
          },
          "impliedApr": {
            "type": "number",
            "description": "Fixed APR derived from `tick` (decimal, e.g. `0.05` = 5%). **Deprecated** \u2014 convert `tick` yourself for full precision.",
            "deprecated": true,
            "example": 0.05
          },
          "tick": {
            "type": "number",
            "description": "Integer tick on the discrete rate ladder. `rate = 1.00005^(tick \u00d7 tickStep) - 1` where `tickStep` is per-market. See [glossary](https://docs.pendle.finance/boros-dev/Backend/glossary#tick)."
          },
          "marginRequired": {
            "type": "string",
            "description": "Initial margin currently locked behind the unfilled portion of this order, with leverage applied (18-decimal bigint string). 0 once the order is no longer active."
          },
          "orderId": {
            "type": "string"
          },
          "root": {
            "type": "string"
          },
          "marketId": {
            "type": "number"
          },
          "accountId": {
            "type": "number"
          },
          "isCross": {
            "type": "boolean",
            "description": "Whether the order uses cross-margin mode"
          },
          "status": {
            "type": "number",
            "enum": [
              0,
              1,
              2,
              3,
              4
            ],
            "description": "Lifecycle status. { Filling : 0, Cancelled : 1, FullyFilled : 2, Expired : 3, Purged : 4 }"
          },
          "orderType": {
            "type": "number",
            "enum": [
              0,
              1
            ],
            "description": "Order kind. Time-In-Force is a separate concept set at order placement. { LIMIT : 0, MARKET : 1 }"
          },
          "blockTimestamp": {
            "type": "number",
            "description": "Block timestamp of the **last** order update in **Unix seconds (UTC)**."
          },
          "eventIndex": {
            "type": "number",
            "description": "Composite cursor of the **last update** to this order \u2014 packs block number and intra-block log index. Used as the cursor by `/v1/accounts/orders`. Mutates whenever the order is filled, cancelled, etc."
          },
          "placedEventIndex": {
            "type": "number",
            "description": "Composite cursor of the **placement** event \u2014 immutable. Used as the cursor by `/v1/accounts/orders-by-placed-time`."
          },
          "placedTimestamp": {
            "type": "number",
            "description": "Block timestamp of the placement event in **Unix seconds (UTC)**."
          },
          "placedTxHash": {
            "type": "string",
            "description": "Transaction hash of the original placement event"
          },
          "marketAcc": {
            "type": "string",
            "description": "Order owner `marketAcc` (54-char hex). Cross orders embed `0xFFFFFF`; isolated orders embed the actual `marketId`. Decode via `/v1/market-acc/decode`.",
            "example": "0x9dcf85824e024fea9e3ef583dccbea68edbc37b8000002ffffff"
          },
          "metadata": {
            "$ref": "#/components/schemas/LimitOrderMetadataResponse"
          }
        },
        "required": [
          "side",
          "placedSize",
          "unfilledSize",
          "impliedApr",
          "tick",
          "marginRequired",
          "orderId",
          "root",
          "marketId",
          "accountId",
          "isCross",
          "status",
          "orderType",
          "blockTimestamp",
          "eventIndex",
          "placedEventIndex",
          "placedTimestamp",
          "placedTxHash",
          "marketAcc"
        ]
      },
      "OrdersByTxHashResponse": {
        "type": "object",
        "properties": {
          "results": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/LimitOrderResponseV2"
            }
          }
        },
        "required": [
          "results"
        ]
      },
      "DepositBodyDto": {
        "type": "object",
        "properties": {
          "marketAcc": {
            "type": "string",
            "description": "Target account identifier (54-char hex). Packs `root + accountId + tokenId + marketId`, where `root` is the user wallet address. For a cross deposit, the embedded `marketId` segment is `0xFFFFFF`; for an isolated deposit, it is the actual market id. See [Glossary](https://docs.pendle.finance/boros-dev/Backend/glossary)."
          },
          "amount": {
            "type": "string",
            "description": "BigInt string of the deposit amount, **in the depositing token's native decimals** (e.g. `\"1000000\"` is `1 USDC` for a 6-decimals token). Use `/open-api-v2/v1/assets` to look up each token's `decimals`."
          }
        },
        "required": [
          "marketAcc",
          "amount"
        ]
      },
      "CalldataBuilderUserResponse": {
        "type": "object",
        "properties": {
          "calldata": {
            "type": "string",
            "description": "ABI-encoded transaction calldata to be included in the transaction data field.",
            "example": "0xa9059cbb000000000000000000000000..."
          },
          "from": {
            "type": "string",
            "description": "The sender address that should sign and submit this transaction (echoes the request's user address)."
          },
          "to": {
            "type": "string",
            "description": "The target contract address to send this transaction to."
          },
          "gas": {
            "type": "string",
            "description": "Estimated gas limit for the transaction as a decimal string. Omitted if the backend could not estimate gas (e.g. the user has not yet granted the ERC20 allowance the tx needs); clients should re-estimate in that case."
          }
        },
        "required": [
          "calldata",
          "from",
          "to"
        ]
      },
      "RequestWithdrawalBodyDto": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Withdrawer wallet address (42-char hex). Aliased as `root` elsewhere in the API \u2014 this is the user's wallet."
          },
          "tokenId": {
            "type": "number",
            "minimum": 1,
            "description": "Collateral token ID to withdraw."
          },
          "amount": {
            "type": "string",
            "description": "BigInt string of the requested withdrawal amount, **in the withdrawing token's native decimals** (e.g. `\"1000000\"` is `1 USDC` for a 6-decimals token). Look up `decimals` from `/open-api-v2/v1/assets`. Withdrawals are only ever made from the **cross** account \u2014 isolated balances must be moved via `cash-transfer` first."
          }
        },
        "required": [
          "root",
          "tokenId",
          "amount"
        ]
      },
      "CancelWithdrawalBodyDto": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Withdrawer wallet address (42-char hex) \u2014 the `root`."
          },
          "tokenId": {
            "type": "number",
            "minimum": 1,
            "description": "Token ID whose pending withdrawal request should be cancelled."
          }
        },
        "required": [
          "root",
          "tokenId"
        ]
      },
      "ApproveAgentBodyDto": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "User wallet address (42-char hex). This is the `root` of the margin account being delegated."
          },
          "accountId": {
            "type": "number",
            "minimum": 0,
            "maximum": 255,
            "default": 0,
            "description": "Sub-account index (0-255) whose trading the agent may sign for. Defaults to 0 (the main account)."
          },
          "agentAddress": {
            "type": "string",
            "description": "Address the agent will sign from."
          },
          "expiry": {
            "type": "number",
            "example": 1735689600,
            "description": "Unix seconds \u2014 approval auto-expires at this time. Must be in the future."
          }
        },
        "required": [
          "root",
          "agentAddress",
          "expiry"
        ]
      },
      "RevokeAgentBodyDto": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "User wallet address (42-char hex) \u2014 the `root`."
          },
          "accountId": {
            "type": "number",
            "minimum": 0,
            "maximum": 255,
            "default": 0,
            "description": "Sub-account index (0-255). Defaults to 0 (the main account)."
          },
          "agentAddress": {
            "type": "string",
            "description": "Address of the agent whose approval is being revoked."
          }
        },
        "required": [
          "root",
          "agentAddress"
        ]
      },
      "VaultPayTreasuryBodyDto": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "User (or vault) wallet address (42-char hex) \u2014 the `root`."
          },
          "tokenId": {
            "type": "number",
            "minimum": 1,
            "description": "Collateral token ID."
          },
          "amount": {
            "type": "string",
            "description": "BigInt string of the treasury payment amount, **in the paying token's native decimals** (e.g. `\"1000000\"` is `1 USDC` for a 6-decimals token). Look up `decimals` from `/open-api-v2/v1/assets`."
          }
        },
        "required": [
          "root",
          "tokenId",
          "amount"
        ]
      },
      "PlaceOrderSimpleBodyDto": {
        "type": "object",
        "properties": {
          "marketAcc": {
            "type": "string",
            "description": "Fully-packed 54-character `marketAcc` (`0x` + 26 bytes). Layout: `root` (20B) \u00b7 `accountId` (1B) \u00b7 `tokenId` (2B) \u00b7 `marketId` (3B). The `marketId` segment is `0xFFFFFF` for a **cross** account, or the actual `marketId` for an **isolated** account."
          },
          "marketId": {
            "type": "number",
            "minimum": 1,
            "description": "Market ID. Must match the `marketId` segment of `marketAcc` (or equal `0xFFFFFF` for cross)."
          },
          "side": {
            "type": "number",
            "enum": [
              0,
              1
            ],
            "description": "Order side. `0` = LONG, `1` = SHORT."
          },
          "size": {
            "type": "string",
            "description": "BigInt string of the notional size, scaled by 10^18."
          },
          "tif": {
            "type": "number",
            "enum": [
              0,
              1,
              2,
              3,
              4
            ],
            "description": "Time-in-force (numeric). `0` = GTC, `1` = IOC, `2` = FOK, `3` = ALO, `4` = SOFT_ALO. `GTC` / `ALO` / `SOFT_ALO` \u2192 limit (rests unmatched remainder); `IOC` / `FOK` \u2192 market (no resting)."
          },
          "rate": {
            "type": "number",
            "description": "Human-readable rate (e.g. `0.085` = 8.5%). Required for `GTC` / `ALO` / `SOFT_ALO` and for `IOC`; optional only for `FOK` (then matches at the side's extreme tick). Backend rounds to the nearest tick: LONG rounds **down**, SHORT rounds **up** (always safer for the user)."
          },
          "slippage": {
            "type": "number",
            "description": "Relative tolerance from current mid-rate (e.g. `0.005` = 0.5%). Backend computes the execution guard as `desiredRate = midRate \u00b1 slippage`. For limit orders this is a safety net; for `FOK` market orders it is strongly recommended."
          },
          "ammId": {
            "type": "number",
            "description": "AMM routing. `0` \u2192 orderbook-only (disables AMM matching); a specific AMM id \u2192 also route through that AMM. Optional \u2014 if omitted, the builder uses the market's active `ammId` from `/v1/markets`.",
            "minimum": 0
          },
          "preCancelOrderId": {
            "type": "string",
            "description": "Optional. Order id of an existing resting order to atomically cancel before this order is matched. See POST /place-orders for the full semantics.",
            "example": "12345"
          }
        },
        "required": [
          "marketAcc",
          "marketId",
          "side",
          "size",
          "tif"
        ]
      },
      "PlaceOrderResolved": {
        "type": "object",
        "properties": {
          "side": {
            "type": "number",
            "enum": [
              0,
              1
            ],
            "description": "Order side (numeric): `0` = LONG, `1` = SHORT. Makes the tick-rounding direction explicit."
          },
          "limitTick": {
            "type": "number",
            "nullable": true,
            "description": "Tick actually encoded into the calldata. `null` for unguarded `FOK` market orders."
          },
          "actualRate": {
            "type": "number",
            "nullable": true,
            "description": "Rate corresponding to `limitTick`. May differ from `requestedRate` by a few bp due to tick snapping. `null` when `limitTick` is `null`."
          },
          "requestedRate": {
            "type": "number",
            "description": "Only present when the client sent `rate` instead of `limitTick` \u2014 echoes the requested rate."
          },
          "desiredRate": {
            "type": "number",
            "description": "Only present when the client sent `desiredRate` / `slippage` (or the backend auto-set one). Mirrors what the contract will use as the execution ceiling."
          }
        },
        "required": [
          "side",
          "limitTick",
          "actualRate"
        ]
      },
      "PlaceOrderCall": {
        "type": "object",
        "properties": {
          "calldata": {
            "type": "string",
            "description": "ABI-encoded calldata for the agent to execute on the target contract."
          },
          "accountId": {
            "type": "number",
            "description": "The account ID this calldata targets. The agent uses this to route the call to the correct margin account."
          },
          "resolved": {
            "description": "Present for every entry that accepts user-friendly alternatives (`rate`/`slippage`). Echoes what the backend actually encoded so the UI can show the user before they sign.",
            "allOf": [
              {
                "$ref": "#/components/schemas/PlaceOrderResolved"
              }
            ]
          }
        },
        "required": [
          "calldata",
          "accountId"
        ]
      },
      "PlaceOrdersResponse": {
        "type": "object",
        "properties": {
          "calls": {
            "description": "One entry per order in the request. Submit them via `POST /agent/send-transactions`.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PlaceOrderCall"
            }
          }
        },
        "required": [
          "calls"
        ]
      },
      "PlaceSingleOrderBodyDto": {
        "type": "object",
        "properties": {
          "marketAcc": {
            "type": "string",
            "description": "Fully-packed 54-character `marketAcc` (`0x` + 26 bytes). Layout: `root` (20B) \u00b7 `accountId` (1B) \u00b7 `tokenId` (2B) \u00b7 `marketId` (3B). The `marketId` segment is `0xFFFFFF` for a **cross** account, or the actual `marketId` for an **isolated** account."
          },
          "marketId": {
            "type": "number",
            "minimum": 1,
            "description": "Market ID. Must match the `marketId` segment of `marketAcc` (or equal `0xFFFFFF` for cross)."
          },
          "side": {
            "type": "number",
            "enum": [
              0,
              1
            ],
            "description": "Order side. `0` = LONG, `1` = SHORT."
          },
          "size": {
            "type": "string",
            "description": "BigInt string of the notional size, scaled by 10^18."
          },
          "tif": {
            "type": "number",
            "enum": [
              0,
              1,
              2,
              3,
              4
            ],
            "description": "Time-in-force (numeric). `0` = GTC, `1` = IOC, `2` = FOK, `3` = ALO, `4` = SOFT_ALO. `GTC` / `ALO` / `SOFT_ALO` \u2192 resting limit; `IOC` and `FOK` do not rest. `GTC` / `ALO` / `SOFT_ALO` and `IOC` require one of `rate` / `limitTick`; only `FOK` may omit both (then matches at the side's extreme tick)."
          },
          "limitTick": {
            "type": "number",
            "description": "Raw contract tick index (market-specific `tickStep`). Mutually exclusive with `rate`. For `GTC` / `ALO` / `SOFT_ALO` and `IOC`, one of `limitTick` / `rate` is required; for `FOK` both are optional (acts as a per-tick guard if set, otherwise the contract uses the side's extreme tick). See [Order Book mechanics](https://docs.pendle.finance/boros-dev/Mechanics/OrderBook) for tick \u2194 rate conversion.",
            "minimum": -32768,
            "maximum": 32767
          },
          "rate": {
            "type": "number",
            "description": "Human-readable rate (e.g. `0.085` = 8.5%). Backend converts to the nearest tick: LONG rounds **down**, SHORT rounds **up** (always safer for the user). Mutually exclusive with `limitTick`."
          },
          "desiredRate": {
            "type": "number",
            "description": "Absolute average-execution ceiling. The contract reverts with `TradeUndesiredRate` if `signedCost > signedSize * desiredRate`. Mutually exclusive with `slippage` \u2014 exactly one of the two must be supplied.",
            "minimum": -1000,
            "maximum": 1000
          },
          "slippage": {
            "type": "number",
            "description": "Relative tolerance from current mid-rate (e.g. `0.005` = 0.5%). Backend computes `desiredRate = midRate \u00b1 slippage`. Mutually exclusive with `desiredRate` \u2014 exactly one of the two must be supplied."
          },
          "ammId": {
            "type": "number",
            "description": "AMM routing. `0` \u2192 orderbook-only (disables AMM matching); a specific AMM id \u2192 also route through that AMM. Optional \u2014 if omitted, the builder uses the market's active `ammId` from `/v1/markets`.",
            "minimum": 0
          },
          "preCancelOrderId": {
            "type": "string",
            "description": "Optional. Order id of an existing resting order to **atomically cancel before this order is matched/placed**. Runs as Phase 2 of the contract's place-and-match sequence, so the two actions land in a single transaction with no race window.\n\n**Strict mode only.** The entire transaction reverts if the cancel cannot be honored (unknown / already cancelled / already filled). Intended for \"cancel-this-specific-resting-order-and-place-a-replacement\" flows.",
            "example": "12345"
          }
        },
        "required": [
          "marketAcc",
          "marketId",
          "side",
          "size",
          "tif"
        ]
      },
      "BulkOrdersCancelDataDto": {
        "type": "object",
        "properties": {
          "ids": {
            "description": "Order ids to cancel as BigInt strings. Ignored when `isAll` is `true`.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "isAll": {
            "type": "boolean",
            "description": "If `true`, cancel every resting order on this market for this account; `ids` is ignored."
          },
          "isStrict": {
            "type": "boolean",
            "description": "If `true`, the **entire `bulkOrders` transaction reverts** when any cancel id cannot be honored (unknown / already cancelled / already filled). If `false`, unhonorable ids are skipped silently and the placement still proceeds. Use `true` for cancel-and-replace flows where placing without the cancel would be unsafe."
          }
        },
        "required": [
          "ids",
          "isAll",
          "isStrict"
        ]
      },
      "BulkOrdersLongShortDataDto": {
        "type": "object",
        "properties": {
          "sizes": {
            "description": "BigInt strings of notional sizes, scaled by 10^18. One per sub-order.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "limitTicks": {
            "description": "Raw contract tick indices (signed int16), one per sub-order. Must be the same length as `sizes`. See [Order Book mechanics](https://docs.pendle.finance/boros-dev/Mechanics/OrderBook) for tick \u2194 rate conversion.",
            "type": "array",
            "items": {
              "type": "number"
            }
          },
          "tif": {
            "type": "number",
            "enum": [
              0,
              1,
              2,
              3,
              4
            ],
            "description": "Time-in-force shared by every sub-order in this entry. `0` = GTC, `1` = IOC, `2` = FOK, `3` = ALO, `4` = SOFT_ALO."
          },
          "side": {
            "type": "number",
            "enum": [
              0,
              1
            ],
            "description": "Order side shared by every sub-order. `0` = LONG, `1` = SHORT."
          }
        },
        "required": [
          "sizes",
          "limitTicks",
          "tif",
          "side"
        ]
      },
      "PlaceBulkOrdersEntryDto": {
        "type": "object",
        "properties": {
          "marketId": {
            "type": "number",
            "minimum": 1,
            "description": "Market ID this entry targets."
          },
          "cancelData": {
            "description": "Resting orders to cancel **atomically before this entry is matched**, in the same on-chain `bulkOrders` call. Pass `{ ids: [], isAll: false, isStrict: false }` for the no-op case (place without cancelling). Use `isAll: true` to wipe the book for this `(marketAcc, marketId)` before placing. Set `isStrict: true` to make the entire transaction revert if any id cannot be honored.",
            "allOf": [
              {
                "$ref": "#/components/schemas/BulkOrdersCancelDataDto"
              }
            ]
          },
          "orders": {
            "description": "Sub-orders to place on this market (shared `side`/`tif`, per-order `size`/`limitTick`).",
            "allOf": [
              {
                "$ref": "#/components/schemas/BulkOrdersLongShortDataDto"
              }
            ]
          },
          "desiredRate": {
            "type": "number",
            "description": "Absolute average-execution ceiling. The contract reverts with `TradeUndesiredRate` if `signedCost > signedSize * desiredRate`. Mutually exclusive with `slippage` \u2014 exactly one of the two must be supplied.",
            "minimum": -1000,
            "maximum": 1000
          },
          "slippage": {
            "type": "number",
            "description": "Relative tolerance from this market's current mid-rate (e.g. `0.005` = 0.5%). Backend computes `desiredRate = midRate \u00b1 slippage`. Mutually exclusive with `desiredRate` \u2014 exactly one of the two must be supplied."
          }
        },
        "required": [
          "marketId",
          "cancelData",
          "orders"
        ]
      },
      "PlaceBulkOrdersDto": {
        "type": "object",
        "properties": {
          "accountId": {
            "type": "number",
            "minimum": 0,
            "maximum": 255,
            "default": 0,
            "description": "Sub-account index (0-255) all entries execute under. Defaults to 0 (main account)."
          },
          "cross": {
            "type": "boolean",
            "description": "`true` \u2192 all `bulks[]` execute against the user's **cross** account for the relevant `(root, accountId, tokenId)`. `false` \u2192 execute against the **isolated** account on each entry's `marketId`. Mixing cross + isolated within a single `bulkOrders` call is not supported \u2014 split into two `orderRequests[]` entries instead."
          },
          "bulks": {
            "description": "Per-market entries packed into a single on-chain `bulkOrders` call.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PlaceBulkOrdersEntryDto"
            }
          }
        },
        "required": [
          "cross",
          "bulks"
        ]
      },
      "OrderRequestDto": {
        "type": "object",
        "properties": {
          "singleOrder": {
            "description": "Single-order entry. Mutually exclusive with `bulkOrders`.",
            "allOf": [
              {
                "$ref": "#/components/schemas/PlaceSingleOrderBodyDto"
              }
            ]
          },
          "bulkOrders": {
            "description": "Multi-market `bulkOrders` entry. Mutually exclusive with `singleOrder`.",
            "allOf": [
              {
                "$ref": "#/components/schemas/PlaceBulkOrdersDto"
              }
            ]
          }
        }
      },
      "PlaceOrdersBodyDto": {
        "type": "object",
        "properties": {
          "orderRequests": {
            "description": "**Advanced / liquidity-provisioning** batch. Each entry is either a `singleOrder` (one `placeSingleOrder` call) or a `bulkOrders` (one `bulkOrders` multicall covering many per-market sub-orders). The backend emits one on-chain call per entry, preserving request order.\n\nBuilt for **market-making / liquidity-provisioning flows** that need to place many resting orders across tick levels and/or markets at once \u2014 not for the common single-order UI case (use `POST /place-order` for that).\n\n**Constraints.** All entries must execute under the same `(root, accountId)` \u2014 one agent signature covers the whole multicall. Each `singleOrder` and each `bulkOrders.bulks[i]` must supply exactly one of `desiredRate` / `slippage`.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/OrderRequestDto"
            }
          }
        },
        "required": [
          "orderRequests"
        ]
      },
      "CancelOrdersMarketDto": {
        "type": "object",
        "properties": {
          "marketAcc": {
            "type": "string",
            "description": "Market account identifier (54-char hex). All entries in one request should unpack to the same `(root, accountId)` \u2014 the agent submits one signature for all of them."
          },
          "marketId": {
            "type": "number",
            "minimum": 1,
            "description": "Market ID. Must match the `marketId` segment of `marketAcc` (or equal `0xFFFFFF` for cross)."
          },
          "cancelAll": {
            "type": "boolean",
            "description": "If `true`, cancel every resting order in this market for this account; `orderIds` is ignored."
          },
          "orderIds": {
            "description": "Specific order ids to cancel. Required when `cancelAll: false`. Cancels are best-effort at the entry level: an id already filled or cancelled is skipped without reverting the other entries.",
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "required": [
          "marketAcc",
          "marketId",
          "cancelAll"
        ]
      },
      "CancelOrdersBodyDto": {
        "type": "object",
        "properties": {
          "markets": {
            "description": "Per-market cancel requests. One on-chain `bulkCancels` call per entry.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CancelOrdersMarketDto"
            }
          }
        },
        "required": [
          "markets"
        ]
      },
      "AgentCall": {
        "type": "object",
        "properties": {
          "calldata": {
            "type": "string",
            "description": "ABI-encoded calldata for the agent to execute on the target contract."
          },
          "accountId": {
            "type": "number",
            "description": "The account ID this calldata targets."
          }
        },
        "required": [
          "calldata",
          "accountId"
        ]
      },
      "AgentCalldataResponse": {
        "type": "object",
        "properties": {
          "calls": {
            "description": "One entry per on-chain call this request produces. Submit them via `POST /agent/send-transactions`.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/AgentCall"
            }
          }
        },
        "required": [
          "calls"
        ]
      },
      "CashTransferBodyDto": {
        "type": "object",
        "properties": {
          "accountId": {
            "type": "number",
            "minimum": 0,
            "maximum": 255,
            "default": 0,
            "description": "Sub-account index (0-255) performing the transfer. Defaults to 0 \u2014 i.e. the user's main account."
          },
          "marketId": {
            "type": "number",
            "minimum": 1,
            "description": "Market ID of the **isolated** leg. The corresponding cross leg is implied \u2014 there is exactly one cross account per `(root, accountId, tokenId)` pair, derived from the market's `tokenId`."
          },
          "direction": {
            "type": "string",
            "enum": [
              "CROSS_TO_ISOLATED",
              "ISOLATED_TO_CROSS"
            ],
            "description": "`CROSS_TO_ISOLATED` \u2014 cash moves from the cross account into the isolated market. `ISOLATED_TO_CROSS` \u2014 reverse."
          },
          "amount": {
            "type": "string",
            "description": "BigInt string of the transfer amount, scaled by 10^18. Always positive \u2014 direction is explicit via `direction`."
          }
        },
        "required": [
          "marketId",
          "direction",
          "amount"
        ]
      },
      "EnterExitMarketsBodyDto": {
        "type": "object",
        "properties": {
          "accountId": {
            "type": "number",
            "minimum": 0,
            "maximum": 255,
            "default": 0,
            "description": "Sub-account index (0-255) this enter/exit targets. Backend echoes it into `executeParams[].accountId`. Defaults to 0."
          },
          "isCross": {
            "type": "boolean",
            "default": true,
            "description": "Which account enters/exits. `true` \u2192 the user's **cross** account (default; can target multiple markets in one call). `false` \u2192 the user's **isolated** account; send exactly one `marketId` \u2014 an isolated account is pinned to a single market."
          },
          "marketIds": {
            "description": "Market IDs to enter or exit. With `isCross: false` the array must have length 1 (isolated accounts are pinned to a single market).",
            "type": "array",
            "items": {
              "type": "number"
            }
          }
        },
        "required": [
          "marketIds"
        ]
      },
      "PayTreasuryBodyDto": {
        "type": "object",
        "properties": {
          "accountId": {
            "type": "number",
            "minimum": 0,
            "maximum": 255,
            "default": 0,
            "description": "Sub-account index (0-255) paying the treasury fee. Defaults to 0."
          },
          "isCross": {
            "type": "boolean",
            "description": "Which account pays the fee. `true` \u2192 deduct from the user's **cross** account. `false` \u2192 deduct from the user's **isolated** account on this market."
          },
          "marketId": {
            "type": "number",
            "minimum": 1,
            "description": "Market ID whose treasury receives the payment. Fees accrue per-market; this identifies which market's treasury is being settled."
          },
          "amount": {
            "type": "string",
            "description": "BigInt string of the treasury payment amount, **in the paying token's native decimals** (e.g. `\"1000000\"` is `1 USDC` for a 6-decimals token). Look up `decimals` from `/open-api-v2/v1/assets`. After the tx executes on-chain, the backend credits the equivalent USD value to the user's **off-chain gas budget** that funds subsequent agent-signed transactions."
          }
        },
        "required": [
          "isCross",
          "marketId",
          "amount"
        ]
      },
      "AddLiquidityToAmmAgentBodyDto": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "User wallet address \u2014 the `root` of the margin account providing the liquidity. Used to resolve the AMM sub-account (`root` \u00b7 accountId 255 \u00b7 `tokenId` \u00b7 `marketId`) so the response can set `enterMarket` correctly and include the one-time `marketEntranceFee` in the `ammCashTransfer` leg's `cashIn`."
          },
          "marketId": {
            "type": "number",
            "minimum": 1,
            "description": "Market ID of the AMM pool to deposit cash into."
          },
          "netCashIn": {
            "type": "string",
            "description": "BigInt string of net cash to add as single-sided liquidity."
          },
          "minLpOut": {
            "type": "string",
            "description": "BigInt string of minimum LP tokens to receive."
          }
        },
        "required": [
          "root",
          "marketId",
          "netCashIn",
          "minLpOut"
        ]
      },
      "AgentExecuteParams": {
        "type": "object",
        "properties": {
          "calldata": {
            "type": "string",
            "description": "ABI-encoded calldata for the agent to execute on the target contract",
            "example": "0xa9059cbb000000000000000000000000..."
          },
          "accountId": {
            "type": "number",
            "description": "The account ID that this calldata should be executed against. The agent uses this to route the call to the correct margin account."
          }
        },
        "required": [
          "calldata",
          "accountId"
        ]
      },
      "BulkAgentExecuteParamsResponseV2": {
        "type": "object",
        "properties": {
          "executeParams": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/AgentExecuteParams"
            }
          }
        },
        "required": [
          "executeParams"
        ]
      },
      "RemoveLiquidityFromAmmAgentBodyDto": {
        "type": "object",
        "properties": {
          "marketId": {
            "type": "number",
            "minimum": 1,
            "description": "Market ID of the AMM pool to burn LP tokens from."
          },
          "lpToRemove": {
            "type": "string",
            "description": "BigInt string of LP tokens to burn."
          },
          "minCashOut": {
            "type": "string",
            "description": "BigInt string of minimum cash to receive."
          }
        },
        "required": [
          "marketId",
          "lpToRemove",
          "minCashOut"
        ]
      },
      "FundingRateSymbolResponse": {
        "type": "object",
        "properties": {
          "fundingRateSymbol": {
            "type": "string",
            "description": "Venue-specific instrument identifier. Two shapes are in use: Binance rows carry the native uppercase ticker (`BTCUSDT`, `ETHUSDT`), while every other venue uses a synthetic lowercase `<venue>-<asset>` key (`okx-btc`, `hyperliquid-eth`). It is NOT always the symbol as listed by the source exchange \u2014 OKX lists `BTC-USDT-SWAP`, which appears here only as `strategyMetadata.name`. Note Hyperliquid uses `-gold`/`-silver` where other venues use `-xau`/`-xag`, so join on this field rather than on `assetSymbol`.",
            "example": "okx-btc"
          },
          "assetSymbol": {
            "type": "string",
            "description": "Canonical asset symbol (venue-agnostic).",
            "example": "BTC"
          },
          "exchange": {
            "type": "string",
            "description": "Source venue this funding rate is sourced from. Capitalised as displayed, e.g. `Binance`, `OKX`, `Hyperliquid`.",
            "example": "Binance"
          }
        },
        "required": [
          "fundingRateSymbol",
          "assetSymbol",
          "exchange"
        ]
      },
      "FundingRateSymbolListResponse": {
        "type": "object",
        "properties": {
          "fundingRateSymbols": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FundingRateSymbolResponse"
            }
          }
        },
        "required": [
          "fundingRateSymbols"
        ]
      },
      "GetSettlementSummaryByMarketQueryDto": {
        "type": "object",
        "properties": {
          "marketIds": {
            "description": "Filter to a specific subset of markets (max 100). Omit to query all whitelisted markets \u2014 keep in mind this scales the CU cost linearly.",
            "type": "array",
            "items": {
              "type": "number"
            }
          },
          "fromTimestamp": {
            "type": "number",
            "description": "Inclusive lower bound of the settlement period (Unix seconds, UTC). Compared against `periodTimestamp` (the period start, aligned to the market's configured `settlementInterval` \u2014 e.g. 1h / 2h / 8h), not block time.",
            "example": 1735689600
          },
          "toTimestamp": {
            "type": "number",
            "description": "Inclusive upper bound of the settlement period (Unix seconds, UTC).",
            "example": 1735776000
          }
        },
        "required": [
          "fromTimestamp",
          "toTimestamp"
        ]
      },
      "SettlementMarketSummaryResponse": {
        "type": "object",
        "properties": {
          "marketId": {
            "type": "number"
          },
          "marketName": {
            "type": "string",
            "example": "BTCUSDT"
          },
          "fundingRateSymbol": {
            "type": "string",
            "description": "Source funding rate symbol the market settles against (see `/v1/funding-rate/all-funding-rate-symbols`).",
            "example": "BTCUSDT"
          },
          "periodTimestamp": {
            "type": "number",
            "description": "Start of the settlement period (Unix seconds, UTC). Period length matches the market's configured `settlementInterval` (e.g. 1h / 2h / 8h); the period covers `[periodTimestamp, periodTimestamp + settlementInterval)`."
          },
          "eventIndex": {
            "type": "number",
            "description": "Composite ordering key for the settlement event: `blockNumber * 1_000_000 + logIndex`. Stable, unique, monotonic across events."
          },
          "blockTimestamp": {
            "type": "number",
            "description": "Unix seconds when the settlement was emitted on-chain. Usually equals `periodTimestamp` plus a small lag from the keeper that triggers the settlement."
          },
          "settlementApr": {
            "type": "number",
            "description": "Realized funding rate for the period, expressed as an annualized rate (1.0 = 100% APR). Multiply by `settlementInterval / SECONDS_PER_YEAR` (per-market \u2014 e.g. `1/8760` only for 1h markets, `2/8760` for 2h, `8/8760` for 8h) to convert back to the per-period funding rate.",
            "example": 0.085
          },
          "totalFee": {
            "type": "number",
            "description": "Total settlement-fee revenue collected in this period, denominated in the market collateral token.",
            "example": 125.5
          },
          "totalNotionalSize": {
            "type": "number",
            "description": "One-sided open interest (notional, in YU) that participated in this settlement. Returned as the absolute long-side notional (`raw_notional / 2`), so it equals long OI = short OI for the period.",
            "example": 500000
          },
          "totalSettledValue": {
            "type": "number",
            "description": "Net settled value transferred from one side to the other this period, denominated in the market collateral token. Already halved (`raw / 2`) to express the magnitude transferred (positive number).",
            "example": 1250.75
          }
        },
        "required": [
          "marketId",
          "marketName",
          "fundingRateSymbol",
          "periodTimestamp",
          "eventIndex",
          "blockTimestamp",
          "settlementApr",
          "totalFee",
          "totalNotionalSize",
          "totalSettledValue"
        ]
      },
      "SettlementMarketSummaryListResponse": {
        "type": "object",
        "properties": {
          "settlementMarketSummaries": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SettlementMarketSummaryResponse"
            }
          }
        },
        "required": [
          "settlementMarketSummaries"
        ]
      },
      "LiquidateMarketAccPositionResponse": {
        "type": "object",
        "properties": {
          "marketAcc": {
            "type": "string",
            "description": "Fully-packed 54-character `marketAcc` (`0x` + 26 bytes). Layout: `root` (20B) \u00b7 `accountId` (1B) \u00b7 `tokenId` (2B) \u00b7 `marketId` (3B). The `marketId` segment is `0xFFFFFF` for a **cross** account, or the actual `marketId` for an **isolated** account."
          },
          "prevPositionRate": {
            "type": "number",
            "description": "Weighted-average implied APR of the position **before** the liquidation trade (1.0 = 100%).",
            "example": 0.085
          },
          "prevPositionSize": {
            "type": "number",
            "description": "Signed notional size of the position **before** liquidation (positive = long YU, negative = short YU).",
            "example": 50000
          },
          "postPositionRate": {
            "type": "number",
            "description": "Weighted-average implied APR of the position **after** liquidation (1.0 = 100%).",
            "example": 0.082
          },
          "postPositionSize": {
            "type": "number",
            "description": "Signed notional size of the position **after** liquidation.",
            "example": 25000
          }
        },
        "required": [
          "marketAcc",
          "prevPositionRate",
          "prevPositionSize",
          "postPositionRate",
          "postPositionSize"
        ]
      },
      "LiquidationEventResponse": {
        "type": "object",
        "properties": {
          "marketId": {
            "type": "number"
          },
          "blockNumber": {
            "type": "number",
            "description": "The block number when the liquidation occurred"
          },
          "timestamp": {
            "type": "number",
            "description": "Block timestamp of the liquidation (Unix seconds, UTC)."
          },
          "txHash": {
            "type": "string"
          },
          "size": {
            "type": "number",
            "description": "Notional size of the liquidation trade (in YU). **Signed int128** as parsed from the on-chain trade payload \u2014 sign indicates the trade direction; magnitude is the absolute notional.",
            "example": 50000
          },
          "rate": {
            "type": "number",
            "description": "Mark implied APR of the market at the moment of liquidation (1.0 = 100%).",
            "example": 0.085
          },
          "tradeRate": {
            "type": "number",
            "description": "Implied APR at which the liquidation trade actually executed (typically off-mark to incentivize the liquidator).",
            "example": 0.083
          },
          "violator": {
            "description": "Position snapshot of the **violator** (the liquidated account) \u2014 pre and post the liquidation trade.",
            "allOf": [
              {
                "$ref": "#/components/schemas/LiquidateMarketAccPositionResponse"
              }
            ]
          },
          "liquidator": {
            "description": "Position snapshot of the **liquidator** account that absorbed the position.",
            "allOf": [
              {
                "$ref": "#/components/schemas/LiquidateMarketAccPositionResponse"
              }
            ]
          }
        },
        "required": [
          "marketId",
          "blockNumber",
          "timestamp",
          "txHash",
          "size",
          "rate",
          "tradeRate",
          "violator",
          "liquidator"
        ]
      },
      "LiquidationEventListResponse": {
        "type": "object",
        "properties": {
          "liquidationEvents": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/LiquidationEventResponse"
            }
          }
        },
        "required": [
          "liquidationEvents"
        ]
      },
      "OnChainEventItem": {
        "type": "object",
        "properties": {
          "eventName": {
            "type": "string",
            "description": "Event name"
          },
          "id": {
            "type": "string",
            "description": "Stable unique event id, formatted as `<blockHash>-<logIndex>`."
          },
          "sourceAddress": {
            "type": "string",
            "description": "Source contract address"
          },
          "blockNumber": {
            "type": "number",
            "description": "Block number"
          },
          "logIndex": {
            "type": "number",
            "description": "Position of the log within its transaction receipt."
          },
          "txHash": {
            "type": "string",
            "description": "Transaction hash"
          },
          "blockHash": {
            "type": "string",
            "description": "Block hash"
          },
          "blockTimestamp": {
            "type": "number",
            "description": "Block timestamp (Unix seconds, UTC)."
          },
          "eventIndex": {
            "type": "number",
            "description": "Composite ordering key: `blockNumber * 1_000_000 + logIndex`. Use this as the sort/cursor field \u2014 it is monotonic across the chain."
          },
          "isFinalized": {
            "type": "boolean",
            "description": "True once the block containing the event has been finalized by the consensus layer. Pre-finalization events can in principle be reorged out."
          },
          "data": {
            "type": "object",
            "description": "Decoded event-specific fields. Shape depends on `eventName` \u2014 e.g. for `LimitOrderFilled` this carries `marketAcc`, `orderId`, `size`, `cost`, etc. Refer to the contract ABI for the exact payload of each event type."
          }
        },
        "required": [
          "eventName",
          "id",
          "sourceAddress",
          "blockNumber",
          "logIndex",
          "txHash",
          "blockHash",
          "blockTimestamp",
          "eventIndex"
        ]
      },
      "OnChainEventsResponse": {
        "type": "object",
        "properties": {
          "events": {
            "description": "Events matching the query, sorted by `eventIndex` descending (newest first).",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/OnChainEventItem"
            }
          },
          "resumeToken": {
            "type": "string",
            "description": "Cursor for the next page. Pass back as `resumeToken` to continue paginating. **`null`** when the current page is the last one."
          }
        },
        "required": [
          "events"
        ]
      },
      "DepositSimulationBodyDto": {
        "type": "object",
        "properties": {
          "marketAcc": {
            "type": "string",
            "description": "Target account identifier (54-char hex). Packs `root + accountId + tokenId + marketId`. For a cross deposit, the embedded `marketId` segment is `0xFFFFFF`; for an isolated deposit, it is the actual market id. Shares the same `marketAcc` + `amount` shape as `POST /calldata-builder/user/deposit`."
          },
          "amount": {
            "type": "string",
            "description": "BigInt string of the deposit amount, **in the depositing token's native decimals** \u2014 same unit the calldata endpoint expects (e.g. `\"1000000\"` for 1 USDC at 6 decimals). Look up `decimals` from `/open-api-v2/v1/assets`."
          }
        },
        "required": [
          "marketAcc",
          "amount"
        ]
      },
      "DepositStateResponse": {
        "type": "object",
        "properties": {
          "collateralBalance": {
            "type": "string",
            "description": "Bigint string representing the collateral balance of the account"
          },
          "maintenanceMargin": {
            "type": "string",
            "description": "Bigint string representing the maintenance margin requirement"
          },
          "marginRatio": {
            "type": "number",
            "description": "Margin ratio of the account (collateral / maintenance margin)",
            "example": 2.5
          }
        },
        "required": [
          "collateralBalance",
          "maintenanceMargin",
          "marginRatio"
        ]
      },
      "DepositSimulationResponse": {
        "type": "object",
        "properties": {
          "minReceived": {
            "type": "string",
            "description": "Bigint string representing the minimum amount of collateral received after deposit"
          },
          "preUserState": {
            "description": "Snapshot of the destination `marketAcc` **before** the deposit lands \u2014 current `collateralBalance` (totalCash + \u03a3 positionValue), aggregate `maintenanceMargin`, and resulting `marginRatio = collateralBalance / maintenanceMargin`.",
            "allOf": [
              {
                "$ref": "#/components/schemas/DepositStateResponse"
              }
            ]
          },
          "postUserState": {
            "description": "Projected snapshot **after** crediting `minReceived` cash. Maintenance margin is unchanged (deposits do not open positions); the new `marginRatio` reflects the higher collateral. Use the delta vs `preUserState` to confirm liquidation headroom.",
            "allOf": [
              {
                "$ref": "#/components/schemas/DepositStateResponse"
              }
            ]
          }
        },
        "required": [
          "minReceived",
          "preUserState",
          "postUserState"
        ]
      },
      "RequestWithdrawalSimulationBodyDto": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Withdrawer wallet address (42-char hex). Aliased as `root` \u2014 the user wallet that owns the cross account. Simulation always assumes `accountId = 0` (the main sub-account)."
          },
          "tokenId": {
            "type": "number",
            "minimum": 1,
            "description": "Collateral token ID to withdraw \u2014 the token whose cross balance is being drained. Use `/open-api-v2/v1/assets` to look up `tokenId`."
          },
          "amount": {
            "type": "string",
            "description": "BigInt string of the requested withdrawal amount, scaled by **10^18 (cash accounting unit)** \u2014 *not* the token's native decimals. This differs from the corresponding `POST /calldata-builder/user/request-withdrawal` body, which expects native decimals; the simulation operates directly on the 18-decimal `collateralBalance` returned in the response. Only the cross account is debited \u2014 move funds from isolated to cross via `cash-transfer` first."
          }
        },
        "required": [
          "root",
          "tokenId",
          "amount"
        ]
      },
      "WithdrawStateResponse": {
        "type": "object",
        "properties": {
          "collateralBalance": {
            "type": "string",
            "description": "Bigint string representing the collateral balance of the account"
          },
          "marginRatio": {
            "type": "number",
            "description": "Margin ratio = `totalMaintenanceMargin / collateralBalance`. The account is liquidatable when this reaches **1.0**; safer accounts trend toward 0. Returns **0** when `collateralBalance` is 0 (no positions or fully empty), not Infinity.",
            "example": 0.4
          }
        },
        "required": [
          "collateralBalance",
          "marginRatio"
        ]
      },
      "WithdrawSimulationResponse": {
        "type": "object",
        "properties": {
          "preUserState": {
            "description": "Snapshot of the user's **cross** `marketAcc` (the only acc allowed to withdraw) before the requested amount leaves. `collateralBalance` is `totalCash + \u03a3 positionValue`; see `marginRatio` for the health metric.",
            "allOf": [
              {
                "$ref": "#/components/schemas/WithdrawStateResponse"
              }
            ]
          },
          "postUserState": {
            "description": "Projected snapshot **after** the withdrawal. `collateralBalance` drops by the requested amount (already 18-dec scaled); `marginRatio` **rises** accordingly (denominator shrinks while `totalMaintenanceMargin` is unchanged). Compare to `preUserState` to confirm the account stays below the liquidation threshold of `1.0`.",
            "allOf": [
              {
                "$ref": "#/components/schemas/WithdrawStateResponse"
              }
            ]
          }
        },
        "required": [
          "preUserState",
          "postUserState"
        ]
      },
      "CashTransferSimulationBodyDto": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "User wallet address \u2014 the `root` of the margin account. Supplies the user context the agent-signed calldata endpoint gets from its signature."
          },
          "accountId": {
            "type": "number",
            "minimum": 0,
            "maximum": 255,
            "default": 0,
            "description": "Sub-account index under `root` (0\u2013255). `0` is the main sub-account."
          },
          "marketId": {
            "type": "number",
            "minimum": 1,
            "description": "Market ID of the **isolated** leg. Matches `POST /calldata-builder/agent/cash-transfer` \u2014 the corresponding cross leg is derived from the market's `tokenId`."
          },
          "direction": {
            "type": "string",
            "enum": [
              "CROSS_TO_ISOLATED",
              "ISOLATED_TO_CROSS"
            ],
            "description": "Transfer direction: `CROSS_TO_ISOLATED` or `ISOLATED_TO_CROSS`."
          },
          "amount": {
            "type": "string",
            "description": "BigInt string of the transfer amount (always positive), scaled by 10^18 (cash accounting unit \u2014 same as the calldata endpoint)."
          }
        },
        "required": [
          "root",
          "marketId",
          "direction",
          "amount"
        ]
      },
      "CashTransferStateResponse": {
        "type": "object",
        "properties": {
          "collateralBalance": {
            "type": "string",
            "description": "Bigint string representing the collateral balance of the account"
          },
          "maintenanceMargin": {
            "type": "string",
            "description": "Bigint string representing the maintenance margin requirement"
          },
          "marginRatio": {
            "type": "number",
            "description": "Margin ratio = `maintenanceMargin / collateralBalance`. Liquidatable at **1.0**; safer accounts trend toward 0. Returns **0** when `collateralBalance` is 0.",
            "example": 0.4
          }
        },
        "required": [
          "collateralBalance",
          "maintenanceMargin",
          "marginRatio"
        ]
      },
      "CashTransferPrePostSimulationResponse": {
        "type": "object",
        "properties": {
          "preUserState": {
            "description": "Snapshot of this side of the transfer **before** the cash moves \u2014 `collateralBalance` (totalCash + \u03a3 positionValue), aggregate `maintenanceMargin`, and `marginRatio = maintenanceMargin / collateralBalance`.",
            "allOf": [
              {
                "$ref": "#/components/schemas/CashTransferStateResponse"
              }
            ]
          },
          "postUserState": {
            "description": "Projected snapshot **after** the transfer settles. Cross side decreases / increases by `amount` depending on `direction`; the isolated side moves by the opposite sign. `maintenanceMargin` is unchanged (cash moves do not open or close positions); `marginRatio` shifts because `collateralBalance` changed.",
            "allOf": [
              {
                "$ref": "#/components/schemas/CashTransferStateResponse"
              }
            ]
          }
        },
        "required": [
          "preUserState",
          "postUserState"
        ]
      },
      "CashTransferSimulationResponseV2": {
        "type": "object",
        "properties": {
          "crossAccState": {
            "description": "Pre/post state of the cross-margin account during the cash transfer",
            "allOf": [
              {
                "$ref": "#/components/schemas/CashTransferPrePostSimulationResponse"
              }
            ]
          },
          "isolatedAccState": {
            "description": "Pre/post state of the isolated-margin account during the cash transfer",
            "allOf": [
              {
                "$ref": "#/components/schemas/CashTransferPrePostSimulationResponse"
              }
            ]
          }
        },
        "required": [
          "crossAccState",
          "isolatedAccState"
        ]
      },
      "PlaceOrderSimulationBodyDto": {
        "type": "object",
        "properties": {
          "marketAcc": {
            "type": "string",
            "description": "Market account identifier (54-char hex). Packs `root + accountId + tokenId + marketId`. Identifies which sub-account / token / market is placing the order. The simulator does **not** require the account to have already entered the market \u2014 entrance is simulated as part of the call."
          },
          "marketId": {
            "type": "number",
            "minimum": 1,
            "description": "Market ID. Must match the `marketId` segment encoded in `marketAcc` (validated server-side)."
          },
          "side": {
            "type": "number",
            "enum": [
              0,
              1
            ],
            "description": "Order side. `0` = LONG (buy yield), `1` = SHORT (sell yield)."
          },
          "size": {
            "type": "string",
            "description": "BigInt string of the notional size (always positive), scaled by 10^18. Validated against the market's minimum order value \u2014 orders below the floor are rejected with `INVALID_INPUT`."
          },
          "tif": {
            "type": "number",
            "enum": [
              0,
              1,
              2,
              3,
              4
            ],
            "description": "Time-in-force (numeric). `0` = GTC (resting limit), `1` = IOC, `2` = FOK, `3` = ALO (post-only), `4` = SOFT_ALO. `FOK` / `IOC` are market-style orders that simulate immediate matching; `GTC` / `ALO` / `SOFT_ALO` are limit orders that simulate resting the unmatched remainder on the book. See [Order book mechanics](https://docs.pendle.finance/boros-dev/Mechanics/OrderBook)."
          },
          "rate": {
            "type": "number",
            "description": "Human-readable rate (e.g. `0.085` = 8.5% APR). **Required** for resting-limit `tif` (`GTC` / `ALO` / `SOFT_ALO`); optional for `IOC` and `FOK` (when omitted, the simulator falls back to the side's extreme tick). Backend rounds to the nearest tick: LONG rounds **down**, SHORT rounds **up** (always safer for the user). The post-rounding tick and rate are echoed back in `resolved.limitTick` / `resolved.actualRate`."
          },
          "slippage": {
            "type": "number",
            "description": "Relative tolerance from the current mid-rate (e.g. `0.005` = 0.5%). Backend computes the execution guard as `desiredRate = midRate \u00b1 slippage`. Strongly recommended for `FOK` / `IOC` market orders; if omitted on a market order, a default slippage is applied. The resolved guard is echoed in `resolved.desiredRate`."
          },
          "ammId": {
            "type": "number",
            "description": "AMM routing. `0` \u2192 orderbook-only (ignore AMM); a specific AMM id \u2192 also route through that AMM. Optional \u2014 if omitted, the simulator uses the market's active `ammId` from `/v1/markets`",
            "minimum": 0
          }
        },
        "required": [
          "marketAcc",
          "marketId",
          "side",
          "size",
          "tif"
        ]
      },
      "PlaceOrderPreStateResponse": {
        "type": "object",
        "properties": {
          "activePositionSize": {
            "type": "string",
            "description": "BigInt string (signed) representing the active position size **before** the order. Positive = LONG, negative = SHORT."
          }
        },
        "required": [
          "activePositionSize"
        ]
      },
      "ContractSwapPositionResponse": {
        "type": "object",
        "properties": {
          "size": {
            "type": "string",
            "description": "Bigint string representing the matched position size"
          },
          "cost": {
            "type": "string",
            "description": "Bigint string representing the cost of the matched position"
          },
          "rate": {
            "type": "number",
            "description": "Volume-weighted average matched APR (decimal, e.g. `0.085` = 8.5%) for the matched portion. `0` when nothing matched (pure resting order).",
            "example": 0.085
          }
        },
        "required": [
          "size",
          "cost",
          "rate"
        ]
      },
      "PlaceOrderPostStateResponse": {
        "type": "object",
        "properties": {
          "marginRequired": {
            "type": "string",
            "description": "BigInt string of the margin the account must hold **after** the order."
          },
          "liquidationApr": {
            "type": "number",
            "description": "Projected liquidation APR. `null` if the position remains margin-safe at any APR.",
            "example": 0.15,
            "nullable": true
          },
          "longYieldApr": {
            "type": "number",
            "description": "Post-trade long-yield APR for this market.",
            "example": 0.09
          }
        },
        "required": [
          "marginRequired",
          "longYieldApr"
        ]
      },
      "PlaceOrderResolvedResponse": {
        "type": "object",
        "properties": {
          "side": {
            "type": "number",
            "enum": [
              0,
              1
            ],
            "description": "Order side (numeric): `0` = LONG, `1` = SHORT. Always present."
          },
          "limitTick": {
            "type": "number",
            "description": "Exact tick used in the simulation (equal to request `limitTick`, or derived from `rate`)."
          },
          "actualRate": {
            "type": "number",
            "description": "Rate corresponding to `limitTick` after tick-to-rate conversion. May differ from `requestedRate` by a fraction of a bp.",
            "example": 0.08497
          },
          "requestedRate": {
            "type": "number",
            "description": "Echoes the original user-supplied `rate`. Only set if the request used `rate`.",
            "example": 0.085
          },
          "desiredRate": {
            "type": "number",
            "description": "Absolute ceiling used in the simulation. Set if `desiredRate` or `slippage` was provided (or auto-set for limit orders); `null` otherwise.",
            "nullable": true,
            "example": 0.09
          }
        },
        "required": [
          "side",
          "limitTick",
          "actualRate"
        ]
      },
      "PlaceOrderSimulationResponseV3": {
        "type": "object",
        "properties": {
          "preState": {
            "description": "Account snapshot **before** the order \u2014 currently exposes `activePositionSize` so callers can compute net deltas without a separate user-info call.",
            "allOf": [
              {
                "$ref": "#/components/schemas/PlaceOrderPreStateResponse"
              }
            ]
          },
          "matched": {
            "description": "Matched portion (size = 0 for a pure resting order).",
            "allOf": [
              {
                "$ref": "#/components/schemas/ContractSwapPositionResponse"
              }
            ]
          },
          "postState": {
            "description": "Projected account snapshot **after** the order \u2014 `marginRequired` (with leverage applied), `liquidationApr` (`null` if margin-safe at any APR), and post-trade `longYieldApr` for this market.",
            "allOf": [
              {
                "$ref": "#/components/schemas/PlaceOrderPostStateResponse"
              }
            ]
          },
          "priceImpact": {
            "type": "number",
            "description": "Price impact of the order against mid-rate (decimal).",
            "example": 0.002
          },
          "status": {
            "type": "string",
            "description": "Human-readable status (e.g. `FILLED`, `PARTIALLY_FILLED`, `RESTING`, `NOT_FILLED`)."
          },
          "statusCode": {
            "type": "string",
            "description": "Contract-level status (`Succeed`, or a contract error code such as `InsufficientMargin`).",
            "example": "Succeed"
          },
          "makerOrderReward": {
            "type": "number",
            "description": "Estimated maker order reward in PENDLE.",
            "deprecated": true
          },
          "resolved": {
            "description": "Echo of the inputs the simulator actually used after rate\u2194tick conversion and slippage resolution. Use these values when building the matching `place-order` calldata to avoid drift between simulation and submission.",
            "allOf": [
              {
                "$ref": "#/components/schemas/PlaceOrderResolvedResponse"
              }
            ]
          }
        },
        "required": [
          "preState",
          "matched",
          "postState",
          "priceImpact",
          "status",
          "statusCode",
          "makerOrderReward",
          "resolved"
        ]
      },
      "PlaceOrderAnonymousSimulationBodyDto": {
        "type": "object",
        "properties": {
          "marketId": {
            "type": "number",
            "minimum": 1,
            "description": "Market ID."
          },
          "side": {
            "type": "number",
            "enum": [
              0,
              1
            ],
            "description": "Order side. `0` = LONG (buy yield), `1` = SHORT (sell yield)."
          },
          "size": {
            "type": "string",
            "description": "BigInt string of the notional size (always positive), scaled by 10^18."
          },
          "tif": {
            "type": "number",
            "enum": [
              0,
              1,
              2,
              3,
              4
            ],
            "description": "Time-in-force (numeric). `0` = GTC, `1` = IOC, `2` = FOK, `3` = ALO, `4` = SOFT_ALO. See [Order book mechanics](https://docs.pendle.finance/boros-dev/Mechanics/OrderBook)."
          },
          "rate": {
            "type": "number",
            "description": "Human-readable rate (e.g. `0.085` = 8.5% APR). **Required** for resting-limit `tif` (`GTC` / `ALO` / `SOFT_ALO`); optional for `IOC` / `FOK`."
          },
          "slippage": {
            "type": "number",
            "description": "Relative tolerance from current mid-rate (e.g. `0.005` = 0.5%). Recommended for `FOK` / `IOC`."
          },
          "ammId": {
            "type": "number",
            "description": "AMM routing. `0` \u2192 orderbook-only; a specific AMM id \u2192 also route through that AMM. Omitted \u2192 market's active `ammId`.",
            "minimum": 0
          }
        },
        "required": [
          "marketId",
          "side",
          "size",
          "tif"
        ]
      },
      "AnonymousPlaceOrderFeeBreakdownResponse": {
        "type": "object",
        "properties": {
          "takerOtcFee": {
            "type": "string",
            "description": "Taker OTC fee charged on the matched portion (bigint, 10^18). Omitted when no fee applies."
          },
          "takerOtcFeeInUSD": {
            "type": "number",
            "description": "Spot-USD value of `takerOtcFee`, valued at query time. Omitted when no fee applies.",
            "example": 0.08
          }
        }
      },
      "MakerIncentiveSimulationResponse": {
        "type": "object",
        "properties": {
          "filledVolumeIncentiveReward": {
            "type": "number",
            "description": "Incentive reward earned from filled order volume, denominated in PENDLE",
            "example": 1.25,
            "deprecated": true
          },
          "provideLiquidityIncentiveReward": {
            "type": "number",
            "description": "Incentive reward earned from providing liquidity, denominated in PENDLE",
            "example": 0.75
          },
          "makerFeeRebate": {
            "type": "number",
            "description": "taker-fee rebate preview (collateral token units)"
          },
          "makerFeeRebateToken": {
            "type": "string",
            "description": "collateral token address"
          }
        },
        "required": [
          "filledVolumeIncentiveReward",
          "provideLiquidityIncentiveReward",
          "makerFeeRebate",
          "makerFeeRebateToken"
        ]
      },
      "AnonymousPlaceOrderSimulationResponse": {
        "type": "object",
        "properties": {
          "matched": {
            "description": "Matched portion (`size = 0` for a pure resting order; `rate = 0` when nothing matched).",
            "allOf": [
              {
                "$ref": "#/components/schemas/ContractSwapPositionResponse"
              }
            ]
          },
          "marginRequired": {
            "type": "string",
            "description": "Initial margin the order would require (bigint, 10^18)."
          },
          "priceImpact": {
            "type": "number",
            "description": "Price impact of the order vs current mid-rate (decimal).",
            "example": 0.003
          },
          "feeBreakdown": {
            "$ref": "#/components/schemas/AnonymousPlaceOrderFeeBreakdownResponse"
          },
          "makerOrderReward": {
            "type": "number",
            "description": "Estimated maker order reward in PENDLE for the matched portion.",
            "deprecated": true
          },
          "makerIncentive": {
            "$ref": "#/components/schemas/MakerIncentiveSimulationResponse"
          },
          "resolved": {
            "description": "Echo of the inputs the simulator actually used after rate\u2194tick conversion and slippage resolution.",
            "allOf": [
              {
                "$ref": "#/components/schemas/PlaceOrderResolvedResponse"
              }
            ]
          }
        },
        "required": [
          "matched",
          "marginRequired",
          "priceImpact",
          "feeBreakdown",
          "makerOrderReward",
          "makerIncentive",
          "resolved"
        ]
      },
      "AddLiquidityToAmmV2SimulationBodyDto": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "User wallet address \u2014 the `root` of the margin account. Supplied explicitly because no signature exists yet at this point in the flow; the calldata-builder counterpart `POST /calldata-builder/agent/add-liquidity-to-amm` takes `root` for the same reason."
          },
          "accountId": {
            "type": "number",
            "minimum": 0,
            "maximum": 255,
            "default": 0,
            "description": "Sub-account index under `root` (0\u2013255). `0` is the main sub-account."
          },
          "marketId": {
            "type": "number",
            "minimum": 1,
            "description": "Market ID whose AMM pool will receive the deposit. The pool's active `ammId` is resolved server-side."
          },
          "netCashIn": {
            "type": "string",
            "description": "BigInt string of net cash to add as single-sided liquidity, scaled by **10^18 (cash accounting unit)** \u2014 same as `POST /calldata-builder/agent/add-liquidity-to-amm`. Funds are pulled from the user's cross account; if the account has not entered the market yet, an additional market-entrance fee is also debited (surfaced in `feeBreakdown.marketEntranceFee`)."
          }
        },
        "required": [
          "root",
          "marketId",
          "netCashIn"
        ]
      },
      "AddLiquidityFeeBreakdownResponse": {
        "type": "object",
        "properties": {
          "marketEntranceFee": {
            "type": "string",
            "description": "Bigint string representing the market entrance fee charged for entering a market"
          },
          "vaultDepositFee": {
            "type": "string",
            "description": "Bigint string representing the vault deposit fee charged when depositing into the vault"
          },
          "marketEntranceFeeInUSD": {
            "type": "number",
            "description": "Spot-USD value of `marketEntranceFee`, valued at query time. Omitted when no fee applies.",
            "example": 0.15
          },
          "vaultDepositFeeInUSD": {
            "type": "number",
            "description": "Spot-USD value of `vaultDepositFee`, valued at query time. Omitted when no fee applies.",
            "example": 0.03
          }
        }
      },
      "AddLiquidityToAmmFeeSimulationResponse": {
        "type": "object",
        "properties": {
          "feeBreakdown": {
            "description": "Per-component fees that will be charged when the add-liquidity transaction lands. Combines the one-off `marketEntranceFee` (only present if the account is not yet a member of the AMM's market) with the `vaultDepositFee` (proportional to the deposited cash). Bigint amounts are in 18-decimal cash units; `*InUSD` siblings are spot-priced floats. All sub-fields are optional \u2014 missing means the corresponding component does not apply.",
            "allOf": [
              {
                "$ref": "#/components/schemas/AddLiquidityFeeBreakdownResponse"
              }
            ]
          }
        },
        "required": [
          "feeBreakdown"
        ]
      },
      "RemoveLiquidityFromAmmV2SimulationBodyDto": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "User wallet address \u2014 the `root` of the margin account. Supplied explicitly because the simulation has no agent signature to read it from (the calldata-builder counterpart `POST /calldata-builder/agent/remove-liquidity-from-amm` derives it from the signed payload)."
          },
          "accountId": {
            "type": "number",
            "minimum": 0,
            "maximum": 255,
            "default": 0,
            "description": "Sub-account index under `root` (0\u2013255). `0` is the main sub-account."
          },
          "marketId": {
            "type": "number",
            "minimum": 1,
            "description": "Market ID whose AMM pool the LP tokens will be redeemed from."
          },
          "lpToRemove": {
            "type": "string",
            "description": "BigInt string of LP tokens to burn, scaled by 10^18 \u2014 same unit as `POST /calldata-builder/agent/remove-liquidity-from-amm`. Caller must hold at least this much LP in the AMM sub-account; the resulting cash is returned to the cross account, net of the vault withdrawal fee surfaced in `feeBreakdown.vaultWithdrawalFee`."
          }
        },
        "required": [
          "root",
          "marketId",
          "lpToRemove"
        ]
      },
      "RemoveLiquidityFeeBreakdownResponse": {
        "type": "object",
        "properties": {
          "vaultWithdrawalFee": {
            "type": "string",
            "description": "Bigint string representing the vault withdrawal fee charged when withdrawing from the vault"
          },
          "vaultWithdrawalFeeInUSD": {
            "type": "number",
            "description": "Spot-USD value of `vaultWithdrawalFee`, valued at query time. Omitted when no fee applies.",
            "example": 0.03
          }
        }
      },
      "RemoveLiquidityFromAmmFeeSimulationResponse": {
        "type": "object",
        "properties": {
          "feeBreakdown": {
            "description": "Per-component fees that will be charged when the remove-liquidity transaction lands. Currently only carries `vaultWithdrawalFee` (proportional to the LP being burned); other fee categories do not apply on the exit path. Bigint amount is in 18-decimal cash units; `vaultWithdrawalFeeInUSD` is the spot-priced equivalent.",
            "allOf": [
              {
                "$ref": "#/components/schemas/RemoveLiquidityFeeBreakdownResponse"
              }
            ]
          }
        },
        "required": [
          "feeBreakdown"
        ]
      },
      "UserVaultStateEntry": {
        "type": "object",
        "properties": {
          "marketId": {
            "type": "number",
            "description": "Numeric market identifier (1-based)."
          },
          "tokenId": {
            "type": "number",
            "description": "Numeric `tokenId` of the collateral asset."
          },
          "ammId": {
            "type": "number",
            "description": "AMM identifier of the vault holding this position."
          },
          "averageLpPrice": {
            "type": "number",
            "description": "Volume-weighted average LP price (in collateral) the user paid into this vault."
          },
          "depositValue": {
            "type": "string",
            "description": "User's aggregate deposit value into this vault, in collateral (18-decimal bigint string)."
          },
          "totalLp": {
            "type": "string",
            "description": "LP tokens currently held by the user in this vault (18-decimal bigint string)."
          },
          "availableBalanceToDeposit": {
            "type": "string",
            "description": "Cap on the user's next LP deposit into this vault \u2014 derived from the cross-account initial-margin headroom (minus any first-time market entrance fee), 18-decimal bigint string. **Not** a wallet ERC-20 balance."
          }
        },
        "required": [
          "marketId",
          "tokenId",
          "ammId",
          "averageLpPrice",
          "depositValue",
          "totalLp",
          "availableBalanceToDeposit"
        ]
      },
      "UserVaultStatesResponse": {
        "type": "object",
        "properties": {
          "results": {
            "description": "AMM vaults the user holds non-zero LP in.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/UserVaultStateEntry"
            }
          }
        },
        "required": [
          "results"
        ]
      },
      "AccountGasBalanceResponse": {
        "type": "object",
        "properties": {
          "balanceInUSD": {
            "type": "number",
            "example": 12.5,
            "description": "Current wallet-level gas balance in USD. Spent automatically on each relayed action; top up via the `pay-treasury` calldata endpoints (collateral ERC-20, not the chain-native gas token)."
          }
        },
        "required": [
          "balanceInUSD"
        ]
      },
      "GasConsumptionV2Response": {
        "type": "object",
        "properties": {
          "actionType": {
            "type": "string",
            "description": "Action type that consumed gas (e.g. deposit, withdraw, trade)",
            "example": "deposit"
          },
          "root": {
            "type": "string",
            "description": "User wallet address \u2014 the `root` of the margin account."
          },
          "gasFee": {
            "type": "number",
            "description": "Gas fee in USD (always positive). 0 when this record is a top-up.",
            "example": 0.35
          },
          "topUpAmount": {
            "type": "number",
            "description": "Top-up / credit amount in USD (positive). Set only for credits; undefined for gas-fee records.",
            "example": 5
          },
          "txHash": {
            "type": "string"
          },
          "blockTimestamp": {
            "type": "number"
          },
          "chainId": {
            "type": "number"
          }
        },
        "required": [
          "actionType",
          "root",
          "gasFee",
          "txHash",
          "blockTimestamp",
          "chainId"
        ]
      },
      "GasConsumptionHistoryV2RootResponse": {
        "type": "object",
        "properties": {
          "results": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/GasConsumptionV2Response"
            }
          },
          "resumeToken": {
            "type": "string",
            "description": "Resume token for fetching the next page. Null if there are no more results."
          }
        },
        "required": [
          "results"
        ]
      },
      "FundLocationResponse": {
        "type": "object",
        "properties": {
          "fundType": {
            "type": "string",
            "enum": [
              "wallet",
              "cross_account",
              "isolated_account",
              "amm"
            ],
            "description": "Where the funds live in the Boros accounting hierarchy. { Wallet : wallet, CrossAccount : cross_account, IsolatedAccount : isolated_account, AMM : amm }"
          },
          "marketId": {
            "type": "number",
            "description": "Numeric `marketId` when `fundType` references an isolated/specific market; omitted for wallet/cross/global locations."
          },
          "tokenId": {
            "type": "number",
            "description": "Per-side token id. Only set on swap-collateral events."
          },
          "amount": {
            "type": "string",
            "description": "Per-side amount (18-decimal bigint string). Only set on swap-collateral events."
          }
        },
        "required": [
          "fundType"
        ]
      },
      "TransferLogResponse": {
        "type": "object",
        "properties": {
          "transferLogId": {
            "type": "string",
            "description": "Stable per-event id (use for de-duplication)."
          },
          "blockTimestamp": {
            "type": "number",
            "description": "Block timestamp in **Unix seconds (UTC)**."
          },
          "root": {
            "type": "string",
            "description": "User wallet address \u2014 the `root` of the margin account."
          },
          "accountId": {
            "type": "number",
            "description": "Sub-account index under `root` (0\u2013255). `0` is the main sub-account."
          },
          "tokenId": {
            "type": "number",
            "description": "Numeric `tokenId` of the collateral asset."
          },
          "amount": {
            "type": "string",
            "description": "Transfer amount, **18-decimal bigint string** (normalised; not in the underlying token's native decimals). Always non-negative \u2014 direction is encoded by `fromFundLocation` / `toFundLocation`."
          },
          "fromFundLocation": {
            "description": "Source bucket the funds left.",
            "allOf": [
              {
                "$ref": "#/components/schemas/FundLocationResponse"
              }
            ]
          },
          "toFundLocation": {
            "description": "Destination bucket the funds entered.",
            "allOf": [
              {
                "$ref": "#/components/schemas/FundLocationResponse"
              }
            ]
          },
          "status": {
            "type": "string",
            "enum": [
              "pending",
              "success",
              "failed"
            ],
            "description": "Lifecycle status (e.g. pending vs finalised; relevant for cooldown-gated withdrawals). { Pending : pending, Success : success, Failed : failed }"
          }
        },
        "required": [
          "transferLogId",
          "blockTimestamp",
          "root",
          "accountId",
          "tokenId",
          "amount",
          "fromFundLocation",
          "toFundLocation",
          "status"
        ]
      },
      "TransferLogsV2Response": {
        "type": "object",
        "properties": {
          "results": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/TransferLogResponse"
            }
          },
          "resumeToken": {
            "type": "string",
            "description": "Resume token for fetching the next page. Null if there are no more results."
          },
          "syncStatus": {
            "description": "Current sync status of the backend",
            "allOf": [
              {
                "$ref": "#/components/schemas/SyncStatusResponse"
              }
            ]
          }
        },
        "required": [
          "results",
          "syncStatus"
        ]
      },
      "LimitOrdersV2Response": {
        "type": "object",
        "properties": {
          "results": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/LimitOrderResponseV2"
            }
          },
          "resumeToken": {
            "type": "string",
            "description": "Resume token for fetching the next page. Null if there are no more results."
          },
          "syncStatus": {
            "description": "Current sync status of the backend",
            "allOf": [
              {
                "$ref": "#/components/schemas/SyncStatusResponse"
              }
            ]
          }
        },
        "required": [
          "results",
          "syncStatus"
        ]
      },
      "TransactionResponse": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "Stable per-event id (use for de-duplication)."
          },
          "timestamp": {
            "type": "number",
            "description": "Block timestamp of the event in Unix seconds (UTC)."
          },
          "side": {
            "type": "number",
            "enum": [
              0,
              1
            ],
            "description": "The trade side: long or short. { LONG : 0, SHORT : 1 }"
          },
          "tradeSize": {
            "type": "string",
            "description": "Absolute notional size, 18-decimal bigint string, always positive \u2014 sign comes from `side`."
          },
          "tradeRate": {
            "type": "number",
            "description": "Implied fixed APR at which the trade was executed (decimal, e.g. `0.05` = 5%).",
            "example": 0.05
          },
          "tradeValue": {
            "type": "string",
            "description": "Notional value in the settlement token, 18-decimal bigint string."
          },
          "fee": {
            "type": "string",
            "description": "Trading fee charged (18-decimal bigint string)."
          },
          "pnl": {
            "type": "string",
            "description": "Realised PnL booked at this event, net of fees (18-decimal signed bigint string). Negative means a loss.",
            "example": "-250000000000000"
          },
          "txHash": {
            "type": "string"
          },
          "isLimitOrderTrade": {
            "type": "boolean",
            "description": "`true` when this `marketAcc` filled as the maker (its resting limit order was hit). `false` for taker (market) fills."
          },
          "blockNumber": {
            "type": "number"
          },
          "orderId": {
            "type": "string",
            "description": "Source `orderId` \u2014 only set for maker fills (`isLimitOrderTrade = true`)."
          },
          "marketId": {
            "type": "number"
          },
          "marketAcc": {
            "type": "string",
            "description": "The position-owner `marketAcc` (54-char hex). Decode via `/v1/market-acc/decode`."
          },
          "prevPositionS": {
            "type": "string",
            "description": "Signed position size **before** this event (18-decimal bigint string; positive = long, negative = short)."
          },
          "prevPositionF": {
            "type": "string",
            "description": "Position fixed-rate cost basis **before** this event, 18-decimal bigint string. Divide by 1e18 for the human-readable value."
          },
          "postPositionF": {
            "type": "string",
            "description": "Position fixed-rate cost basis **after** this event (18-decimal raw value)."
          },
          "postPositionS": {
            "type": "string",
            "description": "Signed position size **after** this event (18-decimal bigint string)."
          }
        },
        "required": [
          "id",
          "timestamp",
          "side",
          "tradeSize",
          "tradeRate",
          "tradeValue",
          "fee",
          "pnl",
          "txHash",
          "isLimitOrderTrade",
          "blockNumber",
          "orderId",
          "marketId",
          "marketAcc",
          "prevPositionS",
          "prevPositionF",
          "postPositionF",
          "postPositionS"
        ]
      },
      "TransactionsV2Response": {
        "type": "object",
        "properties": {
          "results": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/TransactionResponse"
            }
          },
          "resumeToken": {
            "type": "string",
            "description": "Resume token for fetching the next page. Null if there are no more results."
          },
          "syncStatus": {
            "description": "Current sync status of the backend",
            "allOf": [
              {
                "$ref": "#/components/schemas/SyncStatusResponse"
              }
            ]
          }
        },
        "required": [
          "results",
          "syncStatus"
        ]
      },
      "SettlementResponse": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "Stable per-event id (use for de-duplication)."
          },
          "timestamp": {
            "type": "number",
            "description": "Block timestamp of the settlement in **Unix seconds (UTC)**."
          },
          "marketAcc": {
            "type": "string",
            "description": "The position-owner `marketAcc` (54-char hex). Decode via `/v1/market-acc/decode`.",
            "example": "0x9dcf85824e024fea9e3ef583dccbea68edbc37b8000002ffffff"
          },
          "marketId": {
            "type": "number",
            "description": "Numeric market identifier (1-based)."
          },
          "side": {
            "type": "number",
            "enum": [
              0,
              1
            ],
            "description": "Position side at time of settlement. { LONG : 0, SHORT : 1 }"
          },
          "positionSize": {
            "type": "string",
            "description": "Signed position size at time of settlement (18-decimal bigint string; positive = long, negative = short)."
          },
          "positionValue": {
            "type": "string",
            "description": "Notional value of the position at time of settlement (18-decimal bigint string in settlement token)."
          },
          "yieldPaid": {
            "type": "string",
            "description": "Yield paid by this position holder during the settlement period (18-decimal **signed** bigint string; sign depends on `paymentIndex` / `fixedApr`)."
          },
          "yieldReceived": {
            "type": "string",
            "description": "Yield received by this position holder during the settlement period (18-decimal **signed** bigint string; sign depends on `paymentIndex` / `fixedApr`)."
          },
          "fee": {
            "type": "string",
            "description": "Fee charged on this settlement (18-decimal bigint string)."
          },
          "settlement": {
            "type": "string",
            "description": "Net settlement = `yieldReceived - yieldPaid - fee` (18-decimal **signed** bigint string). Positive means the holder was paid out this period."
          },
          "settlementRate": {
            "type": "number",
            "description": "Annualised settlement rate effectively applied this period (decimal, e.g. `0.05` = 5%).",
            "example": 0.05
          },
          "cumulativeSettlementPnl": {
            "type": "string",
            "description": "All-time cumulative settlement PnL for this `(marketAcc, marketId)` (18-decimal signed bigint string)."
          },
          "sinceOpenSettlementPnl": {
            "type": "string",
            "description": "Cumulative settlement PnL since the **current** position was opened from flat (18-decimal signed bigint string). Resets each time the position fully closes and re-opens."
          }
        },
        "required": [
          "id",
          "timestamp",
          "marketAcc",
          "marketId",
          "side",
          "positionSize",
          "positionValue",
          "yieldPaid",
          "yieldReceived",
          "fee",
          "settlement",
          "settlementRate",
          "cumulativeSettlementPnl",
          "sinceOpenSettlementPnl"
        ]
      },
      "SettlementsV2Response": {
        "type": "object",
        "properties": {
          "results": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SettlementResponse"
            }
          },
          "resumeToken": {
            "type": "string",
            "description": "Resume token for fetching the next page. Null if there are no more results."
          },
          "syncStatus": {
            "description": "Current sync status of the backend",
            "allOf": [
              {
                "$ref": "#/components/schemas/SyncStatusResponse"
              }
            ]
          }
        },
        "required": [
          "results",
          "syncStatus"
        ]
      },
      "GetMarketAccInfosV2Dto": {
        "type": "object",
        "properties": {
          "marketAccs": {
            "maxItems": 100,
            "description": "Array of fully-packed 54-character `marketAcc` identifiers. Mix cross and isolated handles freely \u2014 each is looked up independently. Build entries via `/v1/market-acc/encode`. Min 1, max 100 per request.",
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "required": [
          "marketAccs"
        ]
      },
      "OrderResponse": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "Order identifier as a bigint string (64-bit uint). Packs an init marker (bit 63) \u00b7 `side` (bit 56) \u00b7 `tickIndex` (bits 55\u201340) \u00b7 `orderIndex` (bits 39\u20130, monotonic per market+side); opaque to the caller."
          },
          "maker": {
            "type": "string",
            "description": "The maker `marketAcc` that placed the order."
          },
          "size": {
            "type": "string",
            "description": "Remaining unfilled order size (18-decimal bigint string)."
          },
          "side": {
            "type": "number",
            "enum": [
              0,
              1
            ],
            "description": "Side of the order. { LONG : 0, SHORT : 1 }"
          },
          "tick": {
            "type": "number",
            "description": "Integer tick on the discrete rate ladder. `rate = 1.00005^(tick \u00d7 tickStep) - 1` where `tickStep` is per-market."
          },
          "rate": {
            "type": "string",
            "description": "Limit rate (fixed APR) of the order, 18-decimal bigint string. Divide by 1e18 to get the APR as a decimal (e.g. `5e16` \u2192 `0.05` = 5%)."
          },
          "initialMargin": {
            "type": "string",
            "description": "Initial-margin requirement currently locked behind this resting order, 18-decimal bigint string."
          },
          "initialMarginWithLeverage": {
            "type": "string",
            "deprecated": true,
            "description": "DEPRECATED: alias of `initialMargin`. Will be removed in a future release."
          }
        },
        "required": [
          "id",
          "maker",
          "size",
          "side",
          "tick",
          "rate",
          "initialMargin",
          "initialMarginWithLeverage"
        ]
      },
      "PositionResponse": {
        "type": "object",
        "properties": {
          "marketId": {
            "type": "number",
            "description": "Numeric market identifier (1-based)."
          },
          "signedSize": {
            "type": "string",
            "description": "Signed position size, 18-decimal bigint string. **Positive = long, negative = short**, zero only for residual entries."
          },
          "positionValue": {
            "type": "string",
            "description": "Mark-to-market notional value of the position, 18-decimal bigint string in the settlement token."
          },
          "liquidationApr": {
            "type": "string",
            "description": "Fixed APR at which this position would be liquidated, 18-decimal bigint string. Divide by 1e18 to get the APR as a decimal (e.g. `5e16` \u2192 `0.05` = 5%)."
          },
          "initialMargin": {
            "type": "string",
            "description": "Raw initial-margin requirement (no leverage), 18-decimal bigint string in the settlement token."
          },
          "initialMarginWithLeverage": {
            "type": "string",
            "deprecated": true,
            "description": "DEPRECATED: with per-account leverage removed this equals `initialMargin`. Will be removed in a future release."
          },
          "maintMargin": {
            "type": "string",
            "description": "Maintenance-margin requirement, 18-decimal bigint string. The position is liquidatable when the account can no longer cover this."
          },
          "orders": {
            "description": "Resting orders pinned to this position.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/OrderResponse"
            }
          }
        },
        "required": [
          "marketId",
          "signedSize",
          "positionValue",
          "liquidationApr",
          "initialMargin",
          "initialMarginWithLeverage",
          "maintMargin",
          "orders"
        ]
      },
      "MarketAccInfoResponse": {
        "type": "object",
        "properties": {
          "marketAcc": {
            "type": "string",
            "description": "The 54-char `marketAcc` this result corresponds to (echoed from the request)."
          },
          "totalCash": {
            "type": "string",
            "description": "Collateral cash held inside this `marketAcc` (18-decimal bigint string in the settlement token)."
          },
          "netBalance": {
            "type": "string",
            "description": "Equity of this `marketAcc`: `totalCash + \u03a3 positionValue` (18-decimal signed bigint string). Drives all margin checks below."
          },
          "positions": {
            "description": "Open positions visible to this `marketAcc`. For an isolated handle this is at most one entry; for a cross handle, one per entered market with a non-zero position.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PositionResponse"
            }
          },
          "initialMargin": {
            "type": "string",
            "description": "Sum of raw per-position initial margins (no leverage), 18-decimal bigint string in the settlement token."
          },
          "initialMarginWithLeverage": {
            "type": "string",
            "deprecated": true,
            "description": "DEPRECATED: alias of `initialMargin`. Will be removed in a future release."
          },
          "availableInitialMargin": {
            "type": "string",
            "description": "Remaining margin available for opening new positions/orders, 18-decimal bigint string. Reaches 0 when no further IM-consuming actions are possible."
          },
          "availableMaintMargin": {
            "type": "string",
            "description": "Remaining buffer before liquidation, 18-decimal bigint string. Zero => health ratio = 1.0 and the account is liquidatable. See [Margin mechanics](https://docs.pendle.finance/boros-dev/Mechanics/Margin#health-ratio)."
          }
        },
        "required": [
          "marketAcc",
          "totalCash",
          "netBalance",
          "positions",
          "initialMargin",
          "initialMarginWithLeverage",
          "availableInitialMargin",
          "availableMaintMargin"
        ]
      },
      "MarketAccInfosResponse": {
        "type": "object",
        "properties": {
          "results": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/MarketAccInfoResponse"
            }
          },
          "syncStatus": {
            "description": "Current sync status",
            "allOf": [
              {
                "$ref": "#/components/schemas/SyncStatusResponse"
              }
            ]
          }
        },
        "required": [
          "results",
          "syncStatus"
        ]
      },
      "ActivePositionWithPnlResponse": {
        "type": "object",
        "properties": {
          "marketId": {
            "type": "number",
            "description": "Numeric market identifier (1-based)."
          },
          "marketAcc": {
            "type": "string",
            "description": "The position-owner `marketAcc` (54-char hex). Decode via `/v1/market-acc/decode`."
          },
          "isCross": {
            "type": "boolean",
            "description": "`true` if owned by a cross `marketAcc`, `false` for isolated."
          },
          "fixedApr": {
            "type": "number",
            "description": "Effective fixed APR cost basis of the position (decimal, e.g. `0.05` = 5%).",
            "example": 0.05
          },
          "signedSize": {
            "type": "string",
            "description": "Signed position size, 18-decimal bigint string. **Positive = long, negative = short.** Always non-zero in this list."
          },
          "side": {
            "type": "number",
            "enum": [
              0,
              1
            ],
            "description": "Position side derived from `signedSize`. { LONG : 0, SHORT : 1 }"
          },
          "cumulativePnl": {
            "type": "string",
            "description": "All-time cumulative realised trade PnL for this `(marketAcc, marketId)` (18-decimal signed bigint string, **net of fees**)."
          },
          "isMatured": {
            "type": "boolean",
            "description": "Whether the underlying market has reached its maturity date. Matured markets are non-tradable; positions stay until final settlement."
          },
          "unrealisedPnl": {
            "type": "string",
            "description": "Live mark-to-market PnL on the open position, derived from current `markApr` vs the position's entered `fixedApr` (18-decimal signed bigint string). Zero when flat or matured."
          },
          "settlementPnl": {
            "type": "string",
            "description": "All-time cumulative funding-rate settlement PnL for this `(marketAcc, marketId)` (18-decimal signed bigint string)."
          }
        },
        "required": [
          "marketId",
          "marketAcc",
          "isCross",
          "fixedApr",
          "signedSize",
          "side",
          "cumulativePnl",
          "isMatured",
          "unrealisedPnl",
          "settlementPnl"
        ]
      },
      "ActivePositionsWithPnlResponse": {
        "type": "object",
        "properties": {
          "results": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ActivePositionWithPnlResponse"
            }
          },
          "syncStatus": {
            "description": "Current sync status of the backend",
            "allOf": [
              {
                "$ref": "#/components/schemas/SyncStatusResponse"
              }
            ]
          }
        },
        "required": [
          "results",
          "syncStatus"
        ]
      },
      "EnteredMarketResponse": {
        "type": "object",
        "properties": {
          "marketId": {
            "type": "number",
            "description": "Numeric id of an entered (and not yet exited) market."
          },
          "isMatured": {
            "type": "boolean",
            "description": "Whether the market has reached its maturity date. Matured markets are non-tradable but may still hold residual positions awaiting final settlement."
          }
        },
        "required": [
          "marketId",
          "isMatured"
        ]
      },
      "EnteredMarketsResponse": {
        "type": "object",
        "properties": {
          "results": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/EnteredMarketResponse"
            }
          }
        },
        "required": [
          "results"
        ]
      },
      "MarginConfigResponse": {
        "type": "object",
        "properties": {
          "marketAcc": {
            "type": "object",
            "description": "The `marketAcc` this config applies to (echoed from the request)."
          },
          "marketId": {
            "type": "number",
            "description": "Numeric market identifier (1-based)."
          },
          "kIM": {
            "type": "string",
            "description": "The on-chain personal initial-margin override (`kIM`) for this `(marketAcc, marketId)`. Raw fixed-point string, same domain as `market.config.kIM`. Only markets with an active personal override appear in `results`; for any market not listed, use the global `market.config.kIM`."
          }
        },
        "required": [
          "marketAcc",
          "marketId",
          "kIM"
        ]
      },
      "MarginConfigsResponse": {
        "type": "object",
        "properties": {
          "results": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/MarginConfigResponse"
            }
          },
          "syncStatus": {
            "description": "Sync-core projection freshness. A market absent from `results` means \"no personal override as of this block\" \u2014 overlay against the global config accordingly.",
            "allOf": [
              {
                "$ref": "#/components/schemas/SyncStatusResponse"
              }
            ]
          }
        },
        "required": [
          "results",
          "syncStatus"
        ]
      },
      "LightEventOrderType": {
        "type": "string",
        "description": "Coarse classification of the order. `Limit`/`Market` carry a bigint-string `orderId`; `Conditional` carries a `Hex` `orderId` (the on-chain `orderHash`).",
        "enum": [
          "Limit",
          "Market",
          "Conditional"
        ]
      },
      "LightEventFeedOrderChangeResponse": {
        "type": "object",
        "properties": {
          "orderId": {
            "type": "string"
          },
          "orderType": {
            "$ref": "#/components/schemas/LightEventOrderType"
          },
          "status": {
            "type": "number",
            "enum": [
              0,
              1,
              2,
              3,
              4
            ],
            "description": "LimitOrderStatus { Filling : 0, Cancelled : 1, FullyFilled : 2, Expired : 3, Purged : 4 }"
          },
          "unfilledSize": {
            "type": "string",
            "description": "Bigint string of remaining unfilled size."
          },
          "eventFilledSize": {
            "type": "string",
            "description": "Bigint string of size filled by THIS event (0 for non-fill events)."
          },
          "side": {
            "type": "number",
            "enum": [
              0,
              1
            ],
            "description": "Side { LONG : 0, SHORT : 1 }."
          },
          "tick": {
            "type": "number",
            "description": "Tick index. Set for limit-order entries only."
          },
          "rate": {
            "type": "number",
            "description": "Decimal rate rounded to 5 fractional digits (e.g. 10.123% \u2192 0.10123). Set for market-order entries only."
          },
          "takerAcc": {
            "type": "string",
            "description": "Market-order user (`MarketAcc`) that filled the maker. Set only on `LimitOrderFilled` and `LimitOrderPartiallyFilled` entries."
          },
          "tif": {
            "type": "number",
            "enum": [
              0,
              1,
              2,
              3,
              4
            ],
            "description": "Support the market-order only. { GOOD_TIL_CANCELLED : 0, IMMEDIATE_OR_CANCEL : 1, FILL_OR_KILL : 2, ADD_LIQUIDITY_ONLY : 3, SOFT_ADD_LIQUIDITY_ONLY : 4 }."
          }
        },
        "required": [
          "orderId",
          "orderType",
          "status",
          "unfilledSize",
          "eventFilledSize",
          "side"
        ]
      },
      "LightEventFeedPositionChangeResponse": {
        "type": "object",
        "properties": {
          "size": {
            "type": "string",
            "description": "Signed bigint string. Net position size right AFTER this chain event applied (NOT post-block). Negative = net short, positive = net long."
          },
          "changedSize": {
            "type": "string",
            "description": "Bigint string. Signed delta from THIS event only. Sub-deltas of the same chain event are summed."
          }
        },
        "required": [
          "size",
          "changedSize"
        ]
      },
      "LightEventFeedItemResponse": {
        "type": "object",
        "properties": {
          "eventIndex": {
            "type": "number",
            "description": "Composite `(blockNumber, logIndex)` index of this event."
          },
          "blockNumber": {
            "type": "number"
          },
          "blockTimestamp": {
            "type": "number",
            "description": "Unix seconds. Same value across all entries from the same block."
          },
          "eventName": {
            "type": "string",
            "description": "On-chain event name in PascalCase. One of: `LimitOrderPlaced`, `LimitOrderFilled`, `LimitOrderPartiallyFilled`, `LimitOrderCancelled`, `LimitOrderForcedCancelled`, `OobOrdersPurged`, `SingleOrderExecuted`, `BulkOrdersExecuted`, `ConditionalOrderExecuted`, `SwapWithAmm`, `OtcSwap`, `ForceDeleverage`, `Liquidate`."
          },
          "eventType": {
            "type": "string",
            "enum": [
              "LimitOrderPlaced",
              "LimitOrderFilled",
              "LimitOrderCancelled",
              "MarketOrderExecuted",
              "OtcSwap",
              "Liquidate",
              "Deleverage",
              "Unknown"
            ],
            "description": "Coarse classification of `eventName`. { LimitOrderPlaced : LimitOrderPlaced, LimitOrderFilled : LimitOrderFilled, LimitOrderCancelled : LimitOrderCancelled, MarketOrderExecuted : MarketOrderExecuted, OtcSwap : OtcSwap, Liquidate : Liquidate, Deleverage : Deleverage, Unknown : Unknown }."
          },
          "txHash": {
            "type": "string"
          },
          "marketAcc": {
            "type": "string"
          },
          "marketId": {
            "type": "number"
          },
          "orders": {
            "description": "Order changes produced by this chain event for this `(marketAcc, marketId)`. Omitted when empty.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/LightEventFeedOrderChangeResponse"
            }
          },
          "position": {
            "description": "Single position move this chain event produced for this `(marketAcc, marketId)`. Omitted when the event did not move the position (e.g. `LimitOrderPlaced` / `LimitOrderCancelled`).",
            "allOf": [
              {
                "$ref": "#/components/schemas/LightEventFeedPositionChangeResponse"
              }
            ]
          }
        },
        "required": [
          "eventIndex",
          "blockNumber",
          "blockTimestamp",
          "eventName",
          "eventType",
          "txHash",
          "marketAcc",
          "marketId"
        ]
      },
      "LightEventFeedResponse": {
        "type": "object",
        "properties": {
          "results": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/LightEventFeedItemResponse"
            }
          },
          "resumeToken": {
            "type": "string",
            "description": "Resume token for fetching the next (older) page. Null if there are no more results."
          },
          "syncStatus": {
            "description": "Current sync status of the backend.",
            "allOf": [
              {
                "$ref": "#/components/schemas/SyncStatusResponse"
              }
            ]
          }
        },
        "required": [
          "results",
          "syncStatus"
        ]
      },
      "AddLiquidityIncentiveSideResponse": {
        "type": "object",
        "properties": {
          "incentiveRange": {
            "type": "number",
            "description": "Half-width of the incentive band around the mid implied APR for this side, expressed as a decimal APR (e.g. `0.005` = \u00b150bps). Resting size within this band qualifies."
          },
          "budgetPerHour": {
            "type": "number",
            "description": "Configured PENDLE budget for this side per hour (human-readable PENDLE, decimals already applied)."
          },
          "currentInRangeLiquidity": {
            "type": "string",
            "description": "Total in-range maker liquidity on this side at the latest hourly snapshot, in raw YU base units (stringified bigint)."
          },
          "currentCappedDistributionPerHour": {
            "type": "number",
            "description": "PENDLE actually emitted per hour after the liquidity-cap throttle is applied (capped at `budgetPerHour`)."
          },
          "currentEligibleShare": {
            "type": "number",
            "description": "Caller's share (0-1) of the in-range YU at the latest sync snapshot. **0 when `maker` is not provided.**"
          },
          "accumulatedReward": {
            "type": "number",
            "description": "PENDLE accumulated by `maker` on this side **for the current epoch only** \u2014 equals already-accumulated rewards plus the in-epoch projection up to the current block (human-readable PENDLE). Resets each epoch. **0 when `maker` is not provided.**"
          }
        },
        "required": [
          "incentiveRange",
          "budgetPerHour",
          "currentInRangeLiquidity",
          "currentCappedDistributionPerHour",
          "currentEligibleShare",
          "accumulatedReward"
        ]
      },
      "AddLiquidityIncentiveResponse": {
        "type": "object",
        "properties": {
          "long": {
            "description": "Incentive parameters and stats for **long-side** maker orders (resting bids on YU).",
            "allOf": [
              {
                "$ref": "#/components/schemas/AddLiquidityIncentiveSideResponse"
              }
            ]
          },
          "short": {
            "description": "Incentive parameters and stats for **short-side** maker orders (resting asks on YU).",
            "allOf": [
              {
                "$ref": "#/components/schemas/AddLiquidityIncentiveSideResponse"
              }
            ]
          }
        },
        "required": [
          "long",
          "short"
        ]
      },
      "FilledVolumeIncentiveResponse": {
        "type": "object",
        "properties": {
          "userMakerVolume": {
            "type": "number",
            "description": "Caller's notional traded as a **maker** (filled side of a resting limit order) since the start of the current epoch, expressed in YU. Filtered to `accountId = 0` only \u2014 non-zero sub-accounts are excluded. **0 when `maker` is not provided.**"
          },
          "totalMakerVolume": {
            "type": "number",
            "description": "Total maker-side notional executed across all participants in the current epoch (in YU)."
          },
          "totalEpochReward": {
            "type": "number",
            "description": "PENDLE budget allocated to the filled-volume track for this market in the current epoch."
          },
          "avgRewardPerYu": {
            "type": "number",
            "description": "Implied PENDLE-per-YU rate so far this epoch \u2014 `totalEpochReward / max(totalMakerVolume, 1)`. Use to estimate the marginal reward for additional maker fills."
          }
        },
        "required": [
          "userMakerVolume",
          "totalMakerVolume",
          "totalEpochReward",
          "avgRewardPerYu"
        ]
      },
      "MakerFeeRebateCampaignResponse": {
        "type": "object",
        "properties": {
          "feeShareRate": {
            "type": "number",
            "description": "Fraction of the gross taker fee rebated to makers this epoch (e.g. `0.2` = 20%). Resolved from the market epoch config, falling back to the protocol default. Market-level \u2014 present even when `maker` is omitted."
          },
          "currentEpochRebate": {
            "type": "number",
            "description": "Caller's maker-fee rebate accrued so far this epoch, in whole units of the market's collateral token. **0 when `maker` is not provided.**"
          },
          "takerFeeContribution": {
            "type": "number",
            "description": "Gross taker fee paid on fills of the caller's resting maker orders this epoch \u2014 the base the rebate is computed from \u2014 in collateral-token units. **0 when `maker` is not provided.**"
          }
        },
        "required": [
          "feeShareRate",
          "currentEpochRebate",
          "takerFeeContribution"
        ]
      },
      "MakerIncentiveCampaignResponse": {
        "type": "object",
        "properties": {
          "epochTimestamp": {
            "type": "number",
            "description": "Start of the current incentive epoch (Unix seconds, UTC). All `epoch`-scoped fields are measured against this anchor."
          },
          "addLiquidityIncentive": {
            "description": "Hourly maker resting-liquidity track (long + short sides).",
            "allOf": [
              {
                "$ref": "#/components/schemas/AddLiquidityIncentiveResponse"
              }
            ]
          },
          "filledVolumeIncentive": {
            "description": "Per-epoch filled-volume track (rewards based on share of maker-side fills).",
            "allOf": [
              {
                "$ref": "#/components/schemas/FilledVolumeIncentiveResponse"
              }
            ]
          },
          "makerFeeRebate": {
            "description": "Current-epoch maker-fee rebate (negative maker fee) for the caller on this market.",
            "allOf": [
              {
                "$ref": "#/components/schemas/MakerFeeRebateCampaignResponse"
              }
            ]
          }
        },
        "required": [
          "epochTimestamp",
          "addLiquidityIncentive",
          "filledVolumeIncentive",
          "makerFeeRebate"
        ]
      },
      "AmmIncentivesAllTimeRewards": {
        "type": "object",
        "properties": {
          "pendleRewards": {
            "type": "number",
            "description": "Lifetime PENDLE incentive rewards earned by the LP, in human-readable PENDLE (token-denominated, **not** USD-converted)."
          },
          "swapFeeRewards": {
            "type": "number",
            "description": "Lifetime swap-fee rewards earned by the LP, in whole units of the AMM's collateral token (decimals applied; **not** USD-converted)."
          }
        },
        "required": [
          "pendleRewards",
          "swapFeeRewards"
        ]
      },
      "AmmIncentivesMarketEntry": {
        "type": "object",
        "properties": {
          "marketId": {
            "type": "number",
            "description": "market id"
          },
          "unclaimedRewards": {
            "type": "string",
            "description": "Reserved \u2014 currently always `\"0\"`. Per-market unclaimed rewards are not yet wired through this endpoint; use the top-level `unclaimedAmountInUsd` for the aggregate."
          },
          "allTimeRewards": {
            "description": "Lifetime rewards earned by the LP on this market, split into PENDLE and swap-fee components (token-denominated, **not** USD-converted).",
            "allOf": [
              {
                "$ref": "#/components/schemas/AmmIncentivesAllTimeRewards"
              }
            ]
          }
        },
        "required": [
          "marketId",
          "unclaimedRewards",
          "allTimeRewards"
        ]
      },
      "AmmIncentivesResponse": {
        "type": "object",
        "properties": {
          "accruedAmountInUsd": {
            "type": "number",
            "description": "Lifetime accrued AMM rewards across **all** Boros AMMs the user has provided liquidity to, valued in USD at query time. Includes both already-claimed and currently-unclaimed amounts."
          },
          "unclaimedAmountInUsd": {
            "type": "number",
            "description": "Currently **unclaimed** AMM rewards across all AMMs, valued in USD at query time."
          },
          "perMarket": {
            "description": "Per-market breakdown \u2014 one entry per market the user has LP exposure in. Empty when `user` is omitted.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/AmmIncentivesMarketEntry"
            }
          }
        },
        "required": [
          "accruedAmountInUsd",
          "unclaimedAmountInUsd",
          "perMarket"
        ]
      },
      "RollingVolumeMarketEntry": {
        "type": "object",
        "properties": {
          "marketId": {
            "type": "number",
            "description": "Market this entry covers.",
            "example": 161
          },
          "volume": {
            "type": "string",
            "description": "Maker volume on this market over the window, summed across **all** users. Raw 18-decimal fixed-point integer as a string, denominated in this market's notional unit \u2014 not comparable across markets. `\"0\"` when the market had no maker fills in the window.",
            "example": "62760618982792013374397715"
          },
          "userVolume": {
            "type": "string",
            "description": "Maker volume of the requested `user` on this market over the same window, in the same units as `volume`. Present only when `user` was supplied; `\"0\"` when that user had no maker fills on this market.",
            "example": "14323940866189248225943680"
          }
        },
        "required": [
          "marketId",
          "volume"
        ]
      },
      "RollingVolumeResponse": {
        "type": "object",
        "properties": {
          "windowHours": {
            "type": "number",
            "description": "Length of the window in hours, echoed back as requested. The window ends at the time of the request.",
            "example": 24
          },
          "markets": {
            "description": "One entry per currently active market, sorted by `marketId` ascending. Markets with no maker fills in the window are included with `volume: \"0\"`. Values are per market and must not be summed across markets \u2014 see the endpoint description.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RollingVolumeMarketEntry"
            }
          },
          "syncStatus": {
            "description": "How far the indexer had progressed when this response was built \u2014 the last fully processed block and its timestamp. Fills in blocks after this are not yet reflected in the volumes.",
            "allOf": [
              {
                "$ref": "#/components/schemas/SyncStatusResponse"
              }
            ]
          }
        },
        "required": [
          "windowHours",
          "markets",
          "syncStatus"
        ]
      },
      "RollingTradeVolumeMarketEntry": {
        "type": "object",
        "properties": {
          "marketId": {
            "type": "number",
            "description": "Market this entry covers.",
            "example": 161
          },
          "totalVolume": {
            "type": "string",
            "description": "Traded volume on this market over the window, across all users. Raw 18-decimal integer as a string; `\"0\"` if the market had no volume.",
            "example": "62760618982792013374397715"
          },
          "userMakerVolume": {
            "type": "string",
            "description": "Volume from this `user`'s limit orders being filled. Same window and units. Present only when `user` was supplied.",
            "example": "14323940866189248225943680"
          },
          "userTakerVolume": {
            "type": "string",
            "description": "Volume from this `user`'s market orders. Same window and units.",
            "example": "2011845330099277184102400"
          }
        },
        "required": [
          "marketId",
          "totalVolume"
        ]
      },
      "RollingTradeVolumeResponse": {
        "type": "object",
        "properties": {
          "windowHours": {
            "type": "number",
            "description": "Window length in hours, echoed back. The window ends at the time of the request.",
            "example": 24
          },
          "markets": {
            "description": "One entry per active market, sorted by `marketId` ascending. Markets with no volume in the window are included with `\"0\"`.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RollingTradeVolumeMarketEntry"
            }
          },
          "syncStatus": {
            "description": "Last fully processed block and its timestamp. Fills in later blocks are not yet reflected in the volumes.",
            "allOf": [
              {
                "$ref": "#/components/schemas/SyncStatusResponse"
              }
            ]
          }
        },
        "required": [
          "windowHours",
          "markets",
          "syncStatus"
        ]
      },
      "AgentExpiryTimeResponse": {
        "type": "object",
        "properties": {
          "expiryTime": {
            "type": "number",
            "description": "Unix seconds (UTC) at which the agent's authorization expires. `0` if the agent has never been approved or has been revoked. Compare against the current time to determine if the agent is still valid.",
            "example": 1735689600
          }
        },
        "required": [
          "expiryTime"
        ]
      },
      "StrategyMarketResponse": {
        "type": "object",
        "properties": {
          "marketId": {
            "type": "number",
            "description": "Numeric market identifier (1-based)."
          },
          "address": {
            "type": "string",
            "description": "Market address"
          },
          "tokenId": {
            "type": "number",
            "description": "Collateral token id this market settles in."
          },
          "name": {
            "type": "string",
            "description": "Human-readable market name (e.g. `BTCUSDT`)."
          },
          "assetSymbol": {
            "type": "string",
            "description": "Asset symbol"
          },
          "maturity": {
            "type": "number",
            "description": "Market maturity (Unix seconds, UTC)."
          },
          "state": {
            "type": "string",
            "description": "Market lifecycle state (one of `Normal`, `Capped`, `Paused`, `Halted`)."
          },
          "impliedApr": {
            "type": "number",
            "description": "Market's current mid implied APR (1.0 = 100%)."
          },
          "maxLeverage": {
            "type": "number",
            "deprecated": true,
            "description": "DEPRECATED: cosmetic 1/kIM. Compute from `kIM` directly. Will be removed in a future release."
          },
          "maxPerpLeverage": {
            "type": "number",
            "description": "Maximum leverage allowed for perpetual-style (rolled) positions."
          },
          "ammId": {
            "type": "number",
            "description": "AMM ID"
          },
          "platformName": {
            "type": "string",
            "description": "Platform / venue label (e.g. `Binance`, `Hyperliquid`)."
          }
        },
        "required": [
          "marketId",
          "address",
          "tokenId",
          "name",
          "assetSymbol",
          "maturity",
          "state",
          "impliedApr",
          "maxLeverage",
          "maxPerpLeverage"
        ]
      },
      "StrategyResponse": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "Strategy ID"
          },
          "longMarket": {
            "description": "Market to be **longed** (buy YU). See `StrategyMarketResponse`.",
            "allOf": [
              {
                "$ref": "#/components/schemas/StrategyMarketResponse"
              }
            ]
          },
          "shortMarket": {
            "description": "Market to be **shorted** (sell YU). See `StrategyMarketResponse`.",
            "allOf": [
              {
                "$ref": "#/components/schemas/StrategyMarketResponse"
              }
            ]
          },
          "daysToMaturity": {
            "type": "number",
            "description": "Calendar days remaining until the shared maturity. Both markets are grouped by identical maturity, so this single value applies to both legs."
          },
          "impliedAprSpread": {
            "type": "number",
            "description": "Absolute spread `|market1.impliedApr - market2.impliedApr|` (always \u2265 0, decimal where 1.0 = 100%). The lower-APR market is assigned as `longMarket`, so this equals `shortMarket.impliedApr - longMarket.impliedApr`. The strategy harvests this spread to maturity."
          },
          "maxPerpLeverage": {
            "type": "number",
            "description": "Max perp leverage achievable on the spread = `min(10, longMarket.maxPerpLeverage, shortMarket.maxPerpLeverage)`. Hard cap of **10** applies on top of the per-market limits."
          },
          "aprTimesMaxLeverage": {
            "type": "number",
            "description": "Indicative annualized return at maximum allowable leverage, net of strategy fees and combined Boros margin (decimal, 1.0 = 100%). Used as the sort key."
          }
        },
        "required": [
          "id",
          "longMarket",
          "shortMarket",
          "daysToMaturity",
          "impliedAprSpread",
          "maxPerpLeverage",
          "aprTimesMaxLeverage"
        ]
      },
      "FindStrategiesResponse": {
        "type": "object",
        "properties": {
          "strategies": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/StrategyResponse"
            }
          },
          "totalCount": {
            "type": "number",
            "description": "Total number of strategies found"
          }
        },
        "required": [
          "strategies",
          "totalCount"
        ]
      },
      "GasPriceInfoResponse": {
        "type": "object",
        "properties": {
          "gasPriceWei": {
            "type": "object",
            "description": "Latest sampled gas price in **wei per gas unit** (stringified bigint). `null` if unavailable.",
            "nullable": true
          },
          "gasPriceUsd": {
            "type": "object",
            "description": "Latest sampled gas price in **USD per gas unit** (decimal). `null` if unavailable.",
            "nullable": true
          },
          "estimatedOrderGasCostUsd": {
            "type": "object",
            "description": "Estimated USD cost of placing **one limit order** at the current gas price (decimal). `null` if unavailable.",
            "nullable": true
          },
          "timestamp": {
            "type": "object",
            "description": "Unix seconds (UTC) when the gas-price sample was taken \u2014 useful to gauge staleness. `null` if unavailable.",
            "nullable": true
          },
          "chainId": {
            "type": "number",
            "description": "EVM chain id this sample is for. **Always `42161` (Arbitrum One)** today."
          }
        },
        "required": [
          "gasPriceWei",
          "gasPriceUsd",
          "estimatedOrderGasCostUsd",
          "timestamp",
          "chainId"
        ]
      },
      "LeaderboardEntryResponse": {
        "type": "object",
        "properties": {
          "rank": {
            "type": "number",
            "description": "1-based rank within this (period, tokenId) leaderboard. Ordinal (no ties)."
          },
          "root": {
            "type": "string",
            "description": "User wallet address \u2014 the `root` of the margin account."
          },
          "accountId": {
            "type": "number",
            "description": "Sub-account index under `root` (0\u2013255). `0` is the main sub-account."
          },
          "pnl": {
            "type": "string",
            "description": "Realized + unrealized PnL over the period, denominated in the collateral token. 18-decimal fixed-point integer as a string; the scale is 1e18 for every token, independent of its ERC-20 decimals."
          },
          "netBalance": {
            "type": "string",
            "description": "Account's current net balance, denominated in the collateral token. 18-decimal fixed-point integer as a string; the scale is 1e18 for every token, independent of its ERC-20 decimals."
          },
          "tradingVolume": {
            "type": "string",
            "description": "Cumulative notional traded over the period, denominated in the collateral token. 18-decimal fixed-point integer as a string; the scale is 1e18 for every token, independent of its ERC-20 decimals."
          },
          "maxCapital": {
            "type": "string",
            "description": "Peak capital deployed during the period \u2014 used as the denominator in ROI. Denominated in the collateral token, as an 18-decimal fixed-point integer string; the scale is 1e18 for every token, independent of its ERC-20 decimals."
          },
          "roi": {
            "type": "number",
            "description": "Return on capital over the period, expressed as a decimal (1.0 = +100%). Used as the ranking key."
          }
        },
        "required": [
          "rank",
          "root",
          "accountId",
          "pnl",
          "netBalance",
          "tradingVolume",
          "maxCapital",
          "roi"
        ]
      },
      "LeaderboardResponse": {
        "type": "object",
        "properties": {
          "snapshotTimestamp": {
            "type": "number",
            "description": "Unix seconds (UTC) of the snapshot used to compute this leaderboard \u2014 always at midnight. Snapshots are produced once per day."
          },
          "entries": {
            "description": "Page of leaderboard entries (sorted by `rank` ascending, i.e. by ROI descending).",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/LeaderboardEntryResponse"
            }
          },
          "totalEntries": {
            "type": "number",
            "description": "Total number of qualifying entries in this snapshot. Use to compute total page count."
          }
        },
        "required": [
          "snapshotTimestamp",
          "entries",
          "totalEntries"
        ]
      },
      "UserSearchResponse": {
        "type": "object",
        "properties": {
          "rank": {
            "type": "number",
            "description": "1-based rank in the snapshot. **Present only when the user is in the leaderboard** \u2014 omitted when computing fallback values from daily snapshots."
          },
          "pnl": {
            "type": "string",
            "description": "User's PnL over the period, denominated in the collateral token. 18-decimal fixed-point integer as a string; the scale is 1e18 for every token, independent of its ERC-20 decimals."
          },
          "netBalance": {
            "type": "string",
            "description": "User's current net balance, denominated in the collateral token. 18-decimal fixed-point integer as a string; the scale is 1e18 for every token, independent of its ERC-20 decimals."
          },
          "tradingVolume": {
            "type": "string",
            "description": "User's notional volume over the period, denominated in the collateral token. 18-decimal fixed-point integer as a string; the scale is 1e18 for every token, independent of its ERC-20 decimals."
          },
          "roi": {
            "type": "number",
            "description": "ROI over the period (1.0 = +100%). **Present only when the user is in the leaderboard.**"
          }
        },
        "required": [
          "pnl",
          "netBalance",
          "tradingVolume"
        ]
      },
      "TvlTokenBreakdownResponse": {
        "type": "object",
        "properties": {
          "address": {
            "type": "string",
            "description": "Collateral token address"
          },
          "symbol": {
            "type": "string"
          },
          "decimals": {
            "type": "number",
            "description": "ERC-20 `decimals` of the token. Use to convert `balance` to a human-readable amount."
          },
          "balance": {
            "type": "string",
            "description": "Raw on-chain balance held by `MarketHub` in **token base units** (stringified bigint).",
            "example": "1000000000"
          },
          "usdPrice": {
            "type": "string",
            "description": "Oracle USD price for **1 whole token** (decimals already applied), stringified decimal.",
            "example": "3500.25"
          },
          "tokenInUSD": {
            "type": "string",
            "description": "`balance / 10^decimals \u00d7 usdPrice`, stringified decimal. Sums to `totalInUSD`.",
            "example": "3500250.12"
          }
        },
        "required": [
          "address",
          "symbol",
          "decimals",
          "balance",
          "usdPrice",
          "tokenInUSD"
        ]
      },
      "TvlResponse": {
        "type": "object",
        "properties": {
          "totalInUSD": {
            "type": "string",
            "description": "Sum of `tokenInUSD` across all collateral tokens held by `MarketHub`, stringified decimal."
          },
          "breakdown": {
            "description": "Per-token breakdown \u2014 one entry per registered collateral token (assets with `isCollateral` set, regardless of whitelist status).",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/TvlTokenBreakdownResponse"
            }
          }
        },
        "required": [
          "totalInUSD",
          "breakdown"
        ]
      },
      "PendleSignTxDto": {
        "type": "object",
        "properties": {
          "account": {
            "type": "string",
            "description": "Packed account identifier (root address + account ID)",
            "example": "0x009dcf85824e024fea9e3ef583dccbea68edbc37b8"
          },
          "connectionId": {
            "type": "string",
            "description": "Unique connection identifier for nonce management",
            "example": "0xabcdef1234567890"
          },
          "nonce": {
            "type": "string",
            "description": "Transaction nonce as a bigint string, must be >= 1"
          }
        },
        "required": [
          "account",
          "connectionId",
          "nonce"
        ]
      },
      "AgentExecuteDto": {
        "type": "object",
        "properties": {
          "agent": {
            "type": "string"
          },
          "message": {
            "$ref": "#/components/schemas/PendleSignTxDto"
          },
          "signature": {
            "type": "string"
          },
          "calldata": {
            "type": "string",
            "description": "ABI-encoded calldata to be executed by the Boros Router contract",
            "example": "0x..."
          }
        },
        "required": [
          "agent",
          "message",
          "signature",
          "calldata"
        ]
      },
      "BulkAgentExecuteDto": {
        "type": "object",
        "properties": {
          "datas": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/AgentExecuteDto"
            }
          },
          "requireSuccess": {
            "type": "boolean",
            "default": false,
            "description": "If true, the on-chain `tryAggregate` reverts the entire batch when any inner call fails."
          },
          "skipReceipt": {
            "type": "boolean",
            "default": false,
            "description": "If true, return as soon as the bot reports the txHash \u2014 do not wait for the on-chain receipt to decode per-call status. The response will have `status` and `error` fields empty for every survivor."
          }
        },
        "required": [
          "datas"
        ]
      },
      "TxResponse": {
        "type": "object",
        "properties": {
          "txHash": {
            "type": "string"
          },
          "status": {
            "type": "string",
            "enum": [
              "success",
              "reverted"
            ]
          },
          "index": {
            "type": "number",
            "description": "On-chain `tryAggregate` position of this call. Includes any startIndex offset if v2 merged this submission with others."
          },
          "error": {
            "type": "string"
          }
        }
      },
      "ApproveAgentQueryDto": {
        "type": "object",
        "properties": {
          "approveAgentCalldata": {
            "type": "string",
            "description": "ABI-encoded calldata for the Router.approveAgent() function call",
            "example": "0x..."
          },
          "skipReceipt": {
            "type": "boolean",
            "description": "If true, return as soon as the bot reports the txHash. Default: false."
          }
        },
        "required": [
          "approveAgentCalldata"
        ]
      },
      "ApproveAgentResponse": {
        "type": "object",
        "properties": {
          "approveAgentResult": {
            "$ref": "#/components/schemas/TxResponse"
          }
        },
        "required": [
          "approveAgentResult"
        ]
      },
      "TraceQueryDto": {
        "type": "object",
        "properties": {
          "agent": {
            "type": "string",
            "description": "Agent address used when submitting the original batch"
          },
          "nonce": {
            "type": "string",
            "description": "Nonce (bigint string) of the calldata to look up"
          }
        },
        "required": [
          "agent",
          "nonce"
        ]
      },
      "TraceResponse": {
        "type": "object",
        "properties": {
          "submissionStatus": {
            "type": "string",
            "enum": [
              "HAVENT_SEEN",
              "PROCESSING",
              "PROCESSED",
              "SEND_FAILED"
            ],
            "description": "Status of the submission the queried (agent, nonce) belongs to"
          },
          "nonceStatus": {
            "type": "string",
            "enum": [
              "HAVENT_SEEN",
              "PROCESSING",
              "PROCESSED",
              "SEND_FAILED"
            ],
            "description": "Status of the specific (agent, nonce). Mirrors the submission status today."
          },
          "result": {
            "description": "Per-nonce result, shaped like one entry from /bulk-calls. Set when submissionStatus is PROCESSED and a tx was actually broadcast for this nonce (i.e. simulation passed for this entry).",
            "allOf": [
              {
                "$ref": "#/components/schemas/TxResponse"
              }
            ]
          }
        },
        "required": [
          "submissionStatus",
          "nonceStatus"
        ]
      },
      "TxStatusQueryDto": {
        "type": "object",
        "properties": {
          "txHash": {
            "type": "string",
            "description": "On-chain transaction hash returned by /bulk-calls (or /approve)"
          }
        },
        "required": [
          "txHash"
        ]
      },
      "DedicatedTxStatusItem": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "Transaction status"
          },
          "index": {
            "type": "number",
            "description": "Index in tryAggregate batch"
          },
          "error": {
            "type": "string",
            "description": "Error message if reverted"
          }
        },
        "required": [
          "status",
          "index"
        ]
      },
      "DedicatedTxStatusResponseV2": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "Overall transaction status: \"success\" if all calls succeeded, \"reverted\" if the transaction reverted"
          },
          "blockNumber": {
            "type": "number",
            "description": "Block number of the transaction"
          },
          "blockTimestamp": {
            "type": "number",
            "description": "Block timestamp of the transaction"
          },
          "statuses": {
            "description": "Transaction status items",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DedicatedTxStatusItem"
            }
          }
        },
        "required": [
          "status",
          "blockNumber",
          "blockTimestamp",
          "statuses"
        ]
      },
      "LimitOrderPlacedEventItem": {
        "type": "object",
        "properties": {
          "marketAddress": {
            "type": "string",
            "description": "Market address that emitted the event"
          },
          "marketId": {
            "type": "number",
            "description": "Market ID"
          },
          "orderIds": {
            "description": "Order IDs placed",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "sizes": {
            "description": "Corresponding sizes for each order",
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "required": [
          "marketAddress",
          "marketId",
          "orderIds",
          "sizes"
        ]
      },
      "LimitOrderCancelledEventItem": {
        "type": "object",
        "properties": {
          "marketAddress": {
            "type": "string",
            "description": "Market address that emitted the event"
          },
          "marketId": {
            "type": "number",
            "description": "Market ID"
          },
          "orderIds": {
            "description": "Order IDs cancelled",
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "required": [
          "marketAddress",
          "marketId",
          "orderIds"
        ]
      },
      "MarketOrderExecutedItem": {
        "type": "object",
        "properties": {
          "marketAddress": {
            "type": "string",
            "description": "Market address"
          },
          "marketId": {
            "type": "number",
            "description": "Market ID"
          },
          "orderId": {
            "type": "string",
            "description": "Market order ID (derived from block + log index)"
          },
          "user": {
            "type": "string",
            "description": "MarketAcc of the order taker"
          },
          "side": {
            "type": "number",
            "enum": [
              0,
              1
            ],
            "description": "Side { LONG : 0, SHORT : 1 }"
          },
          "size": {
            "type": "string",
            "description": "Absolute filled size as a bigint string"
          }
        },
        "required": [
          "marketAddress",
          "marketId",
          "orderId",
          "user",
          "side",
          "size"
        ]
      },
      "DedicatedTxStatusWithEventsItem": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "Transaction status"
          },
          "index": {
            "type": "number",
            "description": "Index in tryAggregate batch"
          },
          "error": {
            "type": "string",
            "description": "Error message if reverted"
          },
          "limitOrdersPlaced": {
            "description": "Limit orders placed by this call",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/LimitOrderPlacedEventItem"
            }
          },
          "limitOrdersCancelled": {
            "description": "Limit orders cancelled by this call",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/LimitOrderCancelledEventItem"
            }
          },
          "marketOrdersExecuted": {
            "description": "Market orders executed by this call",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/MarketOrderExecutedItem"
            }
          }
        },
        "required": [
          "status",
          "index",
          "limitOrdersPlaced",
          "limitOrdersCancelled",
          "marketOrdersExecuted"
        ]
      },
      "DedicatedTxStatusWithEventsResponse": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "Overall transaction status: \"success\" if all calls succeeded, \"reverted\" if the transaction reverted"
          },
          "blockNumber": {
            "type": "number",
            "description": "Block number of the transaction"
          },
          "blockTimestamp": {
            "type": "number",
            "description": "Block timestamp of the transaction"
          },
          "statuses": {
            "description": "Per-call status with associated limit order events",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DedicatedTxStatusWithEventsItem"
            }
          }
        },
        "required": [
          "status",
          "blockNumber",
          "blockTimestamp",
          "statuses"
        ]
      },
      "DedicatedBulkAgentExecuteDto": {
        "type": "object",
        "properties": {
          "datas": {
            "description": "Array of agent execute requests. All must share the same agent address and root account.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/AgentExecuteDto"
            }
          },
          "requireSuccess": {
            "type": "boolean",
            "default": false,
            "description": "If true, reverts the entire batch when any single transaction fails."
          },
          "simulate": {
            "type": "boolean",
            "default": false,
            "description": "If true, simulates the batch before sending. When `requireSuccess=true`, returns failedSimulations without broadcasting if any call would revert. When `requireSuccess=false`, filters reverters out of the batch before sending."
          }
        },
        "required": [
          "datas"
        ]
      },
      "FailedSimulationItem": {
        "type": "object",
        "properties": {
          "index": {
            "type": "number",
            "description": "Index of the calldata in the original sorted batch"
          },
          "calldata": {
            "type": "string",
            "description": "The calldata that failed simulation"
          },
          "reason": {
            "type": "string",
            "description": "Reason for failure"
          }
        },
        "required": [
          "index",
          "calldata",
          "reason"
        ]
      },
      "DedicatedTxResponse": {
        "type": "object",
        "properties": {
          "txHash": {
            "type": "string",
            "nullable": true,
            "description": "Transaction hash, null if no calldata was sent"
          },
          "failedSimulations": {
            "description": "List of calldata that failed simulation. Only present when simulate=true and some calls reverted.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FailedSimulationItem"
            }
          }
        }
      },
      "CreateSigningKeyDto": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Root wallet that owns the keys",
            "example": "0x1234567890123456789012345678901234567890"
          },
          "timestamp": {
            "type": "number",
            "description": "Unix milliseconds; must be within 5 minutes of server time",
            "example": 1786000000000
          },
          "nonce": {
            "type": "string",
            "description": "Single-use 32-byte hex nonce",
            "example": "0x3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f"
          },
          "signature": {
            "type": "string",
            "description": "EIP-712 ApiKeyAction signature by the root wallet"
          },
          "name": {
            "type": "string",
            "description": "Your label for the key. 1-32 printable ASCII characters, unique among your live keys.",
            "example": "prod-mm-bot-01"
          },
          "expiresInDays": {
            "type": "number",
            "description": "Expire the key after this many days. Omit for no expiry.",
            "example": 90
          }
        },
        "required": [
          "root",
          "timestamp",
          "nonce",
          "signature",
          "name"
        ]
      },
      "SigningKeySecretDto": {
        "type": "object",
        "properties": {
          "keyId": {
            "type": "string",
            "example": "pdk_9f3c1e7a4b20d85c"
          },
          "name": {
            "type": "string",
            "example": "prod-mm-bot-01"
          },
          "createdAt": {
            "type": "number",
            "description": "Unix seconds",
            "example": 1786000000
          },
          "expiresAt": {
            "type": "number",
            "description": "Unix seconds",
            "example": 1793776000
          },
          "lastUsedAt": {
            "type": "number",
            "description": "Unix seconds of the last successful authenticated request. Best-effort telemetry, flushed periodically \u2014 may lag by a few minutes.",
            "example": 1786000000
          },
          "root": {
            "type": "string",
            "example": "0x1234567890123456789012345678901234567890"
          },
          "privateKey": {
            "type": "string",
            "description": "Ed25519 private key, PKCS#8 PEM. Shown once \u2014 store it now. Sign every request JWT with it (alg EdDSA).",
            "example": "-----BEGIN PRIVATE KEY-----\\nMC4CAQAwBQYDK2VwBCIEIG...\\n-----END PRIVATE KEY-----\\n"
          }
        },
        "required": [
          "keyId",
          "name",
          "createdAt",
          "root",
          "privateKey"
        ]
      },
      "SigningKeyDto": {
        "type": "object",
        "properties": {
          "keyId": {
            "type": "string",
            "example": "pdk_9f3c1e7a4b20d85c"
          },
          "name": {
            "type": "string",
            "example": "prod-mm-bot-01"
          },
          "createdAt": {
            "type": "number",
            "description": "Unix seconds",
            "example": 1786000000
          },
          "expiresAt": {
            "type": "number",
            "description": "Unix seconds",
            "example": 1793776000
          },
          "lastUsedAt": {
            "type": "number",
            "description": "Unix seconds of the last successful authenticated request. Best-effort telemetry, flushed periodically \u2014 may lag by a few minutes.",
            "example": 1786000000
          }
        },
        "required": [
          "keyId",
          "name",
          "createdAt"
        ]
      },
      "UpdateSigningKeyDto": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Root wallet that owns the keys",
            "example": "0x1234567890123456789012345678901234567890"
          },
          "timestamp": {
            "type": "number",
            "description": "Unix milliseconds; must be within 5 minutes of server time",
            "example": 1786000000000
          },
          "nonce": {
            "type": "string",
            "description": "Single-use 32-byte hex nonce",
            "example": "0x3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f"
          },
          "signature": {
            "type": "string",
            "description": "EIP-712 ApiKeyAction signature by the root wallet"
          },
          "keyId": {
            "type": "string",
            "description": "Key to act on. Carried in the body, never the URL path.",
            "example": "pdk_9f3c1e7a4b20d85c"
          },
          "name": {
            "type": "string",
            "description": "New label. Same rules as create.",
            "example": "prod-mm-bot-02"
          }
        },
        "required": [
          "root",
          "timestamp",
          "nonce",
          "signature",
          "keyId",
          "name"
        ]
      },
      "SigningKeyIdDto": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Root wallet that owns the keys",
            "example": "0x1234567890123456789012345678901234567890"
          },
          "timestamp": {
            "type": "number",
            "description": "Unix milliseconds; must be within 5 minutes of server time",
            "example": 1786000000000
          },
          "nonce": {
            "type": "string",
            "description": "Single-use 32-byte hex nonce",
            "example": "0x3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f3f"
          },
          "signature": {
            "type": "string",
            "description": "EIP-712 ApiKeyAction signature by the root wallet"
          },
          "keyId": {
            "type": "string",
            "description": "Key to act on. Carried in the body, never the URL path.",
            "example": "pdk_9f3c1e7a4b20d85c"
          }
        },
        "required": [
          "root",
          "timestamp",
          "nonce",
          "signature",
          "keyId"
        ]
      },
      "Eip712DomainDto": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "example": "Pendle Boros Router"
          },
          "version": {
            "type": "string",
            "example": "1.0"
          },
          "chainId": {
            "type": "number",
            "example": 42161
          },
          "verifyingContract": {
            "type": "string",
            "description": "Boros router \u2014 differs per environment",
            "example": "0x0000000000000000000000000000000000000000"
          }
        },
        "required": [
          "name",
          "version",
          "chainId",
          "verifyingContract"
        ]
      },
      "StopOrderRowResponse": {
        "type": "object",
        "properties": {
          "orderId": {
            "type": "string",
            "description": "Order hash. Pass to `GET /stop-orders/detail` or `POST /cancel`."
          },
          "root": {
            "type": "string",
            "description": "Root wallet that owns the order. Always the API key holder."
          },
          "marketId": {
            "type": "number"
          },
          "accountId": {
            "type": "number",
            "description": "Sub-account index (0-255) the order belongs to."
          },
          "marketAcc": {
            "type": "string",
            "description": "Packed market account of the maker."
          },
          "isCross": {
            "type": "boolean",
            "description": "`true` \u2192 cross-margin, `false` \u2192 isolated on `marketId`."
          },
          "side": {
            "type": "number",
            "enum": [
              0,
              1
            ],
            "description": "Side { LONG : 0, SHORT : 1 }"
          },
          "status": {
            "type": "number",
            "enum": [
              0,
              1,
              2,
              3,
              4,
              5,
              6,
              7,
              8
            ],
            "description": "Status { Filling : 0, Cancelled : 1, FullyFilled : 2, Expired : 3, Purged : 4, Pending : 5, Executing : 6, Retrying : 7, Failed : 8 }. Note the values are not contiguous with the limit-order enum."
          },
          "orderType": {
            "type": "number",
            "enum": [
              2,
              3
            ],
            "description": "Order type { TAKE_PROFIT_MARKET : 2, STOP_LOSS_MARKET : 3 }. Same numbering as the limit-order `orderType`, minus its two non-conditional members."
          },
          "stopApr": {
            "type": "number",
            "description": "Trigger APR. The order is submitted once the market crosses this rate."
          },
          "placedSize": {
            "type": "string",
            "description": "BigInt string of the notional size, scaled by 10^18."
          },
          "unfilledSize": {
            "type": "string",
            "description": "BigInt string of the size still unfilled, scaled by 10^18."
          },
          "marginRequired": {
            "type": "string",
            "description": "BigInt string of the initial margin this order reserves, scaled by 10^18."
          },
          "tick": {
            "type": "number",
            "description": "Tick of the trigger APR (the same threshold as `stopApr`), snapped to the market tick step. NOT the execution guard tick of the order that gets placed \u2014 that is pinned to the side extreme and is not returned."
          },
          "isClosePosition": {
            "type": "boolean",
            "description": "Whether the order closes the whole position when it triggers."
          },
          "blockTimestamp": {
            "type": "number",
            "description": "Unix seconds of the last state change, not of placement \u2014 cancelling or failing an order rewrites it."
          }
        },
        "required": [
          "orderId",
          "root",
          "marketId",
          "accountId",
          "marketAcc",
          "isCross",
          "side",
          "status",
          "orderType",
          "stopApr",
          "placedSize",
          "unfilledSize",
          "marginRequired",
          "tick",
          "isClosePosition",
          "blockTimestamp"
        ]
      },
      "StopOrdersResponse": {
        "type": "object",
        "properties": {
          "results": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/StopOrderRowResponse"
            }
          },
          "resumeToken": {
            "type": "string",
            "description": "Resume token for fetching the next page. Null if there are no more results."
          },
          "syncStatus": {
            "description": "Current sync status of the backend",
            "allOf": [
              {
                "$ref": "#/components/schemas/SyncStatusResponse"
              }
            ]
          }
        },
        "required": [
          "results",
          "syncStatus"
        ]
      },
      "StopOrderRequestResponse": {
        "type": "object",
        "properties": {
          "account": {
            "type": "string",
            "description": "Packed 44-character account (`root` 20B \u00b7 `accountId` 1B) derived from the API key. Part of the signed struct.",
            "example": "0x009dcf85824e024fea9e3ef583dccbea68edbc37b8"
          },
          "cross": {
            "type": "boolean",
            "description": "`true` \u2192 cross-margin account, `false` \u2192 isolated on `marketId`."
          },
          "marketId": {
            "type": "number",
            "description": "Market the triggered order executes on."
          },
          "side": {
            "type": "number",
            "enum": [
              0,
              1
            ],
            "description": "Side { LONG : 0, SHORT : 1 }"
          },
          "tif": {
            "type": "number",
            "enum": [
              0,
              1,
              2,
              3,
              4
            ],
            "description": "TimeInForce { GOOD_TIL_CANCELLED : 0, IMMEDIATE_OR_CANCEL : 1, FILL_OR_KILL : 2, ADD_LIQUIDITY_ONLY : 3, SOFT_ADD_LIQUIDITY_ONLY : 4 }. Always FILL_OR_KILL \u2014 a triggered stop takes liquidity or nothing."
          },
          "size": {
            "type": "string",
            "description": "BigInt string of the notional size, scaled by 10^18. `2^256 - 1` when `closePosition` was requested with `size = 0`."
          },
          "tick": {
            "type": "number",
            "description": "Execution guard tick \u2014 pinned to the side's extreme so the order fills at any rate once triggered."
          },
          "reduceOnly": {
            "type": "boolean",
            "description": "Always `true` \u2014 a stop order may only reduce an existing position."
          },
          "salt": {
            "type": "string",
            "description": "BigInt string salt making the order hash unique."
          },
          "expiry": {
            "type": "string",
            "description": "BigInt string expiry in unix seconds."
          },
          "offchainCondition": {
            "type": "string",
            "description": "Raw trigger condition. Its keccak256 is the last field of the signed `actionHash`; send it back on `POST /stop-orders/place` as the top-level `offchainCondition` \u2014 the server hashes it itself."
          }
        },
        "required": [
          "account",
          "cross",
          "marketId",
          "side",
          "tif",
          "size",
          "tick",
          "reduceOnly",
          "salt",
          "expiry",
          "offchainCondition"
        ]
      },
      "PrepareTpslStopOrderResponse": {
        "type": "object",
        "properties": {
          "req": {
            "$ref": "#/components/schemas/StopOrderRequestResponse"
          },
          "offchainCondition": {
            "type": "string",
            "description": "Raw trigger condition, unhashed. Send it as `offchainCondition` on `POST /stop-orders/place` \u2014 the backend needs it to evaluate the trigger."
          }
        },
        "required": [
          "req",
          "offchainCondition"
        ]
      },
      "StopOrderResponse": {
        "type": "object",
        "properties": {
          "result": {
            "$ref": "#/components/schemas/StopOrderRowResponse"
          },
          "syncStatus": {
            "description": "Current sync status of the backend",
            "allOf": [
              {
                "$ref": "#/components/schemas/SyncStatusResponse"
              }
            ]
          }
        },
        "required": [
          "result",
          "syncStatus"
        ]
      },
      "PlaceStopOrderMsgDto": {
        "type": "object",
        "properties": {
          "actionHash": {
            "type": "string",
            "description": "Action hash the agent signed to authorise this placement.",
            "example": "0xabcdef1234567890abcdef1234567890abcdef1234567890abcdef1234567890"
          }
        },
        "required": [
          "actionHash"
        ]
      },
      "PlaceStopOrderRequestDto": {
        "type": "object",
        "properties": {
          "cross": {
            "type": "boolean",
            "description": "`true` \u2192 cross-margin account, `false` \u2192 isolated account on `marketId`."
          },
          "marketId": {
            "type": "number",
            "minimum": 1,
            "description": "Market the triggered order executes on."
          },
          "side": {
            "type": "number",
            "enum": [
              0,
              1
            ],
            "description": "Side { LONG : 0, SHORT : 1 }"
          },
          "tif": {
            "type": "number",
            "enum": [
              0,
              1,
              2,
              3,
              4
            ],
            "description": "TimeInForce { GOOD_TIL_CANCELLED : 0, IMMEDIATE_OR_CANCEL : 1, FILL_OR_KILL : 2, ADD_LIQUIDITY_ONLY : 3, SOFT_ADD_LIQUIDITY_ONLY : 4 }"
          },
          "size": {
            "type": "string",
            "description": "BigInt string of the notional size, scaled by 10^18."
          },
          "tick": {
            "type": "number",
            "minimum": -32768,
            "maximum": 32767,
            "description": "Signed integer tick on the discrete rate ladder, in [-32768, 32767] (int16). Maps to APR via `rate = 1.00005^(tick \u00d7 tickStep) - 1`, where `tickStep` is per-market."
          },
          "reduceOnly": {
            "type": "boolean",
            "description": "If `true` the triggered order can only reduce an existing position, never open one."
          },
          "salt": {
            "type": "string",
            "description": "BigInt string salt making the order hash unique."
          },
          "expiry": {
            "type": "string",
            "description": "BigInt string expiry in unix seconds. The order is dropped once passed, triggered or not."
          }
        },
        "required": [
          "cross",
          "marketId",
          "side",
          "tif",
          "size",
          "tick",
          "reduceOnly",
          "salt",
          "expiry"
        ]
      },
      "PlaceStopOrderBodyDto": {
        "type": "object",
        "properties": {
          "agent": {
            "type": "string",
            "description": "Address of the agent whose key produced `placeSignature`. Must be an agent approved by the API key owner \u2014 the account is derived from the key, never from this field."
          },
          "placeMsg": {
            "$ref": "#/components/schemas/PlaceStopOrderMsgDto"
          },
          "placeSignature": {
            "type": "string",
            "description": "EIP-712 signature over `placeMsg.actionHash` from `agent`."
          },
          "request": {
            "$ref": "#/components/schemas/PlaceStopOrderRequestDto"
          },
          "offchainCondition": {
            "type": "string",
            "description": "Raw (unhashed) offchain condition from `GET /stop-orders/prepare`. Its keccak256 is what `placeMsg.actionHash` commits to, so sending a different one fails the hash check."
          },
          "type": {
            "type": "number",
            "enum": [
              2,
              3
            ],
            "description": "StopAprOrderType { TAKE_PROFIT_MARKET : 2, STOP_LOSS_MARKET : 3 }"
          },
          "closePosition": {
            "type": "boolean",
            "description": "If `true` the trigger closes the whole position, ignoring `request.size`."
          }
        },
        "required": [
          "agent",
          "placeMsg",
          "placeSignature",
          "request",
          "offchainCondition",
          "type",
          "closePosition"
        ]
      },
      "PlaceStopOrderResponse": {
        "type": "object",
        "properties": {
          "orderHash": {
            "type": "string",
            "description": "Hash identifying the placed stop order \u2014 the `orderId` used by the list, get and cancel endpoints.",
            "example": "0xabcdef1234567890abcdef1234567890abcdef1234567890abcdef1234567890"
          }
        },
        "required": [
          "orderHash"
        ]
      },
      "CancelStopOrdersBodyDto": {
        "type": "object",
        "properties": {
          "agent": {
            "type": "string",
            "description": "Address of the agent whose key produced `cancelSignature`. Must be an agent approved by the API key owner \u2014 the account is derived from the key, never from this field."
          },
          "orderIds": {
            "description": "Conditional-order hashes to cancel, 1-50 per request. All-or-nothing: the request fails if any id is unknown, already finalised, or owned by another account.",
            "example": [
              "0xabcdef1234567890abcdef1234567890abcdef1234567890abcdef1234567890"
            ],
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "cancelSignature": {
            "type": "string",
            "description": "EIP-712 cancellation signature from `agent`."
          }
        },
        "required": [
          "agent",
          "orderIds",
          "cancelSignature"
        ]
      },
      "CancelStopOrdersResponse": {
        "type": "object",
        "properties": {
          "message": {
            "type": "string",
            "example": "Order cancelled successfully"
          }
        },
        "required": [
          "message"
        ]
      }
    }
  }
}
